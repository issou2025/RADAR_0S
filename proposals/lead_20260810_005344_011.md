# Project Proposal — Unknown

## Project Understanding
You need assistance with: 
**P0 replacement #2416: eliminate root import of mutable checkout during snapshot signer install**

*Project description:*
Mode: RUNNER_TASK
Parent: #2414 #2407 #2395 #2383 #2343
Supersedes-for-review: PR #2416 head 8ceb4a59cdda364cbabae9539785bad2b1114d20
Repository: alanua/Skeleton
Base: main@5e549ad32d3a60b4f7fd823107a798acf36d2350
Priority: P0 / RUN_NOW
Risk: yellow
Operator Approval: EXPLICIT_MINIMAL_HOME_EDGE_SNAPSHOT_ACCESS_REPAIR_2026_08_09
Privacy Boundary: PRIVATE_CONTROLLER_CREDENTIAL / PUBLIC_SAFE_STATUS_ONLY

Purpose
Keep #2416's useful exact approval binding and immutable runtime layout, but remove the remaining privilege-escalation path in installer activation. The root-run installer currently imports mutable Runner checkout Python via `PYTHONPATH=$REPO_ROOT` to generate the signer payload/wrapper. An unprivileged Runner that can mutate the checkout can therefore cause arbitrary repository import code to execute as root during install.

```task
schema: skeleton.runner_task.v1
repo: alanua/Skeleton
base: main@5e549ad32d3a60b4f7fd823107a798acf36d2350
task_kind: code_generation
payload:
  operation: finalize_snapshot_signer_static_payload_install
  task: >-
    Rebuild the useful #2416 snapshot signer on exact current main. Preserve exact code-owned operator approval `EXPLICIT_MINIMAL_HOME_EDGE_SNAPSHOT_ACCESS_REPAIR_2026_08_09`, fixed node/lane/run_as/script/interpreter/timeout/output/public/idempotency/request-id/nonce authority, Runner-owned existing execute_home_edge_request transport, private one-shot artifact handling, strict non-executing HMAC config parser, minimal environment, complete root-owned immutable installed runtime outside the repo, and exact Runner-service-user no-argv sudoers. Fix the installer so no root process imports, executes, evals, compiles, or sets PYTHONPATH/cwd into mutable repository/worktree/home/agent content. Store the signer payload and wrapper as reviewed inert repository files (or equivalent static byte content) that the installer validates as regular non-symlink bounded files and copies atomically as data; any syntax/behavior validation that requires executing source must occur unprivileged before the protected install, never by importing mutable checkout under root. The final privileged signer may execute only the installed root-owned payload after installation.
requested_capabilities:
  - repository_read
  - repository_write
  - test_execution
allowed_files:
  - core/home_edge/**
  - scripts/**
  - tests/**
  - docs/home_edge/**
forbidden_actions:
  - no live Home Edge execution
  - no merge/deploy/runtime sync
  - no HMAC exposure to Runner/Codex
  - no root import/execution/eval/compile from repository/worktree/home/agent paths
  - no root `PYTHONPATH=$REPO_ROOT` or repo cwd
  - no shell sourcing of credentials
  - no generic sudo shell/python/bash/cat permission
  - no second executor/transport
requirements:
  - signer payload/wrapper exist as reviewed static data or otherwise can be copied without privileged repository Python import;
  - root installer validates source file type/symlink/mode/size and atomically copies to fixed root-owned non-group/world-writable install/exec trees;
  - sudoers activation happens only after immutable installed files are in place and is bound only to actual Runner service user + exact no-argv executable;
  - installer process never imports/executes mutable checkout Python, even for generation/smoke tests;
  - installed signer uses fixed system Python and installed immutable payload only;
  - exact operator approval mismatch is rejected before credential read/sign;
  - returned signed request is revalidated for exact approval and all authority fields before transport;
  - signer never executes transport and never reads/copies live source/private artifact;
  - no direct Runner HMAC path in live execution flow;
  - child-HMAC isolation and queue replenisher tests remain green.
required_tests:
  - installer text/behavior contains no privileged `PYTHONPATH`, repo import, `python -c`, heredoc import, eval/exec/compile of checkout content;
  - mutate checkout Python before/during modeled install cannot change installed signer payload bytes except through the explicitly copied reviewed static signer files;
  - installed signer runs with repository removed/unavailable;
  - installed tree files are root-owned/not group-world writable in installer model;
  - exact approved unsigned request signs successfully;
  - wrong/missing approval causes zero credential reads/sign actions;
  - altered signed approval causes zero transport;
  - arbitrary argv/oversize/extra stdin rejected before credential read;
  - full pytest + py_compile + git diff --check pass.
validation_commands:
  - [python3, -m, pytest, -q]
  - [python3, -m, py_compile, core/home_edge/media_source_snapshot.py]
  - [git, diff, --check]
validation_timeout_seconds: 1800
expected_output:
  - one draft replacement PR against exact current main
  - exact head SHA and changed files
  - proof root installer treats checkout only as inert reviewed file source and never executes/imports it
  - focused/full tests PASS
  - no live side effects
privacy_boundary: PRIVATE_CONTROLLER_CREDENTIAL / PUBLIC_SAFE_STATUS_ONLY
idempotency_key: snapshot-signer-static-payload-no-root-checkout-import-20260809-v1
```

After clean exact-head review and explicit protected merge approval: squash merge -> runtime_sync_main -> protected signer install/activation -> exactly one read-only `home_edge_01_media_source_snapshot_v1` -> canonicalize exact live source -> unblock #2343.

## Scope of Work
- Asset analysis and workspace initialization.
- Core modeling / development based on specifications.
- Technical validation and quality checks.
- Incorporation of review feedback.
- Clean handover of source files and documentation.

## Required Files & Inputs
1. Complete reference files (drawings, access tokens, test data).
2. Exact dimensional specs or business rules.
3. Schedule/deadline expectations.

## Estimated Price and Timeline
- **Estimated Price:** 300 - 800 USD
- **Estimated Timeline:** 3 to 7 business days (to be refined after reviewing the final assets).

## Project Questions
To help me refine this estimate, please clarify:
1. Could you describe the project scope and deliverables in detail?
2. Do you have any existing drawings or reference documents?
3. What is your target budget range?
4. What is your expected timeline?
5. Which tools/software do you prefer for this project?

## Agreement Terms
The final source files will be delivered upon approval of the milestones. Substantial revisions outside the agreed scope will require a change order.
