# Project Proposal — Flutter App

## Project Understanding
You need assistance with: 
**Complete store, Auth, RevenueCat and signed-device provisioning after the prototype**

*Project description:*
Part of #25, the implementation handoff for #1.

## Outcome and readiness

Human handoff: resolve the listed decisions/actions and record the result; dependent implementation must not invent these choices.

Blocked by: #3, #38, #47, #48, #49. These are completion prerequisites, not permission to start store enrolment early.

## Decision sources and glossary

Decisions: #2 #3 #7 #11 #12 #13 #15 #17 #23 #24. Read the latest resolution/feedback comments, not just the original questions. #19's final accepted flow supersedes its initial review-screen proposal. ADR 0006 supersedes earlier wording about runtime Constraint validation; ADR 0007 moves Plan fields into `users/{uid}/server/plan`.

Glossary terms from [CONTEXT.md](https://github.com/CommRogue/recipe-app/blob/main/CONTEXT.md): Subscription, Plan, Profile.

Read applicable `AGENTS.md`, `docs/adr/`, and `docs/later-versions.md` hooks. Android/iOS, English, single-Recipe Cooking Sessions only; deferred features stay deferred. #3 enrolment is intentionally postponed until after prototype development (owner decision on #25, 2026-09-21).

## Starting points

scripts/wizard-store-accounts.sh; app/README.md; app/android/fastlane; app/ios/fastlane; .github/workflows/release.yml; .github/workflows/patch.yml

Scaffold inspected at `a4844e8e92f60558a7f0c0cd7ceddce929dfd26a`; recheck current code before changing it. Existing scaffold claims do not imply feature completion. Source paths above are repository-relative unless an archived branch is named. Native UI work follows `DESIGN.md` and the impeccable skill; ordinary user data stays client-direct to Firestore. Do not put secrets or real user data into issue evidence.

## Scope and required behavior

Deferred until the developer is ready after prototype development; do not start enrolment automatically. Resume existing #3 rather than duplicating it. Use wizard for human-only dashboard/secrets steps, performing agent-accessible setup directly.
Complete Apple/Play app records/signing/release secrets and agreements/banking, then RevenueCat products/monthly-yearly offering/plus entitlement/yearly-only trial and dev/prod webhook configuration. Complete prod Google Auth OAuth client (explicit #24 gap), Apple provider Services ID/keys and native entitlements, release and Play app-signing SHA restrictions, and refreshed flavor config where needed. Verify sign-in/account switching on release-signed Android/iOS. Register App Check production providers when required. Use existing Shorebird/fastlane pipeline; first Play upload/manual setup and physical iPhone testing remain human work. Store secrets only in approved secret stores, never issue comments.

## Acceptance and verification

Record nonsecret resource ids and evidence of Google/Apple real-device sign-in, signed build installed from Play internal/TestFlight, sandbox monthly/yearly/trial purchase→server Plan, restore, renewal/expiry/refund and account switching. Exercise Apple revocation and appropriate App Check. Existing CI green/debug APK does not count as installability or purchase proof.

Run checks relevant to changed code: Flutter format/analyze/tests from `app/`, Go vet/tests from `backend/`, and Firebase emulator tests from `firebase/` (currently `npm run test:emulator`; add Storage coverage where needed). Report actual results and explicitly identify checks needing external credentials/devices. This issue is done when its behavior, verification evidence and any changed contracts/runbooks are committed and linked; do not treat a mock as proof of a live store or production integration.


## Scope of Work
- Asset analysis and workspace initialization.
- Core modeling / development based on specifications.
- Technical validation and quality checks.
- Incorporation of review feedback.
- Clean handover of source files and documentation.

## Required Files & Inputs
1. Complete reference files (drawings, access tokens, test data).
2. Exact dimensional specs or business rules.
3. Schedule/deadline expectations.

## Estimated Price and Timeline
- **Estimated Price:** 1500 - 4000 USD
- **Estimated Timeline:** 3 to 7 business days (to be refined after reviewing the final assets).

## Project Questions
To help me refine this estimate, please clarify:
1. Do you have UI designs ready (Figma, Adobe XD) or should we design the screens?
2. Which backend service will the app connect to (REST API, Firebase)?
3. Do you require integration with app stores (App Store, Play Store)?
4. What state management solution do you prefer (Bloc, Riverpod, Provider)?
5. What is your target launch date?

## Agreement Terms
The final source files will be delivered upon approval of the milestones. Substantial revisions outside the agreed scope will require a change order.
