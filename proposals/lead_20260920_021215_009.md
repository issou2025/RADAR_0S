# Project Proposal — Flutter App

## Project Understanding
You need assistance with: 
**[P0][Public API] Sanitize current visible issue and architecture terminology**

*Project description:*
Parent: #931

## Objective

Audit the current visible GitHub issue-tracker surface of this public reusable CI repository and replace unnecessary private consumer/product/repository naming with generic capability/project-class terminology.

This child owns GitHub metadata/text cleanup only. Do not change executable workflow behavior here.

## Scope

Audit open and closed ci-workflows:

- issue titles;
- issue bodies;
- top-level comments that define reusable architecture/contracts;
- tracker/adoption summaries.

Use generic terminology where a private identity is not technically necessary, for example:

- Apple application project;
- Apple library/package project;
- Android application project;
- Android library/package project;
- Linux service/backend project;
- generic package producer/consumer.

Preserve issue/PR/run identifiers needed for traceability.

## Historical limitation

This task cleans current visible/canonical text only. Do not claim that GitHub edit history, forks, clones, caches, or external archives are erased.

## Safety/traceability

Do not rewrite technical evidence so aggressively that the referenced run/PR/issue becomes unintelligible. Preserve exact run IDs, PR numbers, commit SHAs, and generic role relationships where they are needed.

Do not rename private repositories themselves.

## Acceptance

- current visible architecture/tracker text no longer uses private consumer identity merely as a reusable-library label;
- open and closed issues are audited;
- architecture language is capability/project-class based;
- historical-publication limitation is retained where relevant;
- no executable Central behavior changes are bundled into this task.

## Current visible-text audit result — 2026-09-20

This metadata-only audit is complete for the current worker carrier. No executable workflow/action/source file was changed.

### Sanitized surfaces

- legacy adoption/incident titles that used private application/library/repository names as public reusable-library labels;
- current and historical adoption/tracker bodies where the project class is sufficient;
- active architecture/finalization bodies, including removal of the concrete private repository-to-`repository_ci` capability mapping from #924;
- top-level architecture/tracker comments that described reusable contracts using private product/repository shorthand.

Representative public terminology now uses:
- Apple application project;
- Android application project;
- library/package project;
- Linux service/backend project;
- Agent State web/dashboard or database/service project;
- generic Flutter application project;
- developer-tooling service project;
- dedicated CI broker service repository.

### Traceability intentionally retained

Exact issue/PR/run numbers, commit/tree SHAs, request keys, branch/ref names, cache keys, workflow/profile keys, published artifact coordinates, and technical product interfaces were retained where removing them would make historical evidence or an exact failure/release contract ambiguous.

Private project identity is not retained merely as a reusable capability label. In particular, public tracker text no longer publishes the concrete private project -> capability-grant mapping that belongs in Agent State/private authority.

### Historical limitation

These edits change only the current visible GitHub text. They do not erase GitHub edit history, forks, clones, caches, or external archives, and no such erasure is claimed.

### Source identity

Because #941 changes GitHub metadata/text only, the source candidate is intentionally unchanged protected `main@bb916480366ba7d1c9e1e9d2554a131fba680b3a` / tree `dc6e663b2ba2988efcd0a492f0a15b9c8d7a957d`.

Independent review should audit the current visible issue/comment surface and verify that retained private-looking strings are evidence/technical identities rather than reusable-library labels.

## Correction implementation complete — residual-label re-audit

The independent review correction is complete.

- #131 reusable Apple architecture wording is now project-class based. Its exact blocked-consumer URL remains only for traceability, and the historical implementation checkpoint retains exact contract/profile identifiers needed to understand the old evidence.
- #929 architecture provenance is now generic developer-tooling/integration wording.
- Active architecture/tracker carriers #924, #929 and #931 were re-audited. Remaining private-looking strings on those surfaces are exact branch/ref identifiers needed for evidence, not reusable capability labels.
- Current issue-title audit remains generic for the known private consumer repository names.
- The historical-publication limitation remains unchanged: current visible edits do not erase GitHub edit history, forks, clones, caches or external archives.
- No executable Central source changed.

Metadata candidate identity therefore remains protected `main@bb916480366ba7d1c9e1e9d2554a131fba680b3a` / tree `dc6e663b2ba2988efcd0a492f0a15b9c8d7a957d`.


## Scope of Work
- Asset analysis and workspace initialization.
- Core modeling / development based on specifications.
- Technical validation and quality checks.
- Incorporation of review feedback.
- Clean handover of source files and documentation.

## Required Files & Inputs
1. Complete reference files (drawings, access tokens, test data).
2. Exact dimensional specs or business rules.
3. Schedule/deadline expectations.

## Estimated Price and Timeline
- **Estimated Price:** 1500 - 4000 USD
- **Estimated Timeline:** 3 to 7 business days (to be refined after reviewing the final assets).

## Project Questions
To help me refine this estimate, please clarify:
1. Do you have UI designs ready (Figma, Adobe XD) or should we design the screens?
2. Which backend service will the app connect to (REST API, Firebase)?
3. Do you require integration with app stores (App Store, Play Store)?
4. What state management solution do you prefer (Bloc, Riverpod, Provider)?
5. What is your target launch date?

## Agreement Terms
The final source files will be delivered upon approval of the milestones. Substantial revisions outside the agreed scope will require a change order.
