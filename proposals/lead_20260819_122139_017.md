# Project Proposal — Python Automation

## Project Understanding
You need assistance with: 
**mccall_risk.md was never delivered — edition is 143/145 against source**

*Project description:*
`lectures/mccall_risk.md` ("Job Search V: Modeling Career Choice" / risk-sensitive preferences) exists upstream in QuantEcon/lecture-python.myst but has never been delivered to this edition. It is absent from `lectures/` and from `lectures/_toc.yml` here, and present in both upstream.

A full set comparison as of 2026-08-19:

| | Count |
|---|---|
| `lectures/*.md` in lecture-python.myst | 145 |
| `lectures/*.md` here | 143 |
| Missing | `learning_approximation.md` (tracked in #210), `mccall_risk.md` (this issue) |

## How it went missing

It was delivered by [#242](https://github.com/QuantEcon/lecture-python.zh-cn/pull/242), which was auto-closed on 2026-08-07 by the rebase automation's force-push race — `head_ref_force_pushed` and `closed` both at 01:34:27Z, with the automation's own "♻️ Automatically rebased after #230 was merged" comment nine seconds later, and no separate manual close in the event log. The mechanism is filed as QuantEcon/action-translation#280.

The gap then went unnoticed for twelve days. The closed PR's other five files were updates to lectures that already existed and have since been superseded by later syncs, so nothing about the tracker state looked wrong, and **no stage in the pipeline compares the source lecture set against this edition's** — not CI, not review mode, not the sync run. It surfaced only because the branch outlived its PR and a post-drain tidy-up went looking for stray refs.

## Recommended recovery

A targeted gap-fill — `translate init -f` for `mccall_risk` alone, at the current engine — rather than recreating #242 from its branch. That branch (`translation-sync-2026-08-05T23-15-41-pr-760`, still present, 15 ahead / 37 behind) carries a 2026-08-05 snapshot of six lectures; merging or regenerating from it risks reverting twelve days of newer edits to the five that did land, which is the failure mode in QuantEcon/action-translation#276. A fresh gap-fill also gets a translation from v0.26.0 rather than the engine of the day.

Two things to check when it lands: that `_toc.yml` gains the entry in the right part, and that the lecture's figure captions and any `{ref}` targets are localised, since the caption classes in QuantEcon/action-translation#254 and QuantEcon/action-translation#255 are still open.

Once the lecture is in, the orphaned branch can be deleted.


## Scope of Work
- Asset analysis and workspace initialization.
- Core modeling / development based on specifications.
- Technical validation and quality checks.
- Incorporation of review feedback.
- Clean handover of source files and documentation.

## Required Files & Inputs
1. Complete reference files (drawings, access tokens, test data).
2. Exact dimensional specs or business rules.
3. Schedule/deadline expectations.

## Estimated Price and Timeline
- **Estimated Price:** 200 - 600 USD
- **Estimated Timeline:** 3 to 7 business days (to be refined after reviewing the final assets).

## Project Questions
To help me refine this estimate, please clarify:
1. What is the input data format (Excel sheets, CSV, website, database)?
2. What are the step-by-step logic rules for processing the data?
3. Where should the output be saved (merged sheet, email, database)?
4. Does this script need to run on a schedule (cron, Windows Scheduler) or manually?
5. Can you share sample data files for testing?

## Agreement Terms
The final source files will be delivered upon approval of the milestones. Substantial revisions outside the agreed scope will require a change order.
