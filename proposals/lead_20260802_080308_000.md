# Project Proposal — Bim Modeling

## Project Understanding
You need assistance with: 
**PR queue — one read: 23 open in this workstream, ranked by risk, staleness measured (nothing merged)**

*Project description:*
One place to work the queue from. **Nothing here has been merged, and I will not merge any of it** — merging auto-deploys to production, so it stays your click.

Measured against `origin/main` @ `097b75a94`. "Behind" is commits on main not in the branch; "conflict" was checked with `git merge-tree`, not assumed.

**Headline: no PR in this workstream conflicts with main.** Five are badly stale and should be rebased before they are trusted. Everything else is within one commit of main.

---

## Merge by risk, not by number

### Tier 1 — closes a defect that is live in production right now

| PR | What breaks today if this is not merged | State |
|---|---|---|
| **#542** | **Cross-tenant read *and* write on MaterialSync.** One tenant can reach another's data. This is the single highest-severity item open. ⚠️ **Breaking route change** — clients need coordinating, so it is not a silent merge, but the hole is live while it waits. | 1 ahead / 1 behind, clean |
| **#540** | Eleven authorization gates that do nothing. Until this lands, "only a PM can do X" is not enforced anywhere. Also the dependency for #547. | 1 ahead / 1 behind, clean |
| **#535** | The viewer invents data — fabricated values shown as real. Same anti-pattern as an empty list standing in for an error. | 1 ahead / 1 behind, clean |
| **#532** | Member Directory **Save silently does not reach the server**. Users believe they saved. | 1 ahead / 1 behind, clean |
| **#537** | Issues can be assigned to people who are not project members. | 1 ahead / 1 behind, clean |
| **#536** | "Create issue" fails without saying why. | 1 ahead / 1 behind, clean |
| **#539** | Heartbeat polls an endpoint it cannot reach — permanent false "offline". | 1 ahead / 1 behind, clean |
| **#448** | A cache outage turns a 404 into a 500. Resilience defect. | ⚠️ **203 behind** — rebase before trusting |

### Tier 2 — correctness and hardening, no live user-facing hole

| PR | Note | State |
|---|---|---|
| #517 / #518 | Project-roster member picker (BCC + web). **Based on `claude/livekit-corporate-ui-research-3233e0`, not main** — that base must land first, or they need retargeting. | clean |
| #503 | SLD: distinguish scan failure from an empty model. | ⚠️ **73 behind** |
| #502 | Adds the missing `Symbols_Validate` button (1 line). | ⚠️ **73 behind** |
| #544 | GDPR/POPIA export + erasure UI, and repairs the export. | 1 behind, clean |
| #547 | Capabilities endpoint. **Blocked on #540** (it is stacked on it). Its twin #548 was closed as a duplicate — same head branch, no unique commits. | clean |

### Tier 3 — tests only, zero production behaviour change

| PR | Note | State |
|---|---|---|
| #487 | Real-Redis handoff JTI replay test. | **0 behind, clean** |
| #549 | DEP-7 HTTP-level provisioning-failure test. | **0 behind, clean** |
| #460 | Server test-health pass. | ⚠️ **232 behind — the stalest branch open** |

These three are the cheapest merges in the queue: they cannot break production because they change no production code.

### Tier 4 — docs and analysis, no code

| PR | Note | State |
|---|---|---|
| #543 | Phase 6A triage (consolidated). | 10 ahead / 1 behind, clean |
| #538 | Document/deliverable gap table with verdicts. | clean |
| #546 | Corrects CLAUDE.md — EF migrations are decorative, not "83 present & applied". Worth landing early: it stops the next reader acting on a false premise. | clean |
| #501 | Ignore timestamped `CompiledPlugin` backups. | ⚠️ 74 behind |
| #533 | Viewer full-screen toggle icon-only. | clean |
| #534 | Level dropdown replaces ◀ ▶ arrows. | clean |

### Tier 5 — waiting on a person, not on review

| PR | Waiting for |
|---|---|
| **#550** | **You** — a Revit window (Revit closed, Companion tray app stopped) so the site-photo tabs can be exercised against a live server. Code complete, build 0/0, no regressions. Stays draft until that pass runs. |
| **#555** | **You** — a decision, not a review. Prepared RLS remediation for #545, no-op until `Database:RlsEnabled` is set. The prerequisite is a `BYPASSRLS` role for Hangfire/admin paths; without it, enabling the key makes background jobs silently find nothing. |

---

## Stale — rebase before trusting

None conflict, but these five have drifted far enough that "mergeable" is not the same as "correct". A clean merge of a 232-commit-old branch can still resurrect a since-fixed bug or re-introduce a since-changed contract.

| PR | Behind main |
|---|---|
| #460 | **232** |
| #448 | **203** |
| #501 | 74 |
| #502 | 73 |
| #503 | 73 |

The other seventeen are 0–1 behind and need nothing.

---

## Not this workstream

Open, but from other lines of work — listed so the count reconciles, not for action here: **#329** (KUT hardening, stacked on `claude/kut-hardening-phase1`), **#338** / **#467** / **#481** (Phase 188 params + native types), **#393** (QS/PM guide), **#396** / **#458** (placement), **#459** / **#480** (PM3 dayworks).

That makes **34 open PRs** in total, of which **23** belong to this workstream.

---

## Suggested order

If you want one: **#542 first** — it is the only live cross-tenant hole, and every day it waits is a day the hole is open. Then the three test-only PRs (#487, #549, #460 after rebase) since they cannot break anything, then #540 → #547 as a pair, then the viewer batch (#535, #536, #537, #539, #532), then docs.

I have not merged anything and am not asking you to. Say the word on #550's Revit window and I will run that pass.


## Scope of Work
- Asset analysis and workspace initialization.
- Core modeling / development based on specifications.
- Technical validation and quality checks.
- Incorporation of review feedback.
- Clean handover of source files and documentation.

## Required Files & Inputs
1. Complete reference files (drawings, access tokens, test data).
2. Exact dimensional specs or business rules.
3. Schedule/deadline expectations.

## Estimated Price and Timeline
- **Estimated Price:** 300 - 800 USD
- **Estimated Timeline:** 3 to 7 business days (to be refined after reviewing the final assets).

## Project Questions
To help me refine this estimate, please clarify:
1. Could you describe the project scope and deliverables in detail?
2. Do you have any existing drawings or reference documents?
3. What is your target budget range?
4. What is your expected timeline?
5. Which tools/software do you prefer for this project?

## Agreement Terms
The final source files will be delivered upon approval of the milestones. Substantial revisions outside the agreed scope will require a change order.
