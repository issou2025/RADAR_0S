# Project Proposal — Bim Modeling

## Project Understanding
You need assistance with: 
**search_functions("join walls at corner") doesn't surface JoinGeometryUtils/WallUtils in the first query**

*Project description:*
## Problem

Found live while building validation-corpus case #2 (\`revit/docs/validation-corpus.md\`, PRD §13 phase 4): "model a closed rectangular building footprint and confirm they join at corners."

\`search_functions("join walls at corner")\` returns almost entirely \`BuiltInParameter\`/\`BuiltInFailures\`/UI-command noise (\`LINE_SHAPE_AT_CORNER\`, \`FabricationAncillaryType.Corner\`, \`PostableCommand.WallJoins\`, ...) — neither \`JoinGeometryUtils.AreElementsJoined\` nor \`WallUtils.IsWallJoinAllowedAtEnd\`/\`DisallowWallJoinAtEnd\` appear anywhere in the first page. A second, more specific reformulation (\`"AreElementsJoined geometry"\`) found \`JoinGeometryUtils.AreElementsJoined\` at rank 1.

Per PRD §13's own grading protocol: *"a case needing […] reformulations before finding the right member is a discovery gap even if it eventually passes."* This case passed once the right query was found, so it's not a blocker, but it's a real ranking miss worth tracking alongside issues #75/#76 (the same "create"/"new" and candidate-limit work already done in this area).

## Notes

- Query tried: \`"join walls at corner"\`. Real members that should plausibly rank highly:
  \`Autodesk.Revit.DB.JoinGeometryUtils.AreElementsJoined\`, \`Autodesk.Revit.DB.WallUtils.IsWallJoinAllowedAtEnd\`,
  \`Autodesk.Revit.DB.WallUtils.AllowWallJoinAtEnd\`/\`DisallowWallJoinAtEnd\`.
- Possibly related to #75 (synonym gap — "join" has no declared synonym class) or a fresh precision/recall
  issue distinct from both #75/#76; needs its own investigation rather than assumed to be one or the other.
- \`revit/docs/validation-corpus.md\` — case #2, where this was found; the case itself passed and its
  regression test lives in \`revit/test-harness/validation_corpus_test.go\`
  (\`TestValidationCorpus_ClosedRectangularFootprint\`).

## Scope of Work
- Asset analysis and workspace initialization.
- Core modeling / development based on specifications.
- Technical validation and quality checks.
- Incorporation of review feedback.
- Clean handover of source files and documentation.

## Required Files & Inputs
1. Complete reference files (drawings, access tokens, test data).
2. Exact dimensional specs or business rules.
3. Schedule/deadline expectations.

## Estimated Price and Timeline
- **Estimated Price:** 300 - 800 USD
- **Estimated Timeline:** 3 to 7 business days (to be refined after reviewing the final assets).

## Project Questions
To help me refine this estimate, please clarify:
1. Could you describe the project scope and deliverables in detail?
2. Do you have any existing drawings or reference documents?
3. What is your target budget range?
4. What is your expected timeline?
5. Which tools/software do you prefer for this project?

## Agreement Terms
The final source files will be delivered upon approval of the milestones. Substantial revisions outside the agreed scope will require a change order.
