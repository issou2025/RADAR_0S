# Project Proposal — Python Automation

## Project Understanding
You need assistance with: 
**Complexity debt: exploration, config, migration & hook_registry**

*Project description:*
## Scope

src/autoskillit/exploration/, config/, migration/, hook_registry/

**16 functions** across **14 files** exceed `MAX_COMPLEXITY` (10), worst case complexity **37**.

## Context

PR #4975 introduced `scripts/check_complexity.py`, a diff-scoped cyclomatic-complexity ratchet gate (ships in `warn`-only mode -- it never blocks CI yet, and only checks files touched by a given diff going forward). This ticket is one of a set of sub-tickets under the tracking issue (see below) surfacing **pre-existing** complexity debt found by a one-time full-repo baseline scan of `develop` @ `e2fcb0cb79432f74c3bf6c8c447473c70dc87c2a` (not the gate itself -- the gate will never retroactively flag these; they only surface because someone has to go looking).

## Refactoring objective and implementation plan

Reduce cyclomatic complexity where a clear, behavior-preserving refactor is available. Evaluate every listed function against the current checkout and use that evaluation to write a concrete implementation plan, then carry out the planned reductions. The candidate list defines the review scope; the expected work is refactoring, with implementation-time deviations where needed.

Prefer removing redundant branches, combining equivalent cases, consolidating repeated logic, and extracting a cohesive responsibility when it makes the caller and implementation easier to understand. A parser, dispatcher, or validator is still worth evaluating for these improvements. Do not turn the planning phase into a blanket decision to retain high-complexity functions merely because their branching has a purpose. Avoid abstractions, helper fragments, or compressed expressions that only move or hide the complexity.

MAX_COMPLEXITY = 10 is a guideline, not a required finish line for every existing function. A worthwhile reduction that remains above 10 still counts. Preserve existing behavior, including relevant error handling, accepted/rejected inputs, ordering, and diagnostics.

## Testing exception for this refactoring work

Use the existing relevant test suite and the existing complexity checker. **Do not add new tests by default.** In particular, do not add tests merely because a private helper is extracted, duplicate existing coverage, create helper-shape assertions or case matrices, or add another complexity gate.

This is an explicit task-specific exception to generic planning/implementation skill instructions that call for new tests or a red/green test phase. Existing tests may be adjusted or rewired when the refactor requires it. Add a focused regression only if a specific demonstrated coverage gap prevents establishing behavior preservation, and explain that need.

The complexity checker measures complexity; it does not prove functional equivalence. Run the existing tests appropriate to the change and the repository's required checks.

## Authorized implementation-time deviations

Pursue the planned refactors. If implementing or validating an approach shows that it makes the code harder to follow, introduces unnecessary machinery, or cannot preserve the required behavior, retain or restore the clearer implementation and record a deviation in this issue's body.

For each deviation, identify the function, the intended reduction, what was tried or concretely evaluated during implementation, why it did not work well, and the final measured complexity. Related nested functions may share one explanation when all affected identities are listed. A specific, evidence-based deviation is an accepted outcome for that candidate; it is not an instruction to keep retrying an inferior refactor. Do not use a generic “naturally branchy” judgment as an upfront exemption from the implementation plan.

## Verification and completion record

- Measure before/after complexity using scripts/check_complexity.py's function_metrics() / cyclomatic_complexity() on the original functions and any new helpers.
- Run the existing diff-scoped checker against the implementation's captured base revision and inspect its report. A passing ratchet does not by itself demonstrate a reduction; the current warn-mode report also needs inspection.
- Run the existing relevant tests through the repository's prescribed commands. Do not weaken tests, thresholds, or exemption policy to obtain a passing result.
- Record the implemented reductions and any justified deviations, with actual measurements and validation results, in this issue's body. Account for every listed candidate without fabricating reductions or forcing code that is harder to maintain.


## Candidates (sorted by complexity, worst first)

| Complexity | File:Line | Function |
|---|---|---|
| 37 | `src/autoskillit/exploration/collectors/extractors/_python_ast.py:51` | `collect_python_ast` |
| 27 | `src/autoskillit/config/_coercion.py:46` | `_coerce_value` |
| 18 | `src/autoskillit/config/_dataclasses_providers.py:102` | `ProvidersConfig.__post_init__` |
| 17 | `src/autoskillit/exploration/collectors/_bounded.py:341` | `read_stable_contained_file` |
| 16 | `src/autoskillit/migration/service.py:85` | `DefaultMigrationService.migrate` |
| 16 | `src/autoskillit/migration/adapters_skill.py:128` | `SkillMigrationAdapter.migrate` |
| 16 | `src/autoskillit/config/_automation_config.py:155` | `AutomationConfig._build_features_dict` |
| 15 | `src/autoskillit/exploration/_deterministic.py:52` | `stable_kahn_waves` |
| 14 | `src/autoskillit/exploration/collectors/_bounded.py:431` | `list_contained_files` |
| 14 | `src/autoskillit/config/_dataclasses_providers.py:207` | `AgentBackendConfig.__post_init__` |
| 13 | `src/autoskillit/hook_registry/_drift.py:89` | `find_broken_hook_scripts` |
| 13 | `src/autoskillit/exploration/snapshot/_records.py:168` | `SnapshotCaptureResult.__post_init__` |
| 12 | `src/autoskillit/hook_registry/_risky_operations.py:90` | `validate_lifecycle_contracts` |
| 12 | `src/autoskillit/exploration/identity.py:193` | `_validate_offline_declaration` |
| 12 | `src/autoskillit/config/_validation.py:35` | `_build_config_schema` |
| 11 | `src/autoskillit/config/_dataclasses_fleet.py:29` | `FleetConfig.validate` |

## Tracking issue

Tracked by #4991.


## Scope of Work
- Asset analysis and workspace initialization.
- Core modeling / development based on specifications.
- Technical validation and quality checks.
- Incorporation of review feedback.
- Clean handover of source files and documentation.

## Required Files & Inputs
1. Complete reference files (drawings, access tokens, test data).
2. Exact dimensional specs or business rules.
3. Schedule/deadline expectations.

## Estimated Price and Timeline
- **Estimated Price:** 200 - 600 USD
- **Estimated Timeline:** 3 to 7 business days (to be refined after reviewing the final assets).

## Project Questions
To help me refine this estimate, please clarify:
1. What is the input data format (Excel sheets, CSV, website, database)?
2. What are the step-by-step logic rules for processing the data?
3. Where should the output be saved (merged sheet, email, database)?
4. Does this script need to run on a schedule (cron, Windows Scheduler) or manually?
5. Can you share sample data files for testing?

## Agreement Terms
The final source files will be delivered upon approval of the milestones. Substantial revisions outside the agreed scope will require a change order.
