# Project Proposal — Python Automation

## Project Understanding
You need assistance with: 
**2-E4: publish the CLI container image + official MCP registry entry (release automation, sunset-dated 20 Sep)**

*Project description:*
# 2-E4: publish the CLI container image + official MCP registry entry (sunset-dated 20 Sep)

Local-MCP phase 2, item 2-E4 (the "last domino"). The hosted-MCP sunset is **Sunday 20 Sep 2026**; this issue tracks the `jentic-one` side of the distribution work, scheduled backward from that date. The plugin-side flip is tracked in the `jentic-claude-plugin` repo; Smithery rides #1147.

## Why now

- The ordering guard is satisfied: the docs flip landed (#1180) and phase 1 (`jentic mcp` stdio server, #1209/#1183/#1186/#1192) is fully merged.
- The 2-E3 container isolation rung was **demoted** in #1191 precisely because no published CLI image exists; this issue un-demotes it. It is also the only automatable isolated rung on Windows.

## Scope (three PRs)

**PR A — CLI container image + release publishing**
- New Dockerfile for the Go CLI suitable for `docker run -i … jentic mcp` (none exists today — `deploy/docker/` is all Python services), built **from source** in a new `publish-cli-image` job in `.github/workflows/release.yml`, parallel to `publish-image` and behind the same `gate` → `smoke` DAG so a release never half-ships.
- Image: `ghcr.io/jentic/jentic-one-cli:<X.Y.Z>` (+ `:latest` last), multi-arch buildx, Trivy pre-push, cosign + SBOM — mirroring the existing `publish-image` job.
- Must carry `LABEL io.modelcontextprotocol.server.name` (registry ownership verification pulls the image and checks it) and the GHCR package must be **public**.

**PR B — `server.json` + registry publish automation**
- `server.json` (schema 2025-12-11) under the DNS-verified **`one.jentic`** namespace; `mcp-publisher login dns` in a `publish-registry` job running last in the release DAG; `mcp-publisher validate` as a ci.yml drift gate.
- Deprecate the stale `io.github.jentic/mcp-server` entry (points at the hosted server) on sunset day.

**PR C — re-enable the 2-E3 container rung (Go)**
- Restore the automated container isolation offer in `jentic setup`: volume provisioning + smoke-spawn, honoring the invariant that **failure keeps the working non-isolated entry**. Flags must match the hardened manual recipe in `docs/security/mcp-same-host-hardening.md` (Recipe 3).

## Lead-time asks (needed this week — admin actions)

- [ ] DNS TXT record on `jentic.one`: `v=MCPv1; k=ed25519; p=<pubkey>` (generate the Ed25519 publisher keypair; private key into Actions secrets)
- [ ] GHCR: make the new `jentic-one-cli` package public once first-pushed

## Schedule (backward from Sun 20 Sep)

| Date | Milestone |
|---|---|
| Fri 5 Sep | DNS TXT live; PR A in review |
| Tue 9 Sep | PR A + B merged; PR C in review |
| Thu 11 Sep | PR C merged; local `search_catalog` coverage check written up (gate for the plugin flip only) |
| Mon 14 Sep | Release cut: first one publishing image + registry entry; verify end-to-end |
| Thu 17 Sep | Buffer release window; coverage go/delay decision recorded |
| Sun 20 Sep (T0) | Plugin flip merges (other repo); deprecate the old registry entry |

T0 is a Sunday — the flip PR must be pre-approved with a named owner by Fri 18 Sep.

Plan slice: `jentic-one-plans` → `themes/local-mcp/phase-2e4.md` (grounded at a3a6ea5c; registry/Smithery facts re-verified 2026-09-01).


## Scope of Work
- Asset analysis and workspace initialization.
- Core modeling / development based on specifications.
- Technical validation and quality checks.
- Incorporation of review feedback.
- Clean handover of source files and documentation.

## Required Files & Inputs
1. Complete reference files (drawings, access tokens, test data).
2. Exact dimensional specs or business rules.
3. Schedule/deadline expectations.

## Estimated Price and Timeline
- **Estimated Price:** 200 - 600 USD
- **Estimated Timeline:** 3 to 7 business days (to be refined after reviewing the final assets).

## Project Questions
To help me refine this estimate, please clarify:
1. What is the input data format (Excel sheets, CSV, website, database)?
2. What are the step-by-step logic rules for processing the data?
3. Where should the output be saved (merged sheet, email, database)?
4. Does this script need to run on a schedule (cron, Windows Scheduler) or manually?
5. Can you share sample data files for testing?

## Agreement Terms
The final source files will be delivered upon approval of the milestones. Substantial revisions outside the agreed scope will require a change order.
