# Project Proposal — Static Website

## Project Understanding
You need assistance with: 
**brand(metadata): platform-uploaded surfaces checklist — the social-preview blind-spot class**

*Project description:*
**The find (maintainer + her Portfolio Agent, 2026-07-20):** the repo's **uploaded social preview** still serves the retired "refuses to lie" card on every topic page and link unfurl — the repo-source card was voice-cleaned in the #1101-era sweep, but the *uploaded platform copy* was never re-uploaded. It survived every sweep because **it isn't a file**: platform-uploaded metadata is invisible to grep, unfixable by CI, and the social preview specifically has **no API at all** (web-UI-only).

That's a blind-spot **class**, not a one-off: any brand/voice truth that lives as uploaded platform state can drift from the repo source silently and forever.

## Scope — a named checklist, run at every brand/voice change and every release cut
The platform-metadata surfaces, each verified **by eye** (grep can't reach them):
- [ ] **Social preview image** (Settings → General → Social preview) — web-UI-only, no API; must match `docs/design/brand/social-preview.png` after any brand-asset change. *(The current stale card is being fixed by the maintainer directly.)*
- [ ] Repo description + website field (`gh repo edit` CAN set these — verify text against current voice)
- [ ] Topics (13 seeded currently — additions/removals are brand acts)
- [ ] Pages settings (custom domain, HTTPS)
- [ ] The GitHub profile README / org card, if/when one exists
- [ ] (At account activation: Mastodon/Bluesky profile bios + pinned posts join this list)

Home for the checklist: `docs/design/brand/` (Design-QA owns brand truth; DX owns the settings surfaces) — plus one line in RELEASE_CUT so it runs at every cut.

Unfurl-cache note for the record: GitHub updates immediately on re-upload; LinkedIn holds ~7 days, Slack/Discord cache too — a fixed card looks unfixed downstream for a while. That's expected, not a regression.

Also flagged for a one-glance ruling (Design-QA): `front-door-design-brief.md` still carries the retired phrase — likely intentional-historical (documenting prior positioning), same call as the Drowning hits. `test_voice_guard.py`'s copy is the guard itself — stays.


## Scope of Work
- Asset analysis and workspace initialization.
- Core modeling / development based on specifications.
- Technical validation and quality checks.
- Incorporation of review feedback.
- Clean handover of source files and documentation.

## Required Files & Inputs
1. Complete reference files (drawings, access tokens, test data).
2. Exact dimensional specs or business rules.
3. Schedule/deadline expectations.

## Estimated Price and Timeline
- **Estimated Price:** 400 - 1000 USD
- **Estimated Timeline:** 3 to 7 business days (to be refined after reviewing the final assets).

## Project Questions
To help me refine this estimate, please clarify:
1. Do you have copywriting and images ready, or should they be generated?
2. Do you need any contact forms, interactive maps, or animations?
3. What static hosting service do you prefer (GitHub Pages, Netlify, Vercel)?
4. Do you have a custom domain name registered?
5. What is the target schedule for publishing?

## Agreement Terms
The final source files will be delivered upon approval of the milestones. Substantial revisions outside the agreed scope will require a change order.
