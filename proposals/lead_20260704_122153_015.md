# Project Proposal — Python Automation

## Project Understanding
You need assistance with: 
**feat(pipeline)!: cutover - pipeline is the default loop path**

*Project description:*
Part of epic #1809. Depends on #1817.

## Context

Cutover: the pipeline becomes the default loop path. This PR is deliberately tiny — a flag-default flip plus docs — so rollback is a single `git revert` or `HEPH_PIPELINE=0`. It merges ONLY after the end-to-end evidence below is attached to this issue.

## Entry criteria (evidence attached to THIS issue before the PR merges)

1. **Two-layer check**: editable install runs branch code (`pixi run dev-install && python -c "import hephaestus.automation.pipeline.coordinator; print('live')"`); target repo clone is on the intended branch.
2. **Dry-run**: `pixi run hephaestus-automation-loop --pipeline --dry-run --loops 1 -v` — every open issue classified; logged mutation plan eyeballed against actual labels.
3. **Shadow diff**: legacy vs `--pipeline`, both `--dry-run --issues N`, on 3 issues in distinct states (needs-plan / plan-go / impl-go+PR); every divergence is a bug or a documented intentional change listed in the PR body.
4. **Scoped live drive**: `pixi run hephaestus-automation-loop --pipeline --issues N --max-workers 1 --loops 1 -v` on one low-stakes issue end-to-end through merge (log excerpt + PR URL attached).
5. **Interrupt drill**: Ctrl-C mid-implementation, re-run, seeding resumes the issue at the correct stage (no lost/duplicated work).

## Deliverables

1. Flip `--pipeline`/`HEPH_PIPELINE` default → ON; add `--legacy-loop` / `HEPH_PIPELINE=0` escape hatch (legacy internals stay in-tree until the cleanup wave).
2. Finalize **`docs/AUTOMATION_LOOP_ARCHITECTURE.md`** (remove pre-implementation banner; per-stage table, ROUTES listing, reconstruction table, interrupt semantics, concurrency/tuning, CLI scopes, glossary — all as-built).
3. Runbook updates: `docs/runbooks/automation-loop-crash.md` (new coordinator log markers; "recovery is always re-run — queues rebuild from labels"; the interrupt-drill resume story), cross-check `docs/runbooks/ci-driver-stall.md` + `docs/runbooks/index.md` state-label table.
4. CLAUDE.md: automation tree line mentions the `pipeline/` coordinator; note in the coverage omit-list invariant section.

## Acceptance criteria

- [ ] All 5 entry-criteria evidence items attached as comments on this issue.
- [ ] Default ON; `--legacy-loop` verified to run the old path (one smoke run).
- [ ] Docs/runbooks/CLAUDE.md updated; no frozen-guardrail edits needed in this PR.
- [ ] mypy/ruff/pre-commit clean; `Closes #1818`.



## Scope of Work
- Asset analysis and workspace initialization.
- Core modeling / development based on specifications.
- Technical validation and quality checks.
- Incorporation of review feedback.
- Clean handover of source files and documentation.

## Required Files & Inputs
1. Complete reference files (drawings, access tokens, test data).
2. Exact dimensional specs or business rules.
3. Schedule/deadline expectations.

## Estimated Price and Timeline
- **Estimated Price:** 200 - 600 USD
- **Estimated Timeline:** 3 to 7 business days (to be refined after reviewing the final assets).

## Project Questions
To help me refine this estimate, please clarify:
1. What is the input data format (Excel sheets, CSV, website, database)?
2. What are the step-by-step logic rules for processing the data?
3. Where should the output be saved (merged sheet, email, database)?
4. Does this script need to run on a schedule (cron, Windows Scheduler) or manually?
5. Can you share sample data files for testing?

## Agreement Terms
The final source files will be delivered upon approval of the milestones. Substantial revisions outside the agreed scope will require a change order.
