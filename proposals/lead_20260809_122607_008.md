# Project Proposal — Unknown

## Project Understanding
You need assistance with: 
**P0 audit: resolve Home Edge snapshot launcher identity without secret access**

*Project description:*
Mode: RUNNER_TASK
Parents: #2337 #2338 #2339 #2340
Priority: P0 / RUN_NOW
Risk: green
Privacy Boundary: PRIVATE_CONTROLLER_METADATA_ONLY / PUBLIC_SAFE_BOOLEANS_ENUMS_ONLY

Purpose
Unblock canonical current Home media source capture needed by the active Home UI fixes. Previous #2338 proved the private controller boundary is coherent but found no live Runner daemon or live Home Edge MCP process to correlate. Determine whether an already-installed canonical launcher/service identity exists that can legitimately read the controller env by its configured user/group/permissions, without reading any env contents and without changing access.

```task
schema: skeleton.runner_task.v1
repo: alanua/Skeleton
task_kind: code_generation
payload:
  operation: audit_home_edge_snapshot_launcher_identity
  task: >-
    Perform a strictly read-only metadata/configuration audit of the known canonical Home Edge controller launch paths. Inspect only installed launcher/service metadata and repository-owned launch definitions necessary to identify the configured execution identity for `/usr/local/bin/skeleton-home-edge-exec-mcp` / `scripts/home_edge_exec_mcp.py`, the Runner service/poller if installed, and the fixed private controller boundary `/etc/skeleton`, `/etc/skeleton/home-edge-01.env`, `/etc/skeleton/home-edge-executor-controller.env`. Do not open/read/source any env file and do not inspect process environments or secret values. Determine whether there is an existing canonical launch path whose configured user/group membership can read the controller env by ordinary mode bits. If such a path exists, report exactly which public-safe route class should execute the read-only snapshot; if none exists, report that a minimal access-boundary repair requires operator approval.
requested_capabilities:
  - repository_read
  - test_execution
allowed_files: []
forbidden_actions:
  - no env file content read/open/source/cat/grep/strings
  - no process environment inspection
  - no secret/key/HMAC/token value access or output
  - no sudo/su/runuser/setuid/setgid
  - no chmod/chown/setfacl/usermod/groupmod/group membership mutation
  - no service start/stop/restart/reload/enable/disable
  - no launcher/MCP execution and no Home Edge request
  - no repository edits/branch/PR
  - no SSH/network/device action
requirements:
  - verify current main/head and zero repository changes;
  - inspect only fixed known launcher/service definitions and metadata, not unrelated system services/processes;
  - classify whether the canonical MCP launcher is systemd/socket/manual/plugin-host managed;
  - classify configured execution owner/group only as SAME_AS_CONTROLLER_OWNER | HAS_CONTROLLER_GROUP | DIFFERENT_NO_ACCESS | UNKNOWN; do not output usernames or numeric ids;
  - classify Runner/poller launch identity the same way if a fixed service definition exists;
  - derive controller_env_readable_via_existing_canonical_launcher=<true|false|unknown> from metadata only;
  - derive profile_env_readable_via_existing_canonical_launcher=<true|false|unknown> from metadata only;
  - if an existing legitimate canonical identity can read both required files, recommend USE_EXISTING_CANONICAL_LAUNCHER and name only the public route class, not private identity details;
  - otherwise recommend OPERATOR_APPROVAL_REQUIRED_FOR_MINIMAL_ACCESS_REPAIR;
  - no mutation of permissions, services, credentials or runtime.
validation_commands:
  - [git, rev-parse, HEAD]
  - [git, status, --porcelain]
validation_timeout_seconds: 300
expected_output:
  - repository_changed_files_count=0
  - controller_boundary_coherent=<true|false|unknown>
  - mcp_launch_kind=systemd|socket|manual|plugin_host|unknown
  - mcp_identity_class=SAME_AS_CONTROLLER_OWNER|HAS_CONTROLLER_GROUP|DIFFERENT_NO_ACCESS|UNKNOWN
  - runner_launch_kind=systemd|poller|manual|unknown
  - runner_identity_class=SAME_AS_CONTROLLER_OWNER|HAS_CONTROLLER_GROUP|DIFFERENT_NO_ACCESS|UNKNOWN
  - controller_env_readable_via_existing_canonical_launcher=<true|false|unknown>
  - profile_env_readable_via_existing_canonical_launcher=<true|false|unknown>
  - recommended_next_step=USE_EXISTING_CANONICAL_LAUNCHER|OPERATOR_APPROVAL_REQUIRED_FOR_MINIMAL_ACCESS_REPAIR|NEEDS_OPERATOR
  - no secret/env content accessed
privacy_boundary: PRIVATE_CONTROLLER_METADATA_ONLY / PUBLIC_SAFE_BOOLEANS_ENUMS_ONLY
idempotency_key: home-edge-snapshot-launcher-identity-audit-20260809-v1
```

This is audit only. No permission/service/credential/runtime mutation is authorized.

## Scope of Work
- Asset analysis and workspace initialization.
- Core modeling / development based on specifications.
- Technical validation and quality checks.
- Incorporation of review feedback.
- Clean handover of source files and documentation.

## Required Files & Inputs
1. Complete reference files (drawings, access tokens, test data).
2. Exact dimensional specs or business rules.
3. Schedule/deadline expectations.

## Estimated Price and Timeline
- **Estimated Price:** 300 - 800 USD
- **Estimated Timeline:** 3 to 7 business days (to be refined after reviewing the final assets).

## Project Questions
To help me refine this estimate, please clarify:
1. Could you describe the project scope and deliverables in detail?
2. Do you have any existing drawings or reference documents?
3. What is your target budget range?
4. What is your expected timeline?
5. Which tools/software do you prefer for this project?

## Agreement Terms
The final source files will be delivered upon approval of the milestones. Substantial revisions outside the agreed scope will require a change order.
