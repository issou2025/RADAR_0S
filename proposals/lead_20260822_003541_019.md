# Project Proposal — Structural Drawings

## Project Understanding
You need assistance with: 
**Tech debt 02: doc-comment bloat and gaps (180 rows, 15 PRs)**

*Project description:*
> Part of the tech-debt audit epic: #561. Source file: [02-doc-comments.md](https://github.com/CoderCoco-Studios/Hyveon/blob/worktree-tech-debt-audit/docs/tech-debt/02-doc-comments.md) (PR #545, closed unmerged — this issue is the canonical copy). Continued in 1 comment(s) below.

# 02 — Doc-comment bloat and gaps

## Summary

25.4% of the TypeScript tree is comment — 27,648 block-comment lines plus 5,162 `//` lines against 129,071 LOC across 486 files — and the great majority of that mass is bloat rather than documentation.
The rubric in `.claude/rules/tsdoc-tags.md` is, surprisingly, already satisfied almost everywhere: a scripted repo-wide sweep found **zero** `@fileoverview`, **zero** invented tags (`@note`/`@warning`), **zero** `@param {Type}` JSDoc-style params, **zero** bare `@link`, exactly **one** tag-ordering violation, and export doc coverage of 99% (952/964) — so there is essentially no mechanical TSDoc-syntax debt to pay, and the `@param game - the game` pattern the audit anticipated does not occur at all (a scripted check for descriptions that are a subset of the parameter name returned 0 hits).
The debt is entirely against `.claude/rules/comment-conciseness.md`: TSDoc blocks written as design-review memos (a single 402-line block on `PulumiService.apply()`), vendored Go and `@pulumi/pulumi` source pasted into comments to prove claims the surrounding sentence already states, 197 lines of history narration, 174 issue/PR/phase references, 249 Terraform HCL-address citations in `infra` pointing at a tree the comments admit was deleted, 234 ASCII `-----` banner lines, and the same explanation retold verbatim in up to five files.
Against that, the genuine **add** cases are few but sharp: six `AWS_REGION_` reads with no note that the trailing underscore is load-bearing, the Caddy sidecar block with no statement of the no-ALB invariant, `canRun()` with no never-fork rule, and an invisible raw NUL byte used as a dedupe separator.
Total effort ≈ **9–11 engineer-days** across **12 PRs**; roughly 6,000–7,000 comment lines removable with zero behaviour change.

### Counts per package

Rows are findings; a collapsed row covering a repeated pattern counts once and its site count is given in brackets.

| Package | delete | trim | rewrite | add | Rows | Comment lines (block / `//`) | LOC |
|---|---|---|---|---|---|---|---|
| `desktop-main` (services) | 9 [~70] | 22 | 6 | 4 | 41 | 10,726 / 1,885 | 42,188 |
| `desktop-main` (controllers, modules, top-level) | 3 [~57] | 32 | 1 | 3 | 39 | 3,429 / 631 | 15,278 |
| `web` | 6 [~90] | 12 | 5 | 1 | 24 | 5,257 / 1,700 | 39,801 |
| `infra` | 7 [~249] | 14 | 4 | 2 | 27 | 2,309 / 413 | 8,789 |
| `shared` | 5 [~50] | 12 | 1 | 2 | 20 | 2,554 / 32 | 7,956 |
| `desktop-preload` | 2 [~54] | 6 | 1 | 1 | 10 | 2,025 / 242 | 6,220 |
| `cloud-aws` | 0 | 6 | 1 [11] | 2 | 9 | 930 / 73 | 4,510 |
| `lambda/*` (six packages) | 2 [6] | 1 | 0 | 3 [8] | 6 | 285 / 6 | 3,401 |
| `.claude/hooks` + app root configs | 0 | 2 | 0 | 2 | 4 | 133 / 180 | 928 |
| **Total** | **34** | **107** | **19** | **20** | **180** | **27,648 / 5,162** | **129,071** |

### Repo-wide mechanical metrics

These are the collapsible patterns; each is a single sweep, not per-site judgement.

| Pattern | Count | Where it concentrates |
|---|---|---|
| Terraform/HCL identifier citations in comments (`` `aws_x.y` ``, `` `var.x` ``, `` `count.index` ``) | 249 across 13 files | `infra` only |
| History narration (`previously`, `used to`, `no longer`, `originally`, `renamed from`, `historically`) | 197 lines | `desktop-main` 75, `web` 54, `shared` 34 |
| Issue / PR / phase / epic references (`#NNN`, `Task 4.2`, `Phase 3`, `Epic F`) | 174 lines | `web` 69, `desktop-main` 56, `shared` 25 |
| ASCII `-----` banner lines inside comments | 234 across 13 files | `desktop-main` 118, `desktop-preload` 80, `web` 36 |
| "keep this copy in sync with it" manual-sync warnings | 76 | `hyveon-api.ts` 51, `web/src/api.service.ts` 24 |
| Stale `/api/*` HTTP-route references in doc comments | 17 across 10 non-test files | `web/src` |
| `Reachable via the Electron IPC transport (…)` above a `@MessagePattern` naming the same channel | 44 | `desktop-main/src/controllers` |
| `no HTTP routes are registered here` restated on every controller class | 10 | `desktop-main/src/controllers` |
| Block comments ≥ 10 lines | 814 | all packages |
| Block comments ≥ 15 lines | 399 | `desktop-main` 222, `web` 79 |

### Verified negatives (do not spend effort here)

| Claim checked | Result |
|---|---|
| `@fileoverview` anywhere | 0 |
| `@packageDocumentation` misplaced | 0 (it is used nowhere at all — see finding I-15) |
| Invented tags (`@note`, `@warning`, …) | 0 — only `@param`, `@returns`, `@throws`, `@remarks`, `@example`, `@deprecated`, `@typeParam`, `@internal` appear |
| `@param {Type} name` JSDoc form | 0 |
| Bare `@link` without braces | 0 |
| TSDoc tag-ordering / modifier-placement violations | 1 (`SafeStorageService.ts:62`) + 2 double-`@remarks` blocks |
| `@param` whose description restates the parameter name | 0 by script; 7 found by judgement (finding S-20) |
| Undocumented exported symbols | 12 of 964, of which 9 are e2e fixtures and hook helpers |
| NestJS `@Module` classes "undocumented" | False positive — the doc sits above the `@Module({…})` decorator, which is correct |
| `process.versions.electron` bootstrap guards | All present *and* commented in `ipc-main-bridge.ts`, `iac.controller.ts`, `iac-runs.controller.ts`, `logs.controller.ts` |
| `logger.debug` entry line on every `@MessagePattern` handler (`.claude/rules/logging.md`) | All 15 controllers comply |
| Secrets-never-reach-renderer invariant documented at the branch sites | Already documented at `discord.controller.ts:31`, `api.service.ts:454`, `discord.page.tsx:89` |
| No-HTTP-transport invariant documented in the renderer | Already stated at `web/src/api.service.ts:1-4` |

---

## Findings

Ranked by impact × ease within each group.

### A. `desktop-main` — `src/services/**` (42,188 LOC, 29.9% comment)

| # | Location (file:line) | Class | Issue | Proposed fix | Effort | Risk / test coverage |
|---|---|---|---|---|---|---|
| A-1 | `app/packages/desktop-main/src/services/PulumiService.ts:2297` | trim | 402-line TSDoc on `apply()` — the largest comment in the repo. Design essay: alternatives-considered, a self-audit section ("Nothing is reserved before step 8 — verified, not just asserted"), a 30-line "Force-close safety net" narrating a review *finding*. | Keep ~70 lines: the 8-step gate list, the DIY-lock yes/no fact, the `partialApply`-independent-of-`status` consumer MUST, the invalidate-before-persist ordering reason, `@param`/`@throws`. Delete the verification prose, the Go-source recap, and the safety-net section (file it as an issue). | L | `PulumiService.apply.test.ts`; docs-only |
| A-2 | `app/packages/desktop-main/src/services/PulumiService.ts:3546` | trim | 283-line TSDoc on `destroy()`. ~120 lines are "how this differs from `apply`"; embeds `diyBackend.Destroy` Go source verbatim; a whole section documents a feature that does not exist ("No partial-destroy concept"). | Keep ~55: token-is-the-only-gate, forced token-before-`createRun` ordering, the no-op-program property (can destroy broken infra), `@param`/`@throws`. Delete the Go block and the mirrors-apply prose. | L | `PulumiService.destroy.test.ts`; docs-only |
| A-3 | `app/packages/desktop-main/src/services/PulumiService.ts:1614` | rewrite | 206-line TSDoc on `preview()` that opens by saying everything it describes actually lives in `previewCore` — then describes it all anyway. `previewCore` has its own block at `:1845`. | Cut `preview()` to ~20 lines (delegation + why the split exists for `confirmRollback`). Move the DIY-lock, leaked-promise, engine-stamping and cancellation sections onto `previewCore:1845`, trimmed to ~50 total. | L | `PulumiService.preview.test.ts`; docs-only |
| A-4 | `app/packages/desktop-main/src/services/PulumiService.ts:791` | rewrite | **Corrupted comment.** A paragraph inside `stackInitInFlight`'s TSDoc begins mid-sentence: `* set (see its own TSDoc, "Workspace mutual exclusion"), and` — the subject was lost in an edit. Verified. | Restore the lost clause (`initializeStack` refuses to start while `operationInFlight` is set) or delete the fragment; then trim the surrounding 39-line block to ~12. | S | `PulumiService.initializeStack.test.ts`; docs-only |
| A-5 | `app/packages/desktop-main/src/services/PulumiService.ts` — 192 runs of ≥4 consecutive `//` lines, worst at `:1888` (32), `:2893` (26), `:3463` (25), `:3306` (22), `:3023` (21), `:2235` (20) | rewrite | Violates comment-conciseness rule 3 ("inline comments are a single line"). These are multi-paragraph essays inside method bodies that restate the TSDoc above them — `:2893` re-derives gate step 8's TOCTOU re-check already covered at `:2297`. | Collapse each to one line naming the constraint; move anything not already in the TSDoc up into it. Mechanical for the ~150 short runs; the 12 runs ≥15 lines need judgement. | L | `PulumiService.{apply,preview,destroy}.test.ts`; docs-only |
| A-6 | `app/packages/desktop-main/src/services/PulumiService.ts:1163` | trim | 135-line TSDoc on `initializeStack()` containing a "what could not be completed" retrospective and a `file://`-backend spike parenthetical. | Keep ~40: why `program` is `noOpProgram`, why `'plugins'` needs an explicit `installPlugin` (zero resources ⇒ no implicit download), the 3-phase error attribution. | M | `PulumiService.initializeStack.test.ts`; docs-only |
| A-7 | `app/packages/desktop-main/src/services/PulumiService.ts:4480` | trim | 132-line TSDoc on `confirmRollback()`; cites "task 7.6's refactor", spends ~25 lines rejecting an alternative the spec permits. | Keep ~35: restore+plan is one guarded unit, lock is `'rollback'` and taken before re-resolve, record-and-surface on post-restore failure. | M | `PulumiService.rollback.test.ts`; docs-only |
| A-8 | `app/packages/desktop-main/src/services/PulumiLeakedPromise.ts:3` | trim | 130-line module TSDoc quoting three blocks of `@pulumi/pulumi` JS source verbatim, plus a section prescribing an e2e test that should be an issue. | Keep ~30: why a leak-check throw in `finally` replaces a successful return, why message matching is provably sufficient, the gRPC/temp-file leak warning. Drop all three code blocks. | M | `PulumiLeakedPromise.test.ts`; docs-only |
| A-9 | `app/packages/desktop-main/src/services/PulumiLockRecovery.ts:6` | trim | 117-line module TSDoc embedding the Go `lockContent` struct and `Fprintf` calls, plus narration of two review findings ("a fix applied after an independent review caught this"). | Keep ~35: the parseable message shape (fields only, no Go) and identity + liveness + time-consistency as the three required conditions, with the same-machine-live-lock failure mode. | M | `PulumiLockRecovery.test.ts`; docs-only |
| A-10 | `app/packages/desktop-main/src/services/PulumiEngineService.ts:258` | trim | 108-line class TSDoc; four sections each restating the step sequence the code reads linearly. | Keep ~40: the `PulumiCommand.install()` **minimum**-version-check quirk (the genuinely non-obvious fact), the staging→rename→verify→trash invariant, "a rejected attempt is not memoized". | M | `PulumiEngineService.test.ts`; docs-only |
| A-11 | `app/packages/desktop-main/src/services/PulumiCancellation.ts:3` | trim | 98-line module TSDoc — high-value content (real SDK quirks) padded with a runnable `AbortController` demo and a restatement of three error classes that have their own TSDoc. | Keep ~35, **including** the 4-line `cmd.js` quote (that one is load-bearing evidence for the single-SIGINT claim). Drop the demo and the recap. | M | `PulumiCancellation.test.ts`; docs-only |
| A-12 | `app/packages/desktop-main/src/services/PulumiWorkspaceService.ts:521` | rewrite | **Tag-order violation:** two consecutive `@remarks` blocks on `migrateLegacyPassphrase`; the first is a 40-line spike-discovery narrative ("read Go source at tag, ran a scratch `file://` backend, high confidence — not a `--help`-output guess"). | Merge into one `@remarks` of ~15 lines: the two-channel passphrase contract, `cwd: workDir`, the wiped-`workDir` edge case. Delete the discovery story. | M | `PulumiWorkspaceService.test.ts`; docs-only |
| A-13 | `app/packages/desktop-main/src/services/PulumiService.ts:1583` | rewrite | **Tag-order violation:** two consecutive `@remarks` blocks on `redactEnvironmentValues`. | Merge into one. | S | `PulumiService.preview.test.ts`; docs-only |
| A-14 | `app/packages/desktop-main/src/services/SafeStorageService.ts:62` | rewrite | **The one scripted tag-order violation in the repo:** `@param`/`@returns` precede `@remarks` on `decrypt()`. Verified. | Move `@remarks` above `@param`. | S | `SafeStorageService.test.ts`; docs-only |
| A-15 | `app/packages/desktop-main/src/services/GameWizardDraftService.ts:306` | **add** | `redactSecretFields()` has **no doc comment at all**, and it is the enforcement point for the "secrets never reach the renderer" invariant — it blanks `file_seeds[].content`/`content_base64`, `environment[].value`, and the `healthCheck` credentials on every draft read. Verified: no comment above it. A contributor adding a secret-bearing draft field has nothing telling them it must be blanked here too. | Add ~4 lines naming the invariant and stating that every new secret-bearing draft field MUST be added here. | S | `GameWizardDraftService.test.ts`; **behaviour-adjacent** — a missed field is a real leak |
| A-16 | `app/packages/desktop-main/src/services/EcsService.ts:169` | **add** | `EcsService`'s class TSDoc explains delegation to `AwsCloudProvider` but never states the "no persistent ECS Service" invariant — that everything here is on-demand `RunTask`/`StopTask` against `{game}-server` families and a long-running Service destroys the cost model. | Add 2 lines to the class TSDoc. | S | `EcsService.test.ts`; docs-only |
| A-17 | `app/packages/desktop-main/src/services/PulumiService.ts:3403` | **add** | The pre-flight in-doubt marker is written before `stack.up()`; nothing at the settlement write states that the settlement **must reuse the same `sk`**, or the marker survives as a duplicate in-doubt record. That coupling is the whole mechanism. | One line: same `sk` as the pre-flight marker — overwrites it in place, leaving exactly one record per attempt. | S | `PulumiService.apply.test.ts`; docs-only |
| A-18 | `app/packages/desktop-main/src/services/DeploymentConfigService.ts:1` | trim + **add** | 47-line module TSDoc whose last paragraph narrates the HCL→JSON migration and cites issue #96. Meanwhile the `DeploymentConfig.gameServers`-is-the-single-source-of-truth invariant is only implied. | Trim to ~25 and replace the migration paragraph with a one-line statement of the invariant. | S | `DeploymentConfigService.{test,write.test,s3.test}.ts`; docs-only |
| A-19 | `app/packages/desktop-main/src/services/PulumiService.ts:981` | trim | 93-line TSDoc on `getStackOutputs()`; the never-throw contract is real, the "a future change that wants callers to distinguish those cases should…" advice and the `stackInitialized` history are not. | Keep ~25: NEVER throws (with the `finally`-caller reason), the three local short-circuits, "a proxy not a proof", empty-outputs → `null`. | M | `PulumiService.test.ts`; docs-only |
| A-20 | `app/packages/desktop-main/src/services/PulumiService.ts:4684` | trim | 98-line TSDoc on `clearStaleLock()`, half of it re-deriving why `PulumiOperationInFlightError` was reused rather than a new class. | Keep ~30: this method proves nothing (the human already judged), both in-flight flags refused and why, the residual synchronous-check-only race, `@throws`. | M | `PulumiService.clearStaleLock.test.ts`; docs-only |
| A-21 | `app/packages/desktop-main/src/services/AwsProfileService.ts:195` | trim | 96-line TSDoc on `rotateActiveCredentials()` opening with ~20 lines on how it differs from `GuidedIamService.rotate` ("independent siblings that happen to share the same shape"). | Delete the comparison preamble; keep the numbered 0–6 sequence and `@throws` → ~55 lines. | M | `AwsProfileService.test.ts`, `verifyAccessKeyWithRetry.test.ts`; docs-only |
| A-22 | `app/packages/desktop-main/src/services/LogsService.ts:351` | trim | 77-line `@remarks` on `fetchAcrossStreams`; kernel is real but each point is argued at 3–4× its needed length. | Keep ~30: `'newer'` walks oldest→newest and why, `atOldest` only when pagination is exhausted, `excludeEventIds` is an occurrence-counted multiset because `${timestamp}:${message}` is a global key. | M | `LogsService.test.ts`; docs-only |
| A-23 | `app/packages/desktop-main/src/services/PulumiCredentialResolver.ts:48` | trim | 75-line `@remarks` tracing the `execa` call chain through four modules, then ~25 lines on what the tests do and don't prove. | Keep ~20: `''` not omission, because `extendEnv:true` spreads `process.env` first, and both AWS SDKs treat empty-string as unset. Move the coverage inventory into the test file. | M | `PulumiCredentialResolver.test.ts`; docs-only |
| A-24 | `app/packages/desktop-main/src/services/PulumiService.ts:322` | trim | 54-line TSDoc arguing at length that `PulumiPendingDestroyConfirmation`'s `stackName` binding is "real but permanently vacuous". | Keep ~12: the token binds `stateBucket`/`stateBucketRegion` because Reconfigure can change them mid-modal; single-use; minting supersedes. | S | `PulumiService.destroy.test.ts`; docs-only |
| A-25 | `app/packages/desktop-main/src/services/GuidedIamService.ts:346` | trim (light) | 73-line numbered `rotate()` sequence — genuinely entitled under comment-conciseness rule 5. Only step 3's cleanup rationale (~18 lines) is over-long. | Compress step 3 to ~8 lines; leave the rest. Listed so a bulk sweep does not gut it. | S | `GuidedIamService.test.ts`, `guided-iam-rotation-integration.test.ts`; docs-only |
| A-26 | `RunRecordService.ts` (`:20`, `:73`, `:102`, `:161`, `:166`, `:199`, `:400`, `:512`), `DeploymentConfigService.ts` (`:120`, `:464`, `:495`, `:525`), `PulumiService.ts` (~9 sites incl. `:2646`, `:2817`, `:2968`), `DriftService.ts:170` | delete [~22] | Issue-number narration inside prose (`#96`, `#106`, `#109`, `#112`, `#179`, `#399`). `RunRecordService.ts:102` even narrates a *rejected* number ("a departure from the 5MB figure floated in issue #179"). | Strip the refs; keep the behavioural statement and the resulting constant's rationale in one line. **Mechanical sweep sufficient.** | S | respective `*.test.ts`; docs-only |
| A-27 | `ConfigService.ts:30`, `GamesWriteService.ts:222`, `DiscordConfigService.ts:222`, `DiscordCommandRegistrar.ts:5` | delete [4] | Comments describing deleted code: `ConfigService.ts:30` documents watchdog knobs "removed (#348)" and a `server_config.json` that no longer exists; `GamesWriteService.ts:222` is `// splice that used to live inline here`. | Delete the paragraphs. Keep `DiscordCommandRegistrar.ts:43`'s per-guild-only invariant — that one is correct and load-bearing. | S | respective `*.test.ts`; docs-only |
| A-28 | `PulumiService.destroy.test.ts:475`, `RunRecordService.test.ts:742`, `DeploymentConfigService.freshInstall.test.ts:1`, `DeploymentConfigService.write.test.ts:1`, `DeploymentConfigService.test.ts:8`, `DiscordConfigService.test.ts:64`, `EcsService.test.ts:554`, `awsCredentialSource.test.ts:151`, `PulumiLockRecovery.test.ts:445` | delete / trim [9] | Review-round narration inside test files: "Corrected ordering (per review)", "final-review round 2, finding 1", "the fix brief's option B", "Regression coverage note: … used to call". `PulumiLockRecovery.test.ts:445` is a 9-line comment self-describing as "a documentation placeholder, not a real assertion". | Delete the narration; keep one line stating what each file proves. For the placeholder, `test.todo()` it instead. | S | the test files themselves; docs-only |
| A-29 | `ElectronStoreService.ts:340` (`@param value - The value to persist`), `:427`, `:471`, `LockClearConfirmationGate.ts:95`, `LogsService.ts:244`, `:416`, `:528` | delete [7] | `@param` restates the parameter name verbatim. Only 7 in the whole scope — the scripted check found 0, these needed judgement. | Delete the seven lines. | S | respective `*.test.ts`; docs-only |
| A-30 | 118 ASCII `-----` banner lines across 7 files incl. `ElectronStoreService.test.ts`, `SafeStorageService.test.ts`, `ipc-main-bridge.test.ts` | delete [118] | Three-line comment banners wrapping a `describe(...)` that already names the section. Verified at `preload.test.ts:105`-style sites. | Delete all banner comment blocks. **Mechanical sweep sufficient**, zero risk. | S | the test files themselves; docs-only |

### B. `desktop-main` — controllers, modules, top-level, hooks, root configs

| # | Location (file:line) | Class | Issue | Proposed fix | Effort | Risk / test coverage |
|---|---|---|---|---|---|---|
| B-1 | 44 sites across 6 controller files — `iac.controller.ts` (11), `games.controller.ts` (12), `logs.controller.ts` (8), `iac-settings.controller.ts` (6), `iac-runs.controller.ts` (6), `audit.controller.ts` (1) | delete [44] | Trailing line `Reachable via the Electron IPC transport (`iac.plan`).` sitting directly above `@MessagePattern('iac.plan')`. Pure restatement of the decorator on the next line. Count verified by grep. | Delete all 44. Keep the ~6 where the sentence continues into a non-obvious bridging note (e.g. `iac.rollback.confirm`'s). **Mechanical sweep sufficient.** | S | docs-only, no behaviour change |
| B-2 | 10 controller class docs incl. `logs.controller.ts:11`, `audit.controller.ts:11`, `costs.controller.ts:10`, `env.controller.ts:10` | delete [10] | `Handles only IPC messages via @MessagePattern — no HTTP routes are registered here.` restated on every single controller. Count verified. | Delete from all 10. The invariant lives in CLAUDE.md and `openspec/specs/desktop-only-operator-surface`. **Mechanical sweep sufficient.** | S | docs-only |
| B-3 | `app/packages/desktop-main/src/modules/run-record.module.ts:8` (47 lines), `config.module.ts:7` (43), `pulumi-service.module.ts:7` (42), `cloud-provider.module.ts:274` (22), `app.module.ts:37` (27) | trim [5 files] | The native-ESM module-cycle explanation (`forwardRef()` unreliable under strict TDZ ⇒ `ModuleRef.get(strict:false)`) is told **in full in five files**, four of which already end by pointing at the fifth — `run-record.module.ts:53` literally says "document the same conclusion from their own side". Verified: 5–6 cycle mentions per file. | Keep `run-record.module.ts` as the ONE canonical explanation, trimmed to ~18 lines. Reduce the other four to a one-line cross-reference plus their own module-specific content. | M | `cloud-provider.module.test.ts`; DI wiring covered by `npm run app:test:integration` |
| B-4 | `app/packages/desktop-main/src/controllers/iac.controller.ts:917` (64 lines), `:661` (56), `:780` (55), `:1446` (59), `:1127` (51), `:1275` (40), `:1322` (31), `:578` (34), `:495` (27), `:524` (19) | trim [10] | Every large `iac.controller.ts` block narrates the method body step by step and argues with a hypothetical reviewer about roads not taken ("there is no `RunService.createRun` call anywhere in this controller" — a claim about absent code). | Keep the genuinely load-bearing kernels: the await-first-`.next()`-because-the-gate-runs-before-first-yield invariant (`:917`), the contextBridge-drops-`Error`-own-properties trap (`:661`), the TOCTOU close and manual generator drive (`:780`), the accepted-double-run-record consequence and `versionId` recovery (`:1446`), the token-consumed-after-cheap-checks reason (`:1127`), server-side approver resolution (`:1322`). Cut each block by ~65%. | M | `iac.controller.test.ts` |
| B-5 | `app/packages/desktop-main/src/ipc-main-bridge.ts:106` (65 lines) and `:7` (42) | trim [2] | The largest blocks in scope and genuinely load-bearing (the `RpcProxy.create`→`throwError` path, `BaseRpcExceptionFilter` replacing `.message` with `'Internal server error'`, the structured-clone failure mode, the Electron guard). But the filter half duplicates `rpc-error-message.filter.ts:5` in substance, and `:7`'s bullet list has six entries saying "for the same reason as `iac.plan`". | Keep `:106` at ~28 lines (it earns length under rule 5) but replace the `RpcExceptionsHandler` paragraph with a `{@link RpcErrorMessageFilter}` pointer, keeping that story in `rpc-error-message.filter.ts`. Collapse `:7` to a rule plus the two distinct notes (`iac.rollback.confirm`'s `initialArgs` crash; `iac.destroy.mintToken` deliberately excluded). | M | `ipc-main-bridge.test.ts` |
| B-6 | `app/packages/desktop-main/src/modules/aws.module.ts:13`, `wizard.module.ts:12`, `discord.module.ts:9` | rewrite [3] | History framing on live modules: "providers that **used to live here** have been removed", "this module **no longer owns** `ConfigService`'s lifecycle", "which no other provider **previously required**", "After the serverless migration this module **no longer hosts** a discord.js `Client`". | Restate present-tense. `discord.module.ts` in particular should say the invariant directly: "No discord.js client here — the bot lives entirely in the interactions/followup Lambdas." | S | docs-only |
| B-7 | `app/packages/desktop-main/src/modules/cloud-provider.module.ts:171` (38 lines), `:91` (37), `:63` (15), `:142` (15) | trim [4] | "Region source, revisited:", "This is not a DI-factory async hazard:" — paragraphs justifying design decisions to a reviewer rather than informing a caller. | Keep the pre-apply-fallback bootstrap-deadlock paragraph (real, Critical-severity) and the same-singleton `remoteFileStore` requirement. Delete the trailing cross-reference chains. | M | `cloud-provider.module.test.ts` |
| B-8 | `app/packages/desktop-main/src/controllers/logs.controller.ts:19`, `games.controller.ts:31`, `iac-settings.controller.ts:92`, `iac-runs.controller.ts:241` | trim [4] | Each 26–36 lines; each has a real kernel buried in restatement. | Keep: the which-error-means-no-Electron paragraph (`logs.controller.ts:19` is the canonical statement of that trap); the two `listGames` constraints (drift computed locally to avoid a racing second S3 read; deliberately not invalidating the stack-outputs cache on a hot read path); the inside-the-`try` validate placement; the mint-own-`AbortController`-because-IPC-context-has-no-signal reason. | S | `logs.controller.test.ts`, `games.controller.integration.test.ts`, `iac-runs.controller.test.ts` |
| B-9 | `app/packages/desktop-main/src/electron-entry.ts:70` (36 lines) and `:121` (27) | trim [2] | The `x: 252 = 240 + 12` and `y: 20 = (56-16)/2` traffic-light coordinate derivations and the zoom-factor division are genuinely non-obvious and must survive. Padded with a `(D1)`/`(D2/D3)` design-doc reference scheme and "(Task 4)" narration. | Keep the arithmetic and the hard-coded-colors note; strip `(D1)`, `(D2/D3)`, `(Task 4)` and the future-work paragraph (already tracked as the Linux native-titlebar item). | S | e2e electron project (`npm run app:test:e2e`) |
| B-10 | `app/packages/desktop-main/src/main.ts:39` and `:52` | trim / rewrite | 17-line `bootstrap()` doc restating `registerIpcMainBridges`'s own doc, plus a 5-line **inline body comment** above `useGlobalFilters` — violates comment-conciseness rule 3. | Move the ordering constraint ("must precede `app.listen()` — Nest reads global filters while registering handlers") into the TSDoc as one `@remarks` line; replace the bridge re-explanation with a `{@link}`. | S | `ipc-main-bridge.test.ts`; integration suite |
| B-11 | `app/packages/desktop-main/src/cloudformationTemplate.ts:10` | trim | 29 lines describing three path candidates the `candidates` array below lists literally, incl. "a later group's `GuidedIamService`" — PR-stack narration. | One line per non-obvious layout + "returns `undefined` rather than throwing". Target ~9 lines. | S | integration suite |
| B-12 | `app/packages/desktop-main/src/test-mocks/pulumi-mock.ts:51` (42 lines); `ecs-mock.ts:11`, `run-record-mock.ts:101`, `remote-file-store-mock.ts:117` | trim [4] | `pulumi-mock.ts` has `**Scripting surface**`/`**Un-scripted surface**`/`**Lifecycle**` sections plus follow-up-work narration. The other three carry near-identical 17–19-line "prototype patching is sufficient because the client is created lazily" blocks. | Keep the real safety property in `pulumi-mock.ts` ("no subprocess, no engine download, no AWS call — structurally incapable of reaching real infra"). Keep the prototype-patching rationale in `ecs-mock.ts` only; the other two become 3-line summaries with a `{@link}`. | S | test-only |
| B-13 | `app/packages/desktop-main/src/controllers/iac.controller.ts:150` | trim | `IacPlanEndMessage`: "unlike the spawned CLI process this channel **originally** bridged" — history inside an otherwise-useful explanation. | Drop the clause; keep "the Automation API has no process exit code, so `null` uniformly means 'did not succeed'". | S | docs-only |
| B-14 | `app/packages/desktop-main/src/test-mocks/mock-store.ts:52` | **add** | `export const mockStore = new MockStore()` — the process-wide singleton every Playwright fixture pushes into — has no doc, while the class above it does. Its reset-between-tests contract is the easily-broken part. | 2 lines: process-wide singleton; `ServerMocks`/`IpcHarness` must `reset()` between tests or queued responses leak across specs. | S | test-only |
| B-15 | `.claude/hooks/lib/hook-io.ts:16` (`readStdin`), `:41` (`deny`) | **add** [2] | Both exported and undocumented while their siblings `ask`, `allow`, `isRecord` have docs. `deny`'s `never` return is non-obvious: it calls `process.exit(0)` — exit code **0**, not non-zero — signalling refusal via the payload. | One-line TSDoc each; for `deny`, state the exit-0 protocol explicitly. | S | hooks are not unit-tested; docs-only |
| B-16 | `app/vitest.config.ts:9-36` | trim | Four near-identical inline blocks, three literally beginning "Same rationale as … above" — ~22 comment lines for 6 alias entries. | Keep the first block (source-alias rationale + the subpath-ordering trap, a real footgun); delete the three repeats. | S | whole unit suite depends on it; `npm run app:test` |
| B-17 | `app/test/packaging-manifest-pins.test.ts:6` | trim | 30-line preamble with a parenthetical history paragraph about `@cdktf/hcl2json` being externalized "before the `migrate-iac-to-pulumi` change removed it". | Delete the parenthetical; keep the three-manifest table and the "nothing else notices" rationale — that genuinely explains what the spec guards. | S | self-covering |

### C. `web` (39,801 LOC, 202 files)

| # | Location (file:line) | Class | Issue | Proposed fix | Effort | Risk / test coverage |
|---|---|---|---|---|---|---|
| C-1 | 17 doc-comment sites across 10 non-test files — `api.service.ts:10,36,417,454,481,521`, `polling/game-status-provider.component.tsx:55`, `pages/discord.page.tsx:89`, `pages/games.page.tsx:41`, `components/pending-changes-banner.component.tsx:6,66`, `components/game-card.component.tsx:135`, `components/add-game-wizard/wizard-form.utils.ts:313`, `components/add-game-wizard/review-step.component.tsx:45`, `components/edit-game-form/edit-game-form.component.tsx:21`, `hooks/use-file-manager.hook.ts:14` | delete [17] | **Doc comments describing HTTP routes that do not exist** — `` `GET /api/status` ``, `` `GET /api/costs/estimate` ``, `` `POST /api/games` ``, `` `PATCH /api/games/:name` ``, `` `GET /api/drift` ``. Directly contradicts the no-HTTP-transport invariant *and* `api.service.ts:1-4`'s own header four lines above the first offender. Count verified by script. | Replace each HTTP verb+path with the IPC channel name (`GET /api/status` → `games.status`; `GET /api/drift` → `drift.get`, already parenthesised at `pending-changes-banner.component.tsx:66`). **Mechanical sweep sufficient.** Highest-value single change in the package. | M | docs-only, no behaviour change |
| C-2 | 41 sites across `src/` (30) and `e2e/` (11) — e.g. `games.page.tsx:22`, `add-game-wizard.component.tsx:1`, `e2e/pages/GamesPage.ts:4,8`, `e2e/pages/AuditPage.ts:5` | delete [41] | Bare issue-number attributions inside TSDoc summaries: `(#99)`, `(issue #92)`, `(#100)`, `(#184, epic #139)`, `(#211)`, `(#348)`. Every one lands in a hover tooltip. | Strip the parentheticals. **Exception:** keep the ~6 that name a still-live constraint rather than provenance (e.g. `iac.page.tsx:60`'s destructive-button gate). **Mechanical sweep sufficient.** | M | docs-only |
| C-3 | `app/packages/web/playwright.config.ts:43-71` | trim | 29-line migration-status essay: "Two e2e projects run side by side during the Electron pivot (Epic F #140)", "`costs.spec.ts` (migrated in #193)", "`logs.spec.ts` (migrated in #191)", "`discord.spec.ts` (migrated in #194)", "renamed from the pre-migration IaC spec". The dual chromium+electron split IS still live (verified: both projects defined), but the archaeology is not — and the spec-name enumeration duplicates the `ELECTRON_SPECS` array immediately below it. | Replace with ~5 lines: `electron` drives the packaged main bundle via `_electron.launch()`; `chromium` runs the remaining stub-based specs against `vite preview`, stubbing `/api/*` via `page.route()` — those cannot run under Electron. Drop every issue number and the spec list. | S | docs-only |
| C-4 | `app/packages/web/playwright.config.ts:76-84` | delete | 9-line inline comment narrating a stale-glob bug — "this glob was left stale, which silently routed the file to the `chromium` project instead. Harmless in practice… but wrong". **The bug is fixed**: `'**/iac.spec.ts'` is present on the next line. Verified. | Delete lines 76-84 outright. | S | docs-only; `iac.spec.ts` runs in the electron project |
| C-5 | `app/packages/web/src/app.component.tsx:79-90` | delete | 12-line bullet list restating the `<Routes>` JSX 20 lines below — **and stale**: it calls `/costs`, `/discord` and `/logs` "placeholder", but `CostsPage` (323 lines), `DiscordPage` (1,259) and `LogsPage` (273) are fully implemented. Verified. | Delete `:79-90`. Keep `:62-78` — the `HashRouter`-vs-`BrowserRouter` `file://` rationale is load-bearing and not inferable. | S | docs-only |
| C-6 | `app/packages/web/src/api.service.ts` — 24 sites | rewrite [24] | 24 copies of "Mirrors `X` in `@hyveon/shared/...` — that file is the source of truth; keep this copy in sync with it." A 24×-repeated manual-sync hazard marker rather than a fix. The file already imports from `@hyveon/shared` (line 6). | Preferred: import the types from `@hyveon/shared` and delete both the local copies and the 24 comments. If the duplication must stay, state the reason **once** in the file header. See the cross-cutting note — this belongs with the duplication audit. | M (comments) / L (dedupe types) | type-only; caught by `npm run app:typecheck` |
| C-7 | `e2e/pages/DiscordPage.ts:11-26`, `e2e/pages/CostsPage.ts:16-24`, `e2e/pages/DashboardPage.ts:24-43`, `e2e/screenshots/capture.spec.ts:64-81` | rewrite [4] | The `file://` + `HashRouter` + two-step-hash-assignment rationale re-derived at paragraph length in four places (~55 lines total), each cross-referencing the others. | Write it once at the top of `e2e/pages/`; reduce each site to one line: "Two-step hash assignment — a same-value `location.hash` set fires no `hashchange`, so `HashRouter` would not re-render." ~55 → ~12. | M | all electron-project specs; behaviour unchanged |
| C-8 | `e2e/specs/streaming-handle-roundtrip.spec.ts:4-48`, the same file `:191-215`, and `e2e/screenshots/demo-data.ts:511-524` | trim [3] | The "mocks return a plain async-iterable, not a real `async function*`" digression appears **three times verbatim**. `:4-25` (the actual contextBridge clone bug and why jsdom cannot catch it) is excellent and must stay. | Keep `:4-25`. Collapse `:26-48` to two lines, `:191-215` to four, and `demo-data.ts:511-524` to a one-line pointer. | S | the specs are their own coverage |
| C-9 | `app/packages/web/src/pages/games.page.tsx:19-64` | trim | 46 lines; one real constraint (only one `AddGameWizard` can be open at a time, because two instances race the single `addGameWizardDraft` autosave slot) buried under JSX re-narration. Also contains a stale claim at `:31-32`: the detail view is "still to be implemented", but `GameDetailPage` exists and is routed at `app.component.tsx:117`. | Trim to ~10 lines: route summary, the three row shapes, the single-instance autosave race. Delete the "still to be implemented" clause. | S | `e2e/specs/games.spec.ts`, `games.page.test.tsx` |
| C-10 | `app/packages/web/src/components/add-game-wizard/add-game-wizard.component.tsx:1-42` | trim | 42 lines restating the component name, the six step imports, and the `GameWriteResult` switch below. | Keep ~12: the debounced-autosave "cleared only on success, never on validation/conflict" rule, the jump-to-earliest-failing-step behaviour, `initialDraft`/`hideTrigger`'s purpose. | S | `add-game-wizard.component.test.tsx`, `e2e/specs/games.spec.ts` |
| C-11 | `iac.page.tsx:171`, `first-run-wizard.component.tsx:13` and `:314-316`, `stack-init-step.component.tsx:65,79`, `e2e/screenshots/demo-data.ts:448`, `pages/settings.page.test.tsx:402` | delete [7] | Behaviour defined by contrast with a **deleted** pre-Pulumi `init` step / IaC CLI: "rather than the pre-migration IaC CLI's subcommand names this type used to carry", "the ANSI log the deleted pre-migration `init` step rendered". Nothing named exists in the tree. | Drop the contrast clause, keep the positive statement. **Mechanical sweep sufficient.** | S | docs-only |
| C-12 | `app/packages/web/src/hooks/use-log-tail.hook.ts:411-445` | trim | 35 lines framed as history ("This replaces the old model where `newestTimestampRef`… were seeded once"). The failure mode is real: a pinned scalar goes stale once `loadOlder`'s evict-from-bottom drops the original tail, leaving an unbridged temporal gap. | Rewrite present-tense as a constraint; drop the replaces-the-old-model framing and the fallback-case paragraph. 35 → 12. | M | `use-log-tail.hook.test.ts` (has generation-guard tests) |
| C-13 | `app/packages/web/src/components/deployment-settings-form.component.tsx:1-34`, `components/edit-game-form/edit-game-form.component.tsx:1-33` | trim [2] | 34 and 33 lines. `edit-game-form` quotes the *issue text* verbatim ("since the issue is scoped as 'reuses most of the wizard from the prior issue but as a flat form (not stepwise)'"). | Keep the always-send-etag rule (deployment settings) and the rename=delete+recreate / self-exclusion-from-collision-list rules (edit game). Delete the issue quote and the branch lists. 34→8, 33→10. | S | `deployment-settings-form.component.test.tsx`, `edit-game-form.component.test.tsx` |
| C-14 | `app/packages/web/src/pages/iac.page.tsx:386-419`, `:91-95` | trim / rewrite [2] | `:386-419` asserts a cross-file implementation detail about `iac.controller.ts`'s catch branches that will silently rot. `:91-95` is an inline body comment explaining a `useState` shape by narrating the lint rule that forced it ("Previously the effect cleared `chunks`/`ended` synchronously… which is what `react-hooks/set-state-in-effect` flags"). | `:386-419` → keep mutual-exclusion as one line + the corrupt-state rationale, 34→12. `:91-95` → move into `useIacRunLog`'s TSDoc, present tense. | S | `iac.page.test.tsx` |
| C-15 | `app/packages/web/src/components/first-run-wizard/first-run-wizard.component.tsx:523-552`, `e2e/fixtures/ipc-harness.ts:105-140`, `e2e/screenshots/demo-data.ts:489-525` | trim [3] | `first-run-wizard` spends half its 30 lines documenting a channel that does not exist ("Deliberately does not call `wizard.bootstrap.lockTable` — no such channel exists"). `ipc-harness` and `demo-data` each have a real API constraint buried in a per-mock inventory. | Keep the config-bucket-seed-is-chained-not-parallel constraint, the `TestingModule`-vs-`createApplicationContext` constraint, and the `addInitScript`-vs-`evaluate` + reload requirement. Delete the negative-space and inventory prose. | S | `first-run-wizard.component.test.tsx`; tier-2 integration specs |
| C-16 | `app/packages/web/src/components/watchdog-panel.component.tsx:1-13` | rewrite | Opens "Panel that **used to** read and write the three watchdog tuning knobs… That save flow was dead on arrival" — the reader learns about a deleted feature before learning what the file is. | Lead with what it is: a static pointer to `DeploymentSettingsForm`'s Watchdog section, because the deployed watchdog Lambda reads `DeploymentConfig`, not `server_config.json`. 13 → 3. | S | `e2e/pages/SettingsPage.ts:18` asserts read-only |
| C-17 | `app/packages/web/src/components/add-game-wizard/wizard-form.utils.ts:555` | **add** | `` const key = `${issue.path}<NUL>${issue.message}`; `` — the separator is a **raw NUL byte, invisible in every editor and diff** (verified: 1 NUL byte at line 555 col 30; it makes `file` report the source as binary data and makes grep skip it). No comment explains the choice; a reader sees `${issue.path}${issue.message}` and "fixes" it into a collision bug. | Add one line: NUL separator — cannot occur in a validator path or message, so keys cannot collide. (Separate from replacing the raw byte with `\0` — see Bugs.) | S | `wizard-form.utils.test.ts` covers `dedupeIssues` |
| C-18 | `src/components/audit-entry-row.component.tsx:30`, `src/components/rollback-action.component.tsx:22`, `src/pages/iac-history.page.tsx:25`, `src/pages/iac-run-detail.page.tsx:160` | rewrite [4] | The duplicated one-line doc is the symptom; `formatTimestamp` itself is byte-for-byte identical in all four files. The doc is correctly terse — the code is not DRY. | Extract to `src/lib/format.utils.ts`, keep the single existing doc, import at four sites. Flagged here but belongs to the duplication audit. | S | `audit-entry-row.component.test.tsx`, `iac.page.test.tsx`; pure refactor |
| C-19 | `e2e/fixtures/hyveon-http-bridge.ts:24` | rewrite | "The Nest API no longer requires a bearer token" — describes a Nest *API server* that per the invariants does not exist. The shim itself is live and correct (it forwards to `page.route()` stubs). | Say the calls resolve against Playwright route stubs, not a Nest API. | S | chromium-project specs |
| C-20 | 36 ASCII `-----` banner lines across 3 files in `web` | delete [36] | Same banner pattern as A-30. | Delete. **Mechanical sweep sufficient.** | S | docs-only |
| C-21 | `e2e/screenshots/demo-data.ts:221,234,260,279,382`; `e2e/fixtures/index.ts:291`; `e2e/fixtures/server-mocks.ts:50` | **no action** | The seven "undocumented exports" in `web`. Five are self-describing demo constants under a 24-line file header that already explains the fixture set; both `test` exports carry adjacent explanatory comments. | **Do not add docs.** `/** Demo cost estimates. */` is exactly the padding comment-conciseness rule 5 warns against. Recorded so a later sweep does not add it. | — | — |

### D. `infra` (8,789 LOC, 26% comment)

| # | Location (file:line) | Class | Issue | Proposed fix | Effort | Risk / test coverage |
|---|---|---|---|---|---|---|
| D-1 | `app/packages/infra/src/program.ts:1` | delete | 136-line file doc, ~95 of which is a numbered 69-row "resource-inventory audit" of the deleted Terraform tree. The comment itself concedes: "the tree no longer exists on disk, so this count is historical and can't be re-derived from the repository today." Verified verbatim. | Delete rows 1–69 and the closing "Confirmed zero unclaimed resources". Keep ~12 lines: the `defineX`-per-area pattern and the two live omissions (runs table is bootstrap-managed; `appliedGameServers` is not `pulumi.secret`, so callers must redact — that one still binds `PulumiService`). | S | `program.test.ts`; docs-only |
| D-2 | 249 HCL identifier citations across 13 files — e.g. `ecs.ts:37`, `iam.ts:177`, `securityGroups.ts:99`, plus ~45 `` \| `aws_x.y` \| {@link …} \| `` address-table rows and 27 ``Mirrors `var.x``` prefixes | delete [249] | Inline citations of `` `aws_ecs_cluster.main` ``, `` `aws_iam_role.watchdog_lambda` ``, `` `for_each` ``, `` `count = 2` ``. Every one names a symbol in a tree that was deleted. Count verified by script. | Strip the citation, keep the surrounding sentence. **Exception — keep any citation that pins a name that must not change**: `ecs.ts`'s `{game}-server` family, `securityGroups.ts`'s `-v2-` `namePrefix`. Those are behavioural. **Mechanical sweep sufficient for the rest.** | L | every `app/packages/infra/src/*.test.ts`; docs-only |
| D-3 | `app/packages/infra/src/securityGroups.ts:49, 60, 107, 328, 401, 408, 415, 465` | rewrite [8] | Eight separate comments each re-citing "issue #349" and re-quoting the same section title verbatim; three restate the full attributes-as-blocks rationale in situ. | One canonical explanation in the file doc (why `efsSeederSg`/`healthCheckSg` carry no inline egress, and why `namePrefix` carries `-v2-`). Every other site collapses to `// No inline egress — see this file's doc.` **Keep the `-v2-` constraint at `:405`** — removing it un-replaces the group and leaves the open-egress rule live. | S | `securityGroups.test.ts`; docs-only, but the `-v2-` note must survive |
| D-4 | `app/packages/infra/src/lambdas.ts:1` | rewrite | 165-line file doc: an 18-row HCL-address table for a deleted tree, a 50-line lambda-bundle-path essay, a 30-line "deviation from the HCL" section — and it is **stale**: the header says "The **five** Lambda functions" while there are six packages under `app/packages/lambda/` and `docs/docs/components/lambdas.md:8` says "Six TypeScript Lambda packages". Verified. | Cut to ~30 lines: a docs pointer; the two real constraints (`lambdaBundlesDir` has no default because a bundled `import.meta.url` default breaks only in the packaged app; policies attach after functions, so the seeder invocation needs an explicit `dependsOn`). Delete the address table. Fix the five/six count. | M | `lambdas.test.ts`; docs-only. See Bugs — CLAUDE.md has the same error |
| D-5 | `securityGroups.ts:1` (75 lines), `secrets.ts:1` (84), `escapes.ts:1` (63), `discordDomain.ts:1` (62), `iam.ts:1` (54), `dynamodb.ts:1` (54), `route53.ts:1` (37), `network.ts:51` (22) | trim [8] | Every file doc restates `docs/docs/components/infra.md` at essay length. `secrets.ts:1` even embeds a test-design essay (why `secretResourceOptions.forVersion` is a method so `vi.spyOn` works) into a *source* file, repeated again at `secrets.ts:130`. `discordDomain.ts` has two sections stating Pulumi/AWS **defaults** rather than a WHY. | Trim each to ~20–30 lines keeping only the constraints: SG inline-vs-standalone hazard; no secret material enters the stack and `ignoreChanges: ['secretString']`; `TableItem` is content-not-infra + `seedsHash`/`dependsOn`; Function URLs cannot be ALIAS targets + us-east-1 provider scoping; the two-function split because policies need `followupLambdaArn`; the runs-table schema block (that one is the contract `ensureRunsTable` must match — keep it). `route53.ts:1`'s `## INVARIANT: no DNS record resources` section is **exactly right — keep verbatim** and delete only the declared-elsewhere inventory around it. | M | each file's sibling `*.test.ts`; docs-only |
| D-6 | Runs-table invariant stated in full at `dynamodb.ts:1`, `program.ts:1` (row 64), `program.ts:556`, and `docs/docs/components/infra.md#the-runs-table-invariant`; DNS invariant at `route53.ts:1`, `discordDomain.ts:1`, `dynamodb.ts:1`, `program.ts:1` | rewrite [~12] | Four copies of one invariant, four of another. Not just bloat — a live drift risk, and `lambdas.ts:1` has already drifted (D-4). | House rule for the sweep: the code comment states the invariant in one line and links the docs page; the docs page carries the reasoning. | M | docs-only; re-run `docs-accuracy-auditor` afterwards |
| D-7 | `app/packages/infra/src/program.ts:675` (56 lines), `:556` (26), `:154` (20), `:494-546` (nine member docs) | trim [4] | `:675` spends ~28 lines touring `@pulumi/pulumi` internals (`runInPulumiStack`, `massage()`, why Node has no `pulumi.export`) — SDK documentation, not this function's contract. `:154` explains which tags the *retired* tool used to set. `:494-546`'s nine members each open "Mirrors {@link StackOutputs.x} — the legacy tool's outputs file's `y` output was…" when the `{@link}` already says it. | `:675` → keep ~15 (config captured by closure; declarations inside the closure only; return `Output`-wrapped fields rather than pre-resolving). `:556` → 5 lines. `:154` → 4 lines. `:494-546` → keep `Mirrors {@link …}` + the derivation, drop the provenance clause; keep `interactionsInvokeUrl`/`discordInteractionsUrl`'s real note. | S | `program.test.ts`; docs-only |
| D-8 | `app/packages/infra/src/index.ts:1` | rewrite | "replaces the Task-2.1 `export {}` placeholder" and "`desktop-main` (wired in a later phase…)" — dead phase narration. This file **is** the package entry point (`package.json` `main: ./dist/index.js`), so it is the one legal home for `@packageDocumentation`, which the repo uses nowhere. | Rewrite as a 3-line `@packageDocumentation` block: what `@hyveon/infra` is + a link to `docs/docs/components/infra.md`. | S | `index.test.ts`; docs-only |
| D-9 | `app/packages/infra/src/ecs.ts:189-207` | **add** | The Caddy sidecar container block (ports 443/80, `caddy reverse-proxy --from`) carries **no comment at all** — verified. The "TLS terminates in-task via a Caddy sidecar; no ALB, target group or ACM certificate" invariant is nowhere at this site; `ecs.ts`'s file doc covers only the no-Service invariant. A contributor adding HTTPS "properly" reaches for an ALB right here. | One line above the `if (isHttps)` block naming the invariant and `openspec/specs/in-task-tls-termination`. | S | `ecs.test.ts`; docs-only |
| D-10 | `app/packages/infra/src/network.ts:45` (`PUBLIC_SUBNET_COUNT = 2`) | **add** | The doc says only "a hardcoded constant in the HCL … so it stays a hardcoded constant here too" — verified. It never says what happens if someone raises it: subnet CIDRs shift, every public subnet is replaced, and `efs.ts`'s mount targets (one per subnet) go with them. Exactly the reasonable-looking change this constant invites. | One line: changing this replaces every public subnet and its EFS mount targets; it is not a safe knob. | S | `network.test.ts`, `efs.test.ts`; docs-only |
| D-11 | `app/packages/infra/src/lambdas.ts:97`, `:352`, `:369` | delete / trim [3] | `:97` is a parenthetical explaining that a precedent is *not* a precedent ("…so it isn't an applicable precedent here"). `:352` spends 9 lines reconstructing `archive_file` semantics to justify a one-line `AssetArchive`. `:369` justifies sorting by appeal to "the legacy tool's `keys(map)` order". | `:97` → delete the parenthetical, keep "env var → `app.isPackaged` → repo-relative dev fallback". `:352` → 3 lines. `:369` → keep only "sorted so the value is deterministic across config-object iteration order". | S | `lambdas.test.ts`; docs-only |
| D-12 | `app/packages/infra/src/network.ts:51`, `efs.ts:30`, `ecs.ts:65` | trim [3] | `network.ts:51` reconstructs `cidrsubnet(var.vpc_cidr, 8, count.index)` semantics for a function with no Terraform counterpart. `efs.ts:30` quotes `count = 2, subnet_id = aws_subnet.public[count.index].id`. `ecs.ts:65` says "the `logConfiguration` block every container definition **in the HCL** uses". | Keep the live constraints only — in particular `network.ts`'s "the derivation must stay byte-identical or every subnet is replaced on the next apply", which is behavioural and must survive the D-2 sweep. | S | `network.test.ts`, `efs.test.ts`, `ecs.test.ts`; docs-only |
| D-13 | `app/packages/infra/src/testing/pulumiMocks.ts:148-153`, `program.test.ts:27`, `discordDomain.test.ts:128`, `securityGroups.test.ts:359` | delete [4] | Regression narration in test/harness files: "both `certificate.arn` and `certificateValidation.certificateArn` **used to resolve to `undefined` before this entry existed**", "deliberately NOT the fixed-flush pattern **this file used to use**", "two `undefined`s **used to** compare equal too". | Keep each live rationale ("`arn` is a computed-only ACM output, never part of `CertificateArgs`, so the mock must supply it"; "await real resource handles, never a fixed flush"); delete the before/after stories. | S | the test files themselves; docs-only |

### E. `shared`, `desktop-preload`, `cloud-aws`

| # | Location (file:line) | Class | Issue | Proposed fix | Effort | Risk / test coverage |
|---|---|---|---|---|---|---|
| E-1 | `app/packages/shared/src/canRun.ts:15` | **add** | The doc says the logic was "moved to shared so the Nest server, InteractionsLambda, and FollowupLambda all use one copy" — it never states the invariant **never fork this**, and "Nest server" is stale (desktop-main is an IPC microservice, no HTTP server). Verified verbatim. Highest-value gap in scope: a contributor adding a Lambda will copy it. | Replace the last paragraph: "Exactly one copy — imported verbatim by `@hyveon/desktop-main` and every Lambda. Never fork or re-implement this logic (CLAUDE.md invariant)." | S | `canRun.test.ts`; docs-only |
| E-2 | `app/packages/desktop-preload/src/hyveon-api.ts:7` | rewrite | **Factually wrong.** "Keep this file in sync with the `contextBridge.exposeInMainWorld('hyveon', {...})` call in `src/index.ts`" — verified: that call is at `preload.ts:962`; `index.ts` is a pure type re-export barrel with no such call. | Point at `preload.ts`. | S | `preload.test.ts` asserts the bridge shape; docs-only |
| E-3 | `app/packages/desktop-preload/src/preload.ts:1` | **add** + trim | 89-line module doc explaining the contextBridge clone boundary at length, but it never states the **no-HTTP-transport** invariant — nothing tells a reader why there is no bearer token, no fetch, no API base URL anywhere. Separately, the `iac.plan` / `iac.runs.get` / `iac.runs.list` sections restate what each wrapper's own TSDoc says. | Add one line naming the invariant and `openspec/specs/desktop-only-operator-surface`. Then trim to ~30 lines, keeping the "Streaming channels and the contextBridge clone boundary" section (the load-bearing kernel) and the channel-naming convention. | M | `preload.test.ts`; docs-only |
| E-4 | `app/packages/shared/src/secrets/secretsStore.ts:26` | **add** | Reads `process.env['AWS_REGION_']` with **zero** explanation, unlike `ddb/client.ts:7` and `infra/src/lambdas.ts:249` which both explain it. Verified. Looks like a typo here. | One line: `AWS_REGION_` (trailing underscore) — `AWS_REGION` is reserved by the Lambda runtime. Better: call a shared `resolveDefaultAwsRegion()`. | S | `secrets/secretsStore.test.ts` |
| E-5 | `app/packages/cloud-aws/src/AwsCloudProvider.ts:361` (`findRunningTask`) | **add** | `ListTasksCommand` is paginated and `nextToken` is never followed; the doc says "leaving the single active task, if any" without saying *why* one page is provably enough. ECS `ListTasks` is also eventually consistent, so a just-launched task can be missed. | Add: one page suffices because `startWorkload`'s guard keeps at most one task per `{game}-server` family; `ListTasks` is eventually consistent, so the in-process `withGameLock` is what actually prevents duplicate starts. | S | `AwsCloudProvider.test.ts` |
| E-6 | `app/packages/cloud-aws/src/AwsRunRecordStore.ts:284` (`listRuns`) | **add** | 27-line doc covers `ExclusiveStartKey` reconstruction in depth but never mentions that `status-index` is a **GSI and therefore eventually consistent** — a run written moments ago can be absent from a status-filtered page. | Append one sentence naming the GSI eventual-consistency window. | S | `AwsRunRecordStore.test.ts` |
| E-7 | `AwsAuditLogStore.ts:26`, `AwsRunRecordStore.ts:90`, `AwsRemoteFileStore.ts:42`, `AwsSecretsStore.ts:41`, `AwsCloudProvider.ts:201`, `AwsDiscordConfigStore.ts:36`, `AwsDiscordEventReceiver.ts:25`, `shared/src/cloud.ts:168`, `shared/src/errors.ts:26`, `:59`, `shared/src/discord/awsLambdaDiscordReceiver.ts:35` | rewrite [11] | **11 TSDoc blocks open directly with `@param` and never state a summary** — the only remaining tag-structure defect class in the repo (the scripted order/modifier check found zero others). | Add a one-line summary above the first `@param` in each. This is also exactly what `jsdoc/require-description` would enforce — see Rules. **Mechanical sweep sufficient.** | M | docs-only |
| E-8 | `AwsAuditLogStore.ts:26` (37 lines), `AwsRunRecordStore.ts:90` (32), `AwsRemoteFileStore.ts:42` (23), `AwsSecretsStore.ts:41` (22), `AwsCloudProvider.ts:201` (18) | trim [5] | The same credential-signature paragraph (rename-safety / zero-arg constructibility / `Promise` return / "no NestJS async-factory gymnastics" / `credentialsSignature`) appears **five times**, largely verbatim. | Keep the canonical version in `AwsAuditLogStore` at ~6 lines (resolved per call so a table rename is picked up; optional ⇒ methods throw "table not configured"; may return a `Promise`; `credentialsSignature` keys the client cache so rotations rebuild). The other four cross-reference it. 132 → ~35 lines. | M | each store's `*.test.ts` |

## Scope of Work
- Asset analysis and workspace initialization.
- Core modeling / development based on specifications.
- Technical validation and quality checks.
- Incorporation of review feedback.
- Clean handover of source files and documentation.

## Required Files & Inputs
1. Complete reference files (drawings, access tokens, test data).
2. Exact dimensional specs or business rules.
3. Schedule/deadline expectations.

## Estimated Price and Timeline
- **Estimated Price:** 800 - 2000 EUR
- **Estimated Timeline:** 3 to 7 business days (to be refined after reviewing the final assets).

## Project Questions
To help me refine this estimate, please clarify:
1. Avez-vous déjà réalisé l'étude de sol géotechnique pour les fondations ?
2. Quelles sont les charges d'exploitation particulières (machines, toiture végétalisée) ?
3. Fournissez-vous les plans d'architecte définitifs au format DWG ?
4. Quels sont les détails d'exécution attendus (nomenclatures d'acier, détails de ferraillage) ?
5. Quel est votre calendrier souhaité pour la validation des plans ?

## Agreement Terms
The final source files will be delivered upon approval of the milestones. Substantial revisions outside the agreed scope will require a change order.
