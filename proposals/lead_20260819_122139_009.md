# Project Proposal — Structural Drawings

## Project Understanding
You need assistance with: 
**[wallet-desktop] remove all btc CLI integration code (Task 17 prep)**

*Project description:*
## Goal

Remove every piece of wallet-desktop code that integrates with the `btc` CLI binary via subprocess. wallet-desktop should integrate ONLY with the `bitcoin-wallet-core` Rust crate via `dart:ffi` (FFI). The `btc` CLI binary remains for **developer terminal testing only** (per user 2026-08-19 directive), not bundled or invoked by the desktop app.

This is Task 17 of `docs/superpowers/plans/2026-08-19-flutter-ffi-bitcoin-wallet-core.md` — pulled forward ahead of the consumer-migration phase because the subprocess path is now provably obsolete (PR #227 spike + Issue #206 + real-binary demo all confirm).

## Scope

### Delete (artifacts)

- `wallet-desktop/test/integration/fixtures/fake_btc.sh`
- `wallet-desktop/test/integration/fixtures/with_secret_env.sh`
- `wallet-desktop/assets/btc/` (entire dir — 5 arch placeholders + .gitkeep)
- `wallet-desktop/scripts/build_fake_btc.sh` (operator-side fixture builder)
- `.github/workflows/btc-bundle.yml` (cross-compile btc on tags)
- `wallet-desktop/pubspec.yaml` — remove `assets:` block

### Delete (Dart code)

- `wallet-desktop/lib/core/btc/btc_invoker.dart` — replaced by FFI `WalletCore`
- `wallet-desktop/lib/core/btc/btc_command.dart` — replaced by typed FFI wrappers (Task 7)
- `wallet-desktop/lib/core/binary/btc_extractor.dart` — replaced by `NativeLib.open()` (Task 6)
- `wallet-desktop/lib/core/binary/` (whole dir)
- `wallet-desktop/lib/core/btc/models/wallet_created.dart` — keep (DTO shape matches FFI return; rename file or migrate later)
- `wallet-desktop/lib/providers/btc_providers.dart` — `btcInvokerProvider` → `walletCoreProvider` (Task 8)

### Rewrite (operator scripts)

- `wallet-desktop/scripts/smoke/v0.1.0.sh` — currently spawns `btc` CLI for headless steps. Rewrite to:
  - Launch `flutter run -d linux` (operator clicks buttons per Issue #205)
  - Capture per-story screenshots
  - Grep logs for L12 CRITICAL #2 cleartext
- `wallet-desktop/scripts/smoke/UI_TEST_CHECKLIST.md` — update prerequisites: "real btc binary" → "build native lib via `tool/build_native.sh`"

### Keep (developer-only, not in wallet-desktop bundle)

- `rust-wallet-app/crates/btc/` (the CLI itself — stays as developer tool for terminal testing)
- `rust-wallet-app/scripts/btc-quickstart.sh`, `btc-networks-demo.sh`, `btc-import-demo.sh`, `btc-balance-demo.sh` (demo scripts that operators run in terminal)
- `.github/workflows/btc-cli-demo.yml` (CI for the CLI demos — still useful for btc CLI regressions)

## Out of scope (separate issues)

- Migrating `WalletListNotifier`, `WalletCreateScreen`, etc. to FFI calls (Tasks 10-16 in the plan)
- Building the FFI cdylib on release tags (Task 18)

## Acceptance

- [ ] All listed files removed from `main`
- [ ] `git grep -n "BtcInvoker\|BtcCommand\|BtcExtractor\|fake_btc" wallet-desktop/` returns 0 hits
- [ ] `wallet-desktop/` has zero `Process.start` calls (grep for it)
- [ ] `cargo build --release -p bitcoin-wallet-core` still works (cdylib intact)
- [ ] `flutter test test/ffi/` still passes (FFI path intact)
- [ ] `Issue #203` (L29 smoke) + `Issue #205` (UI checklist) become achievable via UI clicks instead of CLI spawn

## Related

- Plan: `docs/superpowers/plans/2026-08-19-flutter-ffi-bitcoin-wallet-core.md`
- Issue #206 (btc→Dart gap) — closed structurally by FFI rewrite; this issue implements Task 17
- Issue #207 (Task 1 spike, DONE)
- Issue #208 (Task 2, in progress)
- Issue #205 (UI checklist) — script rewrite needed
- Issue #203 (L29 smoke) — script rewrite needed

## User directive (verbatim)

> "we are integrating with rust core /home/nhitran/Projects/blockchain-sdk/rust-wallet-app/crates/bitcoin-wallet-core, don't use btc cli"
> "remove integrate btc cli in wallet-desktop"
> "btc cli only use on terminal for developer testing"

## Scope of Work
- Asset analysis and workspace initialization.
- Core modeling / development based on specifications.
- Technical validation and quality checks.
- Incorporation of review feedback.
- Clean handover of source files and documentation.

## Required Files & Inputs
1. Complete reference files (drawings, access tokens, test data).
2. Exact dimensional specs or business rules.
3. Schedule/deadline expectations.

## Estimated Price and Timeline
- **Estimated Price:** 800 - 2000 EUR
- **Estimated Timeline:** 3 to 7 business days (to be refined after reviewing the final assets).

## Project Questions
To help me refine this estimate, please clarify:
1. Avez-vous déjà réalisé l'étude de sol géotechnique pour les fondations ?
2. Quelles sont les charges d'exploitation particulières (machines, toiture végétalisée) ?
3. Fournissez-vous les plans d'architecte définitifs au format DWG ?
4. Quels sont les détails d'exécution attendus (nomenclatures d'acier, détails de ferraillage) ?
5. Quel est votre calendrier souhaité pour la validation des plans ?

## Agreement Terms
The final source files will be delivered upon approval of the milestones. Substantial revisions outside the agreed scope will require a change order.
