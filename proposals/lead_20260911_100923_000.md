# Project Proposal — Pdf To Revit

## Project Understanding
You need assistance with: 
**ACS Design Pipeline v2 — plan-first product delivery and release checklist**

*Project description:*
## Approved user direction
The engineer describes the project in chat, reviews/edits intelligent 2D plans until explicitly accepting a revision, then derives BIM/3D from that exact canonical baseline. Preserve warehouse behavior, mobile job recovery and saved versions. Do not silently modify approved geometry, discard requested rooms, invent metrics or claim regulatory approval.

## Implemented / verified candidate state
- [x] PR #20 merged to `main` as `4bde4cb95b935b978e5f87fc3c4a4ac74a1c0d2d` after CI #152 and Async delivery #9 passed. It records Design Pipeline v2 for residential and warehouse projects.
- [x] PR #19 merged to `main` as `64ed12676fced31c7835f4d34f0c98c24b33213b` after CI #158 and Async delivery #15 passed. Device-local saved versions and the residential plan-quality gate are in main.
- [x] Netlify production was rechecked on 2026-09-11 and is `ready` on `64ed12676fced31c7835f4d34f0c98c24b33213b`. Draft Plan-first PRs are not production-deployed. Render connector verification is currently blocked by its workspace-selection safety gate; do not infer a new Render deploy from Netlify state.
- [x] Draft PR #21 provides immutable plan revisions, explicit room locks, measured program/geometry review, conceptual approval, plan-only/chat adapters and schematic SVG/DXF export.
- [x] Warehouse-aware measured scorecard reports only canonical facts: zone areas, docks/by edge, rack groups/declared levels, stations and explicit lane rectangle areas. Capacity/throughput/travel/compliance stay unknown when not provable.
- [x] Measured A/B/C comparison for 2–8 alternatives uses only measured scorecard facts, keeps unknowns null and makes no automatic best-option/ranking claim.
- [x] Stable semantic locks cover site, rooms/zones and explicitly identified racks/docks/lanes/stations/openings/objects; locks survive reordering, bind placement context and reject tampering/wrong source.
- [x] Semantic locks are revision/approval/handoff-bound in the process-local foundation. `PlanLockWorkspace` versions lock-set changes, verifies inherited locks before revision admission, rebinds them to every admitted revision, includes lock hash/count in review/approval, and requires the exact approved lock receipt at handoff. Later lock edits cannot rewrite an old approved baseline. Warehouse rack/dock/staging and residential elevator cases are covered.
- [x] Offline approved-baseline-only 3D handoff under `tools/acs_plan_handoff.py`: drafts cannot invoke compiler; exact approved canonical model is compiled; compiler mutation fails closed; glTF and `.baseline.json` carry revision/model/source provenance and SHA-256 binding; zero provider calls.
- [x] Real compiler regressions cover residential geometry and warehouse rack/dock geometry, plus >900 kB glTF provenance.
- [x] PR #21 current head `d1b476b5933996349c30349128180a0d0145cfed` is now fully green on its recorded gates: Plan-first `34572141088` passed 116 tests, Async `34572141094` passed, and full CI `34572141091` / #188 completed **SUCCESS**. Keep #21 draft because product/UI/persistence/public integration slices remain incomplete; green CI is not a production claim.
- [x] Stacked draft PR #23 (`feature/plan-first-durable-store`) adds a filesystem-backed SQLite persistence contract for projects/revisions/bound semantic-lock receipts/conceptual approvals. It enforces owner/editor/viewer RBAC against a host-supplied already-authenticated `actor_id`, optimistic persisted-head writes, immutable revision/approval receipts, cross-project isolation and fail-closed tamper checks. Exact approved canonical baseline + lock receipt can be reloaded after process restart; later drafts do not replace it.
- [x] PR #23 dedicated Plan-first run `34576953916` / #70 passed on head `2f191fc22207f50d43b07cc2fa5e2375da6fc1e7`. The stacked foundation matrix is now **128 tests** = prior 116 + 12 durable-store/RBAC/persistence regressions.
- [x] PR #23 initially failed Deployment Closure because new canonical `acs_plan_store.py` was unclassified. The gate was not weakened: the dormant stdlib-only companion is now explicitly packaged in the backend image without adding a route or creating a DB. Replacement full CI `34576954023` has verified the formerly failing Deployment Closure step as **PASS**; remaining long jobs are still running.
- [ ] PR #23 full CI `34576954023` / #190 and Async delivery `34576954086` / #47 are still running on the current head. Do not merge #23; it is stacked on draft #21 and must not merge to `main` independently.

## Remaining product work — retain all agreed requirements
- [~] Authenticated project ownership/RBAC and durable storage for brief/program/constraints/revisions/bound semantic-lock receipts/approval receipts. PR #23 now provides the transactional filesystem persistence + RBAC contract, but **actual authentication/session binding, production datastore/volume, cross-device cloud persistence, durable generation jobs/results and backups are still missing**. A SQLite file on an ephemeral service filesystem is not a cloud-durability claim.
- [ ] Four-stage Arabic/mobile UI: requirements → intelligent 2D plan → 3D/BIM → presentation/export. Stop before 3D until engineer accepts the exact displayed revision.
- [ ] Structured brief extraction with source spans, requested/inferred/unknown labels, unit checks, clarification questions and engineer confirmation.
- [ ] Generate A/B/C alternatives under the same confirmed hard constraints and expose measured comparison. Any mixing creates a new proposal; never auto-rank or trigger unrequested paid generation.
- [ ] Expand semantic identities where needed for floor-instance-specific residential cores and warehouse zones/equipment/MEP/fire objects; keep template-vs-floor-instance targeting explicit.
- [ ] Full architectural 2D: shared walls, door/window geometry and identities, stairs/elevators/shafts, grids, dimensions, circulation/connectivity checks and transparent review scorecard.
- [ ] Warehouse planning/review beyond the current scorecard: receiving/shipping/staging/storage/circulation constraints, pedestrian/vehicle geometry, equipment/MEP/fire zones when supplied, and capacity only when canonical data makes calculation unambiguous.
- [~] Baseline-checked deterministic BIM/3D: offline exact-baseline compiler handoff/provenance is implemented and tested; public authenticated route, browser renderer handoff, persistent approval lookup in production and end-to-end production integration are still missing. No later LLM floor-plan redesign is allowed.
- [ ] Complete CAD export/import validation in a supported CAD tool, IFC/IDS validation, SVG/PDF sheets with revision identity; native DWG/Revit/Autodesk automation remains optional and separate.
- [ ] Office standards / Design DNA, named saved alternatives, approved baseline, restore, rename, compare, explicit delete and backups.
- [ ] Real end-to-end residential and warehouse examples from brief through option selection, edit/lock/approval/3D/save/reopen, plus physical mobile verification and measured before/after quality review.

## Current boundary
PR #21 remains a headless foundation, not a published plan-first UI. CAD is `SPACE_BOUNDARIES_ONLY`, not construction drawings or native DWG. Semantic lock binding is revision/approval/handoff-bound. PR #23 adds tested restart-safe filesystem persistence and RBAC policy boundaries, but does not authenticate identities or provide cloud/cross-device durability by itself. The approved-baseline 3D handoff is offline tooling, not a public Render route. OpenAI provider work remains separate in draft PR #16 and still needs its secure provider release gate / bounded real Responses smoke test before any production provider change.

Every continuation must read actual main/PR/CI/deploy state before writing; never overwrite concurrent work or merge failed gates.

## Scope of Work
- Asset analysis and workspace initialization.
- Core modeling / development based on specifications.
- Technical validation and quality checks.
- Incorporation of review feedback.
- Clean handover of source files and documentation.

## Required Files & Inputs
1. Complete reference files (drawings, access tokens, test data).
2. Exact dimensional specs or business rules.
3. Schedule/deadline expectations.

## Estimated Price and Timeline
- **Estimated Price:** 350 - 900 USD
- **Estimated Timeline:** 3 to 7 business days (to be refined after reviewing the final assets).

## Project Questions
To help me refine this estimate, please clarify:
1. How many levels/floors does the building have?
2. Are the PDF plans fully dimensioned, or do we have a graphic scale?
3. Do you require elevations and sections, or floor plans only?
4. Do you require a specific Revit version (e.g. Revit 2023, 2024)?
5. What is your target deadline for the initial draft?

## Agreement Terms
The final source files will be delivered upon approval of the milestones. Substantial revisions outside the agreed scope will require a change order.
