# Project Proposal — Unknown

## Project Understanding
You need assistance with: 
**P0 restore canonical Home Edge executor request signing and complete PR #2609 live self-heal canary**

*Project description:*
Classification: P0 / RUNTIME_INCIDENT / HOME_EDGE_EXECUTOR / REQUEST_SIGNATURE / PUBLIC_SAFE_CONTROL_ONLY

Context:
- PR #2609 was squash-merged into main at merge commit 6eaa17ef02d4e94ef18824793f6638f531ca6bdd.
- Operator explicitly approved subsequent runtime_sync_main and a live queue self-heal test.
- Canonical Home Edge execution immediately failed with `HomeEdgeExecError: request signature mismatch`.
- A follow-up minimal read-only canonical `hostname` attempt could not be invoked because the surfaced Home Edge tool binding became unavailable/stale after rediscovery.

```task
repo: alanua/Skeleton
base: main
risk: yellow
privacy_boundary: PUBLIC_SAFE_CONTROL_ONLY; NO_SECRETS
operator_approval: EXPLICIT_PR2609_RUNTIME_SYNC_AND_LIVE_SELF_HEAL_20260814

task:
  Restore the canonical Skeleton Home Edge execution path only. Diagnose the request-signature / connector binding failure fail-closed, without bypassing the canonical executor and without exposing secrets. Once the canonical Home Edge executor is healthy, perform the already-approved runtime_sync_main to current main and execute the bounded live queue self-heal canary for the PR #2609 behavior. Verify that an idle queue with eligible runner:ready work is replenished/continued automatically and record only public-safe aggregate evidence.

required_behavior:
  - no SSH/manual alternate executor/bypass route;
  - preserve canonical Home Edge request signing and approval semantics;
  - distinguish stale tool binding/discovery failure from actual signed-request verification failure;
  - do not weaken signature validation, authentication, authorization, approval refs, lane checks, node_id checks or fail-closed behavior;
  - do not log or expose signing keys, tokens, credentials, private paths/IPs or raw headers;
  - after transport recovery, sync Home Edge runtime to merge commit 6eaa17ef02d4e94ef18824793f6638f531ca6bdd or a verified descendant on main;
  - run a bounded live self-heal canary proving queue continuation from idle-with-eligible-work;
  - leave queue in normal autonomous operating state after the canary;
  - notify operator only if a true NEEDS_OPERATOR condition remains.

validation:
  - canonical read-only Home Edge `hostname` request succeeds through the same executor path;
  - runtime_sync_main succeeds and reports a main HEAD containing 6eaa17ef02d4e94ef18824793f6638f531ca6bdd;
  - live queue self-heal canary observes eligible work while idle, invokes the registered safe queue replenishment response, and reaches continued/running work without manual relabeling;
  - no signature/auth/authz checks are disabled;
  - no secrets/private runtime values appear in issue/PR/log receipts;
  - public-safe before/after receipt and stable reason classes are recorded.

expected_output:
  - one bounded repair PR only if code/config change is actually required; otherwise runtime-only recovery receipt;
  - exact public-safe root-cause class;
  - runtime sync evidence;
  - live self-heal canary result;
  - final queue state.
```


## Scope of Work
- Asset analysis and workspace initialization.
- Core modeling / development based on specifications.
- Technical validation and quality checks.
- Incorporation of review feedback.
- Clean handover of source files and documentation.

## Required Files & Inputs
1. Complete reference files (drawings, access tokens, test data).
2. Exact dimensional specs or business rules.
3. Schedule/deadline expectations.

## Estimated Price and Timeline
- **Estimated Price:** 300 - 800 USD
- **Estimated Timeline:** 3 to 7 business days (to be refined after reviewing the final assets).

## Project Questions
To help me refine this estimate, please clarify:
1. Could you describe the project scope and deliverables in detail?
2. Do you have any existing drawings or reference documents?
3. What is your target budget range?
4. What is your expected timeline?
5. Which tools/software do you prefer for this project?

## Agreement Terms
The final source files will be delivered upon approval of the milestones. Substantial revisions outside the agreed scope will require a change order.
