# Project Proposal — Structural Drawings

## Project Understanding
You need assistance with: 
**Build and test biuniqueness/allomorphy tooling against synth0001 before touching real data**

*Project description:*
## Purpose

Tracking issue for building and testing the new biuniqueness/allomorphy mechanism (schema design finalized across #249, #58, #252, #253) end-to-end on `synth0001` — deliberately *not* real data — before any of it touches `stan1293`. Two parts: harden the synthetic-language tooling first, then build/test the mechanism using it.

## Part 1 — Snapshot and harden the synth tooling ✅ DONE (2026-07-30)

- [x] **1a.** Regenerate `synth0001` from current `stan1293` (`make_synthetic_lang.py --clean --apply` then `--apply`) — picks up structural changes since March, including the July 28 pronoun-splitting change.
- [x] **1b.** Verify the four positions the design depends on (`v:frontedauxiliary`, `v:frontedauxiliaryinflection`, `v:verbstem`, `v:verbinflection`) survived this seed's drop; run `pytest`/`integrity-check`/`check-codebook` as the standard post-regeneration health check.
- [x] **1c.** Generalize `make_synthetic_lang.py`'s `build_plan()` to glob `lang_setup/*.tsv` generically rather than hardcoding filenames — so future new `lang_setup` file types (this one, and whatever comes after) don't require touching the script by name each time.
- [x] **1d.** Solve the position-survival gap for the new allomorphy tables: since `Position_Name` was deliberately dropped from them (inferred via lookup, not stored), the transform needs to look up each `Element`'s position(s) in the *old* structure and drop rows whose position didn't survive. Exercised against a minimal smoke-test fixture first, since `stan1293` has no real allomorphy data yet to test against.

**Additionally decided/built while doing Part 1** (not originally scoped, discovered along the way):
- Added `--full-copy` mode (identity position map, no structural drop) — used for this test case specifically, to avoid structural-drop noise while validating the mechanism itself. Random-drop mode remains the default for other multi-language test cases.
- `_transform_filled_tsv` crashed outright on pair-construction TSVs (`coreference`'s three `Position_A`/`Position_B` pair tabs, `nonpermutability/general.tsv`'s `Element_A`/`Element_B` rows) — pre-existing bug, unrelated to this design work, fixed as part of "belt and suspenders" hardening rather than deferred.
- Found and fixed a separate, unrelated gap: `coding.make_forms._read_diagnostics_for_language()` silently falls back to TSV-only when `diagnostics_{lang_id}.yaml` is missing — correct for legacy never-migrated languages, but it meant `synth0001` lost its YAML (source of truth) during regeneration with nothing anywhere catching it. Fixed at the root (generator now copies `.yaml` too) and added a permanent guard: `check-codebook` check 7 now hard-errors if any `diagnostics_{lang_id}.tsv` has no matching `.yaml`. `data_dependency_schema/facts.yaml`'s `diagnostics_scope` entry updated with this as a third drift-risk case.
- Found and fixed a real repo-hygiene bug: 9 `synth0001` files were tracked directly in the *outer* `planars` repo's git index despite `.gitignore` excluding `coded_data/` entirely (legacy from before the outer/nested split was fully enforced) — untracked them; `coded_data`'s own nested repo is now the sole source of truth for them.

Commits: outer repo `7992456` (untrack cleanup), `2f4a5e3` (classify_element, prerequisite work from earlier), `48bc2b4` (position-drop fix + `--full-copy`), `915530e` (no-YAML-no-truth fix). `coded_data`: `26884a0` (synth0001 regeneration), `d8bc6ec` (missing YAML). All pushed. 691 tests passing, `check-codebook`/`integrity-check` clean.

## Part 2 — Build and test the mechanism, entirely on `synth0001` (not started)

**Sequencing decision**: populate `synth0001`'s `allomorphs_synth0001.tsv`/`allomorph_forms_synth0001.tsv` directly with the already-designed English content (do/have/say suppletion, go/ablaut suppletion, *t/d* zero-marking) rather than writing it on `stan1293` first and porting it over — keeps `stan1293` untouched until the mechanism is validated.

- [x] **2a.** Finalize and add the Stage-0 `planar.yaml` structural column (flags filled items and open categories for biuniqueness relevance — name/values TBD) + validator support, mirroring how `Element_Types` was built. ✅ `Biuniqueness_Scope` (`filled` / `open_category` / `excluded`), built on top of `classify_element()`; see comment below.
- [x] **2b.** Mark the flags on `synth0001`'s `planar_synth0001.tsv`. ✅ Backfilled via `migrations/backfill_biuniqueness_scope_synth0001.py`; see comment below.
- [x] **2c.** Build Stage 1 Sheet generation (reads the flags, writes a real Drive Sheet — `synth0001` already has Drive presence from existing daily-refresh automation). ✅ Code built and dry-run verified against real synth0001 data; **live `--apply` not yet run** — see comment below for why and what to run.
- [ ] **2d.** Fill in Stage 1 (coordinator + Adam).
- [ ] **2e.** Build Stage 2 Sheet generation from Stage 1's filled output (expands category rows into specific members).
- [ ] **2f.** Fill in Stage 2.
- [ ] **2g.** Build the import step (Stage 2 Sheet → the two structural TSVs), matching `import-sheets`'s existing pattern.
- [ ] **2h.** Build validation for the two new tables, including the planar-cross-reference problem (enumerated vs. open-position `Element`s need different validation paths).
- [ ] **2i.** Open problem, not yet solved by anything above: how a language-level lexical fact (e.g. "go/went") connects to a span for a specific annotated example — every other planars module derives spans from a specific annotated instance; this mechanism is a standing fact about the lexicon, decoupled from any one example. Needs resolving before span computation can be built.
- [ ] **2j.** Adam exercises the full Stage 0→2 flow on `synth0001` and reports back.

## Related
- #249 — Expand planar structure representation (schema design; final `allomorphs_{lang_id}.tsv`/`allomorph_forms_{lang_id}.tsv` shape posted there)
- #58 — Biuniqueness type 2: non-automatic allomorphy
- #252 — Feature-content coding
- #253 — Murrinhpatha positional dependency (deferred extensibility target)

---
*klᴐd*





## Scope of Work
- Asset analysis and workspace initialization.
- Core modeling / development based on specifications.
- Technical validation and quality checks.
- Incorporation of review feedback.
- Clean handover of source files and documentation.

## Required Files & Inputs
1. Complete reference files (drawings, access tokens, test data).
2. Exact dimensional specs or business rules.
3. Schedule/deadline expectations.

## Estimated Price and Timeline
- **Estimated Price:** 800 - 2000 EUR
- **Estimated Timeline:** 3 to 7 business days (to be refined after reviewing the final assets).

## Project Questions
To help me refine this estimate, please clarify:
1. Avez-vous déjà réalisé l'étude de sol géotechnique pour les fondations ?
2. Quelles sont les charges d'exploitation particulières (machines, toiture végétalisée) ?
3. Fournissez-vous les plans d'architecte définitifs au format DWG ?
4. Quels sont les détails d'exécution attendus (nomenclatures d'acier, détails de ferraillage) ?
5. Quel est votre calendrier souhaité pour la validation des plans ?

## Agreement Terms
The final source files will be delivered upon approval of the milestones. Substantial revisions outside the agreed scope will require a change order.
