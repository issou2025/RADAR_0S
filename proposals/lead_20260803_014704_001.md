# Project Proposal — Bim Modeling

## Project Understanding
You need assistance with: 
**Rebuild Revit local adapter boundary without generation naming**

*Project description:*
The historical branch `agent/revit-local-adapter-v0` contains useful Revit boundary work but must not be merged as-is. It is behind current `main` and uses generation-named active artifacts.

Rebuild from current `main` under neutral responsibility-based names:

```text
docs/governance/REVIT_LOCAL_ADAPTER.md
```

Preserve only the useful boundaries:

- Pantheon governs scope, claims, ChangeCandidates, status and consequential review;
- Hermes or another admitted external runtime orchestrates bounded tasks when required;
- the Revit add-in owns Revit API execution and named transactions;
- Cockpit/OpenWebUI exposes preflight, warnings, proposed changes and results;
- the human decides consequential mutations;
- preflight pass, transaction success and runtime success do not constitute authorization or Evidence.

Reconcile the former warning levels, blocked-action lifecycle and transaction discipline with current Capability Slot, adapter, Evidence, Claim and ChangeCandidate doctrine. Do not add a new semantic layer where existing concepts already cover the need.

Do not retain `V0`, `v1`, `v2` or other generation labels in filenames, headings, schema identities, routes or active architecture names.

After the neutral reconstruction is merged, delete `agent/revit-local-adapter-v0`.

## Scope of Work
- Asset analysis and workspace initialization.
- Core modeling / development based on specifications.
- Technical validation and quality checks.
- Incorporation of review feedback.
- Clean handover of source files and documentation.

## Required Files & Inputs
1. Complete reference files (drawings, access tokens, test data).
2. Exact dimensional specs or business rules.
3. Schedule/deadline expectations.

## Estimated Price and Timeline
- **Estimated Price:** 300 - 800 USD
- **Estimated Timeline:** 3 to 7 business days (to be refined after reviewing the final assets).

## Project Questions
To help me refine this estimate, please clarify:
1. Could you describe the project scope and deliverables in detail?
2. Do you have any existing drawings or reference documents?
3. What is your target budget range?
4. What is your expected timeline?
5. Which tools/software do you prefer for this project?

## Agreement Terms
The final source files will be delivered upon approval of the milestones. Substantial revisions outside the agreed scope will require a change order.
