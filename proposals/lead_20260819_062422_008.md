# Project Proposal — Structural Drawings

## Project Understanding
You need assistance with: 
**Remaining work — consolidated survey (2026-08-18)**

*Project description:*
Consolidated survey of every open item in `docs/product/followups.md` (72 boxes) plus the 5 other open issues, as of 2026-08-18. Supersedes #664, which was closed on 2026-08-04 and is now stale.

`docs/product/followups.md` remains the source of truth — this issue is a navigable index over it, grouped by **what is actually blocking each item** rather than by the doc's chronological sections. Close boxes here only when the matching entry in the doc is ticked.

**Context:** PR #787 (8 rounds, 141 commits) closed ~30 follow-ups. Nothing below was closed by it.

**Scope (corrected 2026-08-18):** sections A–H come from `followups.md` + the open issues. Section **I** holds the 84 further real items found by a classified sweep of every other doc in the repo. See the scope-correction comment for what was excluded and why — ~210 boxes elsewhere are acceptance criteria or not-in-scope markers, and 83 are stale.

---

## A. Actionable now — code only, nothing external blocking

The default place to pull work from.

### Cross-platform mirrors owed (web shipped, mobile hasn't)
- [x] Mirror "train for this race" onto the phone (`race_plan_preset.ts`, decisions §606) — **closed by #791**
- [x] Mirror the runner's own load-ramp band onto the phone (`self_load.ts`, §609) — **closed by #791**
- [x] `comeback.ts` has no Dart twin — **port `self_load` + `plan_ramp` + `comeback` together**, they share `recentRunVolume` and a mutual-exclusion gate — **closed by #791**
- [ ] Mobile mirror: famous-segment catalogue UI (`/segments`, §593)
- [ ] Mobile mirror: add a club event to the phone's calendar (whole-series `.ics`, §599)
- [ ] Mobile has no per-challenge progress signal at all — when it grows one, mirror `myProgressView`'s unknown-vs-zero contract
- [ ] Nutrition targets are not a peer of the mobile Nutrition surface (#666 M1's structural half)

### Defects in shipped surfaces
- [x] The web backup writer has the same undisclosed per-track swallow mobile just fixed (§668) — its manifest emits neither `complete` nor `incomplete` — **closed by #791**
- [ ] The `/live/[id]` "Elapsed" stat freezes at the last ping while the page internally knows the true race clock (§601) — **needs a product call**: `elapsed` excludes auto-paused time
- [x] Web's Strava importer is lenient where mobile is now strict — a `Filename` naming a file the archive lacks imports silently as a summary (§664) — **closed by #791**
- [x] The mobile gym resume-card scan still tests the draft marker for mere presence, where every other rail now requires an object (§662) — **closed by #791**
- [ ] A run's segment rank and the segment's leaderboard rank are computed from different populations (needs a migration)
- [x] The segment catalogue is in no global nav slot and no sitemap — **closed by #791**
- [x] Retire the vestigial `onStartRun` route-carrying callback plumbing — **closed by #791**
- [x] iOS pace look-back spans a pause (`resume()` clears the wrong state) — **closed by #791**
- [x] iOS `HKWorkoutSession` failure is swallowed — `didFailWithError` is not surfaced — **closed by #791**
- [x] Should `backoff` and `dropset` sets also be excluded from progression? — **closed by #791**

### Documentation / registry drift
- [x] **The challenges feature has no rows in `parity.md` at all** — create, join, leave, goal progress, on-pace hint, both board types, completion badge, all shipping on 3 platforms, none in the matrix. Exactly the drift the matrix exists to make visible. — **closed by #791**
- [ ] `parity.md`'s whole iOS "Sync and backup" column reads `✗` while the iOS tree is byte-identical and carries no `Platform` gate; `apps/mobile_ios/CLAUDE.md` says the same. One of three sources is wrong — **verify on a device before flipping any of them.**
- [x] §653's stated `in`-filter failure mode ("empty result, not an error") does not reproduce locally — it is a **414** past ~200 ids. Gateway property, not code. Confirm which form prod produces. — **closed by #791**

### Hygiene
- [ ] `LocalSessionStore` has no production consumer — dead offline-cache scaffolding from session-planner P1. Delete it or wire it up (a product call, not a cleanup).
- [ ] The eight `replaceFromServer` overrides repeat their gate line by line — reach for a `replaceResident(build)` template if a ninth store lands
- [ ] `OfflineSyncStore.rewriteAll()`'s sibling `rewriteAll` residue: see §663
- [ ] The export builds the whole archive in memory (5000-run cap, 50k-row-per-section ceiling, both honestly reported) — durable fix is a chunked/resumable upload
- [ ] Web bundle weight
- [ ] Course-marker drag-to-move on mobile (needs device)

---

## B. Blocked on an operator step — code is merged

- [ ] share-run / share-route Lambda og:image PNG — verify the arm64 `@resvg` binary ships in the zip; `terraform apply` for the new CloudFront behaviours
- [ ] Live spectator hub → Fly.io — **cutover only.** The worker deployed 2026-07-21 as Fly app `threkir-worker` (region `ord`) and the `live.threkir.com` CNAME has landed; what remains is the `flyctl certs add` check and flipping `PUBLIC_LIVE_HUB_URL` / `LIVE_HUB_URL`. (`followups.md:15` overstates this — correct it there too.)
- [ ] Map matching deploy (OSRM alongside Supabase + extract-refresh pipeline)
- [ ] **Route builder needs a reachable OSRM — a design decision first, not just a deploy.** The shipped internal build has `OSRM_URL` unset, so the Build button throws on a phone. Do *not* simply expose the internal host.
- [ ] Graph-cycle loop generator (v3) — prod deploy only, gated on `GRAPH_CYCLE_URL`
- [ ] Protomaps self-hosted tiles (4 items)
- [ ] Auth email — localized templates (Send Email Hook); the six-locale templates are not live
- [ ] #703 — confirm the `consent_write_gate` migration is applied to prod (CISO / legal sign-off)

---

## C. Blocked on external credentials, accounts, or hardware

- [ ] Push notifications (FCM + APNs + web Push) — needs a Firebase project + APNs key + VAPID
- [ ] Native push delivery (FCM/APNs leg) — same blocker, sibling consumer pattern already proven by the web-push leg
- [ ] Garmin Connect — multi-day Developer Program application + OAuth provisioning
- [ ] RunSignUp race-results — needs a (free) API key
- [ ] Pro native RevenueCat purchase sheet + native donate — needs a RevenueCat project + `pro_monthly` package + platform keys
- [ ] Premium billing extensions (#12) — Stripe Checkout + portal + paywall enforcement
- [ ] iOS verification — Mac-only; `parity.md` cells stay ✗/Partial until a device run
- [ ] Apple Watch — Xcode / watchOS device required (route-nav, complications, ultra stress test, …)
- [ ] Background sync doesn't execute on an iOS device (`BGAppRefreshTask` vs `fetch` UIBackgroundMode)
- [ ] Health Connect import brings tracks — needs device + HC `ExerciseRoute` reading
- [ ] BLE chest-strap HR paired directly to the watch (~1–1.5 wk + device)
- [ ] Real body weight → calorie estimate on the watch (~2–4 d + device)
- [ ] Watch-face complication (~1–1.5 wk + device)
- [ ] Treadmill BLE FTMS — live run-screen wiring (parser + pairing tile already ship)
- [ ] #597 — custom_watch bench-verification backlog, blocked on the nRF52840 dev kit
- [ ] Validate the Strava-CSV-UTC + FIT-compressed-timestamp fixes against real files pre-prod
- [ ] Device-instrumented `integration_test` harness (new infrastructure)
- [ ] OSRM smoke test in CI (blocked on free-runner capacity)
- [ ] Positive-path Edge Function tests (needs real secrets in CI config)

---

## D. Product / policy decisions needed before any code

- [ ] Wear OS activity words drift from web and mobile — **owner call**: one difference (`hike` → "Trail run") is a product rename, not a translation fix, so it was deliberately not steamrollered
- [ ] `app_pt.arb` ships to nobody — decide whether pt-PT is a locale at all
- [ ] Auto-follow on club join — decided NOT a silent fan-out; revisit only with a real design
- [ ] Family / household Pro tier — pricing + entitlement model decision
- [ ] Strava community segment import — ToS redistribution limits + OAuth scope decision
- [ ] Segment-KOM "crowns" in the recap — deferred, needs a cross-user aggregation
- [ ] In-app "Send a route to a follower" (targeted DM send)
- [ ] Consent-flow consistency for health-data fields (legal-adjacent) — onboarding writes bare `date_of_birth` unconditionally while gating the rest on the Art 9 checkbox
- [ ] Heatmap / popular-route discovery — open decision: opt-in vs opt-out privacy default
- [ ] Data-export-ready email — **NOT planned**; the export is synchronous, so the mail would arrive stale. Revisit only if export goes async.

---

## E. Internationalisation

- [ ] Native review of machine-extracted translations (de/es/fr/ja/pt-BR) — fitness-domain tooltips (CTL/ATL/TSB/VO₂max) flagged specifically
- [ ] Supabase auth email templates (SRV-1) — GoTrue serves one template set per project, so this is platform-limited
- [ ] RTL on mobile — `EdgeInsetsDirectional` sweep, deferred until an RTL locale ships. (Web RTL is done.)
- [ ] Security emails (password-changed, new-device sign-in) — blocked on absent GoTrue auth hooks + no device/session table

---

## F. Feature-scale backlog (from `roadmap.md § Competitor-parity backlog`)

Rough sizes carried from the roadmap table; none of these is a defect.

- [ ] Trail / offline navigation (#5, ~3–4 wk) — needs a routing-engine choice (Valhalla vs GraphHopper)
- [ ] Audio-coached runs (#9, ~3–4 wk) — audio CDN + voice-talent budget
- [ ] Race calendar + results import (#10, ~2 wk) — gated on the RunSignUp key
- [ ] Advanced analytics polish (#11, ~2 wk) — race-time predictor already shipped
- [ ] Route-design preferences: scenic / elevation-aware / cul-de-sac half (~2–3 d, needs v3)

---

## G. CI reliability

- [ ] **The local Supabase edge runtime intermittently never boots**, failing a job in setup. Seen twice on 2026-08-14. Two things to look at: why the container never serves (the step's forensics don't capture its logs or exit state), and that the message says "within 90s" while the loop actually ran ~9 minutes — one of them is lying to whoever reads the failure. **Do not widen the timeout**: status `000000` is connection-refused, not a slow 200.
- [ ] Local Playwright is unusable when several agents share the checkout — `reuseExistingServer: !CI` serves another worktree's bundle, and a mid-suite death gives `ERR_CONNECTION_REFUSED` on unrelated specs. A `globalSetup` probe landed (§631); the build-matches-tree assertion did not.
- [ ] `node-version: 24` inputs track a major line rather than an exact patch (deliberate — a Node break is loud and local)
- [ ] `apple-actions/upload-testflight-build@v1` is tag-pinned, not SHA-pinned — the only such reference in the repo; sits in a commented-out block, SHA-pin it when that block goes live

---

## H. Other open issues

- [ ] #785 — feat(mobile): explain why "Allow all the time" matters for background run tracking
- [ ] #784 — fix(mobile): while-in-use location permission falsely treated as broken during run recording
- [ ] #746 — Dependency audit: vulnerabilities detected
- [ ] #703 — ops: confirm `consent_write_gate` applied to prod (also in section B)
- [ ] #597 — custom_watch bench-verification backlog (also in section C)

---

## Notes for whoever picks this up

- **Section A is the only section where work can start from a keyboard today.** B needs credentials or a `terraform apply`, C needs hardware or a third-party account, D needs a decision from the owner.
- Three items are **corrections to previously-recorded claims**, not new work: the §653 `in`-filter mechanism, the iOS backup parity column, and the missing challenges rows. Each was found by doing something else, which is the argument for keeping them visible rather than folding them into the entries they contradict.
- Several entries carry an explicit **"do not fix it this way"** clause (the OSRM host, the edge-runtime timeout, the sim budget, a mobile-only `fetchGymRoutineSessions` page). Read the full entry in `followups.md` before starting — the cheap fix is usually the one already ruled out.


---

## I. Real work found outside `followups.md` (added 2026-08-18)

A classified sweep of every other doc found **440 open boxes**, of which **84 are real work**. The rest: 210 are not work items (acceptance criteria, "not in scope" markers, a parts shopping list, bench-verification steps already covered by #597), 83 are stale, 63 duplicate a box above. The stale ones are being fixed in their own docs, not copied here.

### I.1 — Per-app `deployment.md` (54 items) — the largest single gap

Six files (`apps/{backend,job_worker,mobile_android,mobile_ios,watch_wear,web}/deployment.md`, 112 boxes) were missed entirely by the first pass. Almost all are operator steps or external accounts rather than code.

- [ ] **iOS — 12 items, all root-blocked on the $99/yr Apple Developer Program enrolment.** Nothing downstream (signing, TestFlight, App Store Connect) can start until it lands.
- [ ] **Wear OS — 11 items** (Play Console track, signing, review)
- [ ] **Android — 9 items** (Play developer account is *not* one of them — that is stale, see below)
- [ ] **job_worker / GraphHopper — 10 items**, including one genuine code item: **`release-graphhopper.yml` does not exist** (its siblings `release-worker.yml` and `release-osrm.yml` both do) **The workflow itself landed in #791**; the other nine job_worker/GraphHopper items are operator steps and stay open.
- [ ] **Web — 6 items**
- [ ] **Backend — 6 items**

### I.2 — `docs/custom_watch/roadmap.md` (20 items)

- [ ] 6 hardware-gated bench steps, all behind **one parts order** (see #597)
- [ ] 2 vendor emails
- [ ] 12 code items — 5 phone-side, 3 firmware, 2 sim scenarios, 2 other
- [ ] **The GPS `BufferedUarte` swap's stated blocker is gone** — it cites a missing Renode SHORTS timer model, which landed as decisions §421. Re-scope or do it.

### I.3 — Scattered (10 items)

- [ ] **`mv_weekly_mileage` has a pg_cron refresh but no reader at all.** Refreshed every 15 min (`20260706_001`), revoked from `anon`/`authenticated`, and `git grep` finds no client or RPC consumer. Pure cost today — wire a `SECURITY DEFINER` wrapper or drop it.
- [ ] **Runners-nearby: mobile UI unbuilt + the flag flip is a compliance gate.** `roadmap.md:203`. Web + schema + RPC + pgtap + Playwright all ship behind default-off `PUBLIC_ENABLE_NEARBY_RUNNERS` (§270). **Tracked in no `followups.md` box.** Two blockers: owner + CISO/counsel sign-off (person-location is Art 9-adjacent), and the mobile UI code.
- [ ] **watchOS has no live-position mini-map during a run.** `parity.md` holds the row at `Partial` for exactly this; Wear OS shipped `ui/RouteMiniMap.kt`. **Not in `followups.md:35`'s Apple Watch list**, which enumerates twelve other gaps but not this one.
- [ ] **Email production ops** — worker SMTP + `APP_BASE_URL`, confirm prod pg_cron, and SPF/DKIM/DMARC on `threkir.com`. `followups.md:27` covers only the *auth-email* hook leg; the worker transport and the DNS records are in no box. Nothing mails in prod until these land.
- [ ] **Instructor-business M0 (4 items)** — assign a named decider to five sign-off rows; provision the three Stripe Connect test keys; engage counsel on the retention term; loop in CISO + Security Analyst on the sub-processor / DSAR footprint. These gate the entire instructor money track (M1/M7/M8/M9/M10) and appear in no `followups.md` box.
- [ ] **Custom watch battery gauge reads the wrong SAADC input** — `main.rs` uses `saadc::VddInput` at gain 1/6 (3.6 V full scale), so a fresh 4.2 V cell saturates and the gauge honestly parks as *absent* from ~4.2 → 3.6 V. Move it to the `VDDH/5` input in high-voltage mode.

### I.4 — A category the first pass missed entirely

- [ ] **12 `- [~]` partial boxes** (built but not live) exist across the docs and are invisible to a `- [ ]` grep. At least one — runners-nearby, above — is tracked nowhere else.

### Docs to correct (not work items, but wrong)

- `docs/backend/backend_scaling.md` — **13 of its 14 "Fixes to apply now" boxes are stale.** Rate limiting, Strava webhook validation, OAuth-token encryption (done via Vault, better than the pgcrypto the box specifies), the `.env` template, routes FTS, PostGIS, `geom`, `training_plans`, `fitness_snapshots`, the RevenueCat webhook and the Go premium endpoints all shipped.
- `apps/web/deployment.md:7` still says "**Status: plan.** Today the web app exists as a working dev server only" — threkir.com has served from CloudFront since 2026-07-20.
- `docs/custom_watch/roadmap.md:152` says BLE sensor pairing is "Not built. The watch is a GATT *server* today" — `hr_strap.rs` implements the GATT **central** role (§365).
- `docs/backend/metadata.md:297` says web / watch_wear / watch_ios have no registry guard "yet" — all four guards exist; only a macOS CI run to compile the Swift one is outstanding.
- `docs/product/roadmap.md` — 4 parent bullets are unticked while **every one of their children is `[x]`** (lines 250, 263, 457, 461). Ticking debt under the repo's own "tick it the moment it merges" rule.
- `followups_archive.md` is a clean negative — 0 of its 47 boxes are real work found only there. Don't mine it.



## Scope of Work
- Asset analysis and workspace initialization.
- Core modeling / development based on specifications.
- Technical validation and quality checks.
- Incorporation of review feedback.
- Clean handover of source files and documentation.

## Required Files & Inputs
1. Complete reference files (drawings, access tokens, test data).
2. Exact dimensional specs or business rules.
3. Schedule/deadline expectations.

## Estimated Price and Timeline
- **Estimated Price:** 800 - 2000 EUR
- **Estimated Timeline:** 3 to 7 business days (to be refined after reviewing the final assets).

## Project Questions
To help me refine this estimate, please clarify:
1. Avez-vous déjà réalisé l'étude de sol géotechnique pour les fondations ?
2. Quelles sont les charges d'exploitation particulières (machines, toiture végétalisée) ?
3. Fournissez-vous les plans d'architecte définitifs au format DWG ?
4. Quels sont les détails d'exécution attendus (nomenclatures d'acier, détails de ferraillage) ?
5. Quel est votre calendrier souhaité pour la validation des plans ?

## Agreement Terms
The final source files will be delivered upon approval of the milestones. Substantial revisions outside the agreed scope will require a change order.
