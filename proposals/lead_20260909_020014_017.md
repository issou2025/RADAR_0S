# Project Proposal — Structural Drawings

## Project Understanding
You need assistance with: 
**Pick a portrait's expression, outfit and pose by naming attributes the art declares, instead of defining a filter per option**

*Project description:*
## Problem

A character portrait in Raffles and Bunny is one SVG per pose holding several expressions, outfits and eye directions as tagged layers, and the script picks a look with `[[bunny_realization~jacket~look_down]]`. The `~` names refer to `filter` structs, each a pair of include and exclude lists matched by regular expression against layer ids. That model has no notion of a group of alternatives, and every problem the authors hit follows from that:

- `scripts/portraits.sd` is 1,882 lines: 205 `filter` defines and 57 `filtered_image` composites. 143 of the filters are the `face_X` and `eyebrows_X` pairs, all of the shape include `X-Y`, exclude `X-(?!Y)`, one per option because the engine cannot be told that `face` is a group.
- Completion after `~` lists every filter in the project, because a filter is a free-standing struct with no link to any image. Only the tags present in that file are valid (#474 is the separate preview defect).
- A variant face with different eyebrows cannot be written as `[[bunny_help~eyebrows_angry]]`: `bunny_help` carries `eyebrows_help`, both eyebrow filters exclude the other, and the lists are concatenated, so no eyebrows show. Authors write the base expression, the face and the eyebrows out in full every time.
- Only SVG can be layered, because only SVG keeps layer ids after export. There is no path for a raster portrait beyond hand-listing files in a `layered_image`. The project's backgrounds are that case already: 49 `layered_image` defines in `scripts/backgrounds.sd` list `bg_x__base.webp` and `bg_x__prop_y.webp` files by hand, one define per combination of props (door open or closed, vase whole or broken).
- Artist mistakes are caught late or never. In the shipped art, `bunny_unimpressed.svg` has six ids the export tool renamed to `...default1` and `...down1`, so its resting pupils vanish; `raffles_confused.svg` has `filter-worried-mouth-open-default1`; Bunny's outer clothes are tagged `jacket` while Raffles's are `clothes-jacket`; `raffles_hmph` references an `eyebrows_hmph` filter that does not exist, which hides every eyebrow; `bunny_lecturing` asks for a `face_lecturing` that `bunny_serious.svg` does not contain, so its exclusion hides the whole face and the look renders faceless; six layers are tagged such that no filter can ever show them; eight folders mark two different options of one group as default. Nothing reads the layer vocabulary as a whole.
- The include lists are OR'd, so any matching include shows a layer: `[[bunny_assertive~phone_hold_right]]` shows the jacket sleeves on a jacketless Bunny because the hold filter's `arm-down-left` include matches the sleeve's id.

## Proposed solution

Replace filters with attributes over groups. A layer's name says when it shows; the script names attributes; the engine derives the vocabulary from the art. The full author-facing chapter is at the end of this ticket under Additional context and is the design of record; this section is the summary.

Layer names. Read as "show NAME when GROUP is OPTION":

| Layer name | Meaning |
| --- | --- |
| `body` | always visible |
| `hat.on` | visible when the `hat` switch is on; a layer can be named by its condition alone |
| `hair-top:hat.off` | `:` means when; a switch is off unless the script turns it on |
| `face.happy`, `face.neutral:default` | `.` means is; `face` is a group, `happy` an option; `:default` at the end marks a layer of the resting look, and every group it names rests on the option it names |
| `pupils:eyes.open:look.left` | two conditions; all must hold |
| `hand-right:arms.down.phone-left` | a second dot lists another option of the same group: down or phone-left |
| `arm:arms.phone` | an option with a hyphen is a kind of the word before it, so `arms.phone` also matches `phone-left` |

Rules: nesting inherits (a layer inside `face.happy` never repeats it); layers may share a name; a group with named options needs one `:default` per folder or nothing from it shows there until the script chooses; a switch is a group whose options are `on` and `off` and needs no default; `~` is accepted wherever `:` is and means the same; hyphens join words. Names are read from the `data-name` attribute, falling back to `id`; a name that starts with `_` is read up to the next `_` (the escape for a tool that mangles ids and keeps no original).

Script. Attributes follow the image name after `:` (or `~`), spelled exactly as in the art:

```
[[mia:happy:hat:look.left]]
[[bunny_help:eyebrows.angry]]
[[raffles_confident:coat:gloves:mask]]
```

A bare word that is a group name turns the switch on; a bare word that is an option sets it in every group that has it (so `happy` sets face and eyebrows together); `group.option` sets one group; the later of two options in one group wins, which is what makes `[[bunny_help:eyebrows.angry]]` work. A bare option that exists in two groups where setting both is unlikely (`down` in `look` and `head`) is a diagnostic asking for the qualified form. Completion after `:` lists only the options and switches the image carries.

Named looks keep the `filtered_image` type with `attributes` replacing `filters`, as quoted strings because a bare `look.left` in a define body is a table lookup:

```
define bunny_angry as filtered_image with
  image = bunny_realization
  attributes = { "angry" }
end
```

`[[bunny_angry:jacket]]` composes as today; `[[bunny_angry:eyebrows.stern]]` overrides one group.

Missing options. An option the file offers nowhere is ignored with a warning. An option a folder lacks (the help face has no `look.far-right`) shows nothing for that group in that folder, and a compile-time warning names the folder. Per-folder fallback to the default was tried against the real art and rendered 79 directives wrongly, because the artists' folders do not line up with groups (a hair layer would reappear under the hat).

Raster portraits. A folder named after the image, files named like layers with a stacking number first and `~` in place of `:` (a colon is not a legal file name character): `41_pupils~eyes.open~look.left.png`. Lowest number on top, matching Photoshop's export index. A folder cannot nest, so a file carries its parent's condition itself. An explicit `layered_image` define overrides the convention.

Validation, in the problems panel, on the image define or the directive: a name that does not parse or mixes `:` and `~`; two defaults for one group in one folder; an option appearing in exactly one layer across a character's files (probable typo); a directive option the image lacks anywhere; a directive option a folder lacks; a bare option ambiguous between groups; a directive that sets `look` while the face's eyes rest closed (the eight `bunny_panicking:eyes.open:look.down` cases in the migration).

Decided by default (my recommendation, not chosen deliberately): the option-prefix rule (`phone` matches `phone-left`) and `script`, `phone` and the phone-hold poses as one `arms` group with `down` as default; both fell out of converting the real art and the chapter documents them.

## Alternatives considered

- Keep `filter` and add conveniences (a `group` property, a generator for the `face_X` family). Rejected: keeps the concatenation semantics that break case 3 and the OR-of-includes that shows jacket sleeves without a jacket.
- Ren'Py `layeredimage`: `group`/`attribute`/`default`/`when` and `auto` groups from file names. Its semantics are the reference for group exclusivity, defaults and later-wins, but it re-declares every layer in the script, which is the maintenance burden this ticket removes. Naninovel's `Head/Emotions>Cool` expression is the reference for selecting within a group without a manifest.
- Infer groups from tags (a word followed by two or more values). Rejected on the real art: `hair` precedes both part names and variant names, so inference would make `hair.short` hide the bangs. A declared group list (`define portrait as config with groups = {...}`) was the next proposal and became unnecessary once the dot in the layer name marks a group.
- Cross-layer rules as script-side declarations (`define hat as attribute with hides = { hair_top }`) or a `without-` keyword in the layer name. Superseded: with switches, the hidden layer simply says `hair-top:hat.off`.
- `default:` as a prefix, `.default` as a suffix on the option, or defaults declared in the script or the file name. Rejected: defaults vary per file and per folder (each face rests on its own gaze), so only the layer name has the granularity; the trailing `:default` reads best in a stack.
- Wrapping every name in underscores so an appended digit falls outside the tag. Adopted, then dropped, once reading `data-name` (Illustrator), `serif:id` (Affinity) and `inkscape:label` (Inkscape) gives the typed name verbatim; kept as the escape for tools that provide nothing else.
- `|` or `_` for "or". Rejected: `|` is illegal in file names and does not survive an id; `_` collides with the escape wrapper. The dot list survives everywhere.
- `~` on all three surfaces for consistency, or `:` on all. Neither is possible (`~` does not survive an id-only export, `:` is illegal in file names), so both are accepted and mean the same.
- Default from the layer's visibility at export time, as Naninovel does. Rejected: depends on the export tool writing hidden layers, and an artist who forgets to reset the file changes the default silently.
- Per-folder fallback when a folder lacks the requested option. Rejected on measurement, see above.
- A character-level index that resolves `[[bunny:help]]` to whichever file holds the help face. Deferred: a face that appears in two files (`worried` in `bunny_flustered` and `bunny_gun`) needs a rule of its own.

## Scope

In: the layer-name grammar and its reader, for any layered image, backgrounds with prop variants as much as portraits; per-image vocabulary; attribute resolution; SVG filtering by attribute set through the existing service-worker route; the directive syntax and implicit attribute images; `filtered_image.attributes`; completion, hover and diagnostics in both editors; the raster folder source; the `data-name` normalizer for Affinity and Inkscape exports; the migration of Raffles and Bunny's art and script with an equivalence test; the guide chapter. `filter` as a define type, `filtered_image.filters`, the regex matcher and the `~`-as-filter-reference grammar are removed once the migration lands.

Out: a character-level portrait index (`[[bunny:help]]` across files); any restructuring of the Raffles and Bunny art beyond renaming; verifying Photoshop, Krita and Clip Studio Paint export naming (a small task inside the raster slice); the asset inspector.

Reused: `filterSVG` and the service-worker on-demand route (`packages/sparkdown/src/filters/filteredSvg.ts`, `impower-dev/src/workers/sw.ts`, `packages/spark-web-player/src/app/assets/AssetCache.ts`) keep their shape with the attribute set replacing the filter param; `resolveImageLayers` and `UIModule.getImageSrcsByName` keep their call sites; the `layered_image` type is the raster override. The converter prototype that produced the migration preview (Python, 650 lines) is attached to the migration slice.

Existing tickets this closes or subsumes: #338 (`populateFilteredImages` never called), #339 (`keyIsArrayIndex`), #340 (`filtered_src` memo), #341 (untyped fan-out composites twice), all in code this replaces. #474 (completion preview) stays separate.

## Implementation plan

Split into five vertical slices, each a Task issue; this ticket is the parent. #480 (slice 1), #481 (slice 2, blocked by #480), #482 (slice 3, blocked by #481), #483 (slice 4, blocked by #481), #484 (slice 5, blocked by #481 and #482).

1. Reader and resolver (`packages/sparkdown`). Parse a layer name (`data-name`, then `id`, then the `_..._` escape) into label, conditions, default; build a per-image vocabulary (groups, options, switches, per-folder defaults) once per file, computed by the workspace on import or change and cached by file signature the way thumbnails are, so neither the language server nor the compiler needs the SVG text; resolve a directive's attribute list to a selection (bare, qualified, later-wins, ambiguity); decide visibility per layer (all conditions hold, same-group list is any-of, option prefix, nesting, folder defaults, switches off); `filterSVG` by selection; the diagnostics listed above. Tests in `packages/sparkdown/src/tests/compiler/` on the parser, the resolver and `filterSVG`, including the pupils, hat, arms and gloves shapes from the migration.
2. Script surface (`definitions/yaml/sparkdown.language-grammar.yaml` and both regenerated JSON copies; `packages/sparkdown` compiler; `packages/sparkdown-language-server`; `packages/spark-engine`; `packages/spark-web-player`; `impower-dev`; `vscode-sparkdown`). `AssetCommandFilter` accepts `:` or `~`; the implicit `filtered_image` carries attributes; `filtered_image.attributes` replaces `filters` in `builtins.sd` and the engine types; the service-worker param carries the canonical selection; completion after `:` lists the image's vocabulary; hover previews resolve through the new path; the `filter` type and regex matcher are removed. Tests: compiler tests on implicit images and named looks; a language-server completion test; a screenshot from the running editor of a directive with two attributes rendering, and of the completion list.
3. Raster folder source (`packages/sparkdown` compiler, `impower-dev` workspace). A folder of numbered files is an image whose layers are the files, names read after the number with `~` as the separator; `layered_image` overrides. Verify what Photoshop, Krita and Clip Studio Paint write and adjust the reader's tolerance. Tests: compiler tests on a fixture folder; a screenshot of a raster portrait switching outfits.
4. Migration (`packages/sparkdown` CLI or a script under `scripts/`). Convert Raffles and Bunny: SVG names (write `data-name` on every filterable group, derived from what the current filters do to each layer), `portraits.sd` to named looks, `main.sd` directives; the `serif:id`/`inkscape:label` normalizer; an equivalence test that renders every directive in `main.sd` before and after and asserts identity except for the 27 documented differences. The Affinity source files are re-imported from the converted SVGs by the artists; whether the color-palette bindings can be preserved through Affinity's automation is a question for that slice.
5. Documentation. The chapter below becomes `packages/sparkdown/docs/guide/Portraits.md`, and the Structure chapter links to it.

## Acceptance criteria

- [ ] Compiler tests prove: a layer name parses into label, conditions and default for every form in the chapter's table; `[[mia:look.left]]` shows the left pupils and the whites without naming the eyes; `[[mia:eyes.closed]]` hides every layer conditioned on `eyes.open`; `[[bunny_help:eyebrows.angry]]` shows the help face with angry eyebrows; `hand-right:arms.down.phone-left` shows for either option; `arms.phone` matches `phone-left`; a folder with no default for a named group shows nothing from it; two defaults in a folder is a diagnostic.
- [ ] A directive naming an option the image lacks is a diagnostic on the directive; one a folder lacks is a warning naming the folder.
- [ ] Completion after `:` in `[[bunny_realization:` lists exactly the options and switches in `bunny_realization.svg` and nothing from any other file, shown in a screenshot from the running editor.
- [ ] The equivalence test in the migration slice passes: every one of the 441 distinct directives in `main.sd` renders the same visible layer set before and after conversion, except the 27 listed there, each of which is a fix.
- [ ] `scripts/portraits.sd` after migration contains no `filter` defines; the 57 named looks are one line of attributes each.
- [ ] A raster portrait folder renders and switches outfits in the running editor, shown in a screenshot.
- [ ] The guide chapter is in the repo and the review found nothing left unanswered.

## Additional context

Prior tickets: #299 (on-demand filtered SVG route), #302 (filtered layers accumulate), #338, #339, #340, #341, #432, #474.

Prior art: Ren'Py `layeredimage` (https://www.renpy.org/doc/html/layeredimage.html); Naninovel layered characters (https://naninovel.com/guide/characters); Inkscape SVG versus plain SVG (https://wiki.inkscape.org/wiki/Inkscape_SVG_vs._plain_SVG). The Affinity export normalization was measured on this project's files: of the printable punctuation, only `-`, `_`, `.` and `:` survive in an id; a duplicate name gets a digit appended; the typed name is kept in `serif:id`.

Migration preview, produced during design and attached to the migration slice: every portrait's converted layer stack, every directive old and new, the 27 differences and the art notes (six dead layers, seven export renames, eight folders with conflicting defaults).

The design of record, the guide chapter as agreed:

<details>
<summary>Character portraits (guide chapter draft)</summary>

# Character portraits

> Draft. This chapter describes a portrait system that is being designed, not one that ships yet. The naming rules are the proposal; a few define names may change before it is built.

A character in a visual novel needs many looks: happy, sad, eyes closed, wearing a coat, holding a phone. Drawing every combination as its own picture does not scale. Three outfits, four expressions and two eye directions are already twenty-four pictures, and adding one hat doubles that.

Sparkdown takes a different approach. You draw the character once as a stack of layers, name each layer so that it says when it should be visible, and the script turns layers on and off by name. One file, any combination.

This chapter shows how to name layers and how to ask for a look from the script. The examples use a character called Mia, and the same rules serve a background whose props change, which the chapter comes to near the end.

## The idea

Every layer has a name. A plain name like `body` or `hair-back` means the layer always shows.

To make a layer conditional, add a colon and the condition. The colon means "when", and inside the condition a dot means "is":

```
hair-top:hat.off        show hair-top when hat is off
hair-under-brim:hat.on  show hair-under-brim when hat is on
```

When the script asks for `[[mia:hat]]`, Sparkdown goes through the layers, keeps every layer whose condition is met, hides every layer whose condition is not met, and shows the result. That is the whole system. The rest of this chapter is the naming rules and what the script can ask for.

## A first portrait: a hat, and the hair around it

Mia sometimes wears a hat. A hat changes the hair, so the hair is drawn in three pieces: the hair at the back of the head, which shows either way; the hair pressed flat under the brim, which only makes sense with the hat; and the fluffy top of the hair, which the hat would cover. In the drawing program the layers are named like this, top layer first:

```
hat.on
hair-under-brim:hat.on
hair-top:hat.off
hair-back
head
body
```

Reading them one at a time:

- `body`, `head` and `hair-back` are plain names, so they always show.
- `hat.on` is the hat itself: show it when `hat` is `on`. A layer can be named by its condition alone, with nothing before a colon.
- `hair-under-brim:hat.on` is the flattened hair: show it when `hat` is `on`.
- `hair-top:hat.off` is the fluffy top: show it when `hat` is `off`.

A switch is off unless the script turns it on, so nothing has to say which state is the resting one.

In the script:

```
[[mia]]
```

shows Mia bare-headed. Nothing mentioned the hat, so it is off. The hair top shows; the hat and the flattened hair do not.

```
[[mia:hat]]
```

turns the hat on. The hat and the flattened hair show, and the hair top hides. Writing a switch's name after `:` turns it on.

This is how any "this part hides when that part shows" relationship works. There is no separate rule to write anywhere else; each layer says when it belongs.

## A choice between several options

Mia has three expressions. Only one can show at a time, so they form a group. A group is a word, the options are the words after the dot:

```
face.sad
face.happy
face.neutral:default
head
body
```

`face` is the group; `sad`, `happy` and `neutral` are its options. `:default` at the end of `face.neutral:default` marks the option that shows when the script does not choose one; read it as "face is neutral, by default". This is the one place the word is needed: a switch rests at `off` on its own, but among three named faces nothing says which one is the resting face unless you mark it.

A switch like `hat` is just a group whose two options are `on` and `off`, with `off` as its built-in default.

```
[[mia]]          neutral face
[[mia:happy]]    happy face
[[mia:sad]]      sad face
```

You do not have to say which group `happy` belongs to. Sparkdown looks through the file, finds `happy` in the `face` group, and chooses it there. If you prefer to be explicit, `[[mia:face.happy]]` means the same thing: the condition is spelled in the script exactly as it is spelled in the layer name. `[[mia:hat]]` is short for `[[mia:hat.on]]`.

## Combining

Conditions from different groups combine freely:

```
[[mia:happy:hat]]
[[mia:sad:hat]]
```

The order after `:` does not matter, except in one case: if you name two options from the same group, the later one wins.

```
[[mia:happy:sad]]    sad face
```

That rule matters when you give a combination a name of its own and then adjust it, which the section on named looks covers.

## Layers inside layers

Expressions are usually more than one layer: skin, mouth, eyes. Put them inside the expression's layer, as a group or folder in the drawing program:

```
face.happy
    mouth
    eyes
    skin
face.neutral:default
    mouth
    eyes
    skin
head
body
```

A layer inside `face.happy` shows only while that layer shows. The inner layers do not have to repeat the condition, so they can have plain names. Two layers may share a name, as `mouth` does here.

## Eyes: two conditions on one layer

Inside each face, Mia's eyes can be open or closed, and when open they can look in different directions. Here is the neutral face in full:

```
face.neutral:default
    lids:eyes.closed
    pupils:eyes.open:look.left
    pupils:eyes.open:look.right
    pupils:eyes.open:look.camera:default
    whites:eyes.open:default
    mouth
    skin
```

Two new things are happening.

`pupils:eyes.open:look.left` has two conditions, each after its own colon. Read the colons as "when ... and when ...": show these pupils when eyes is open and look is left. Both have to hold.

`:default` at the end of a name marks the layer as part of the resting look, the one you get when the script chooses nothing. Every group named on that layer rests on the option the layer names. `whites:eyes.open:default` says eyes rest open. `pupils:eyes.open:look.camera:default` says eyes rest open and the look rests at the camera. Mark every layer that belongs to the resting look, the way you would leave them visible in the drawing program.

What the script gets:

```
[[mia]]              eyes open, looking at the camera
[[mia:look.left]]    eyes open, looking left
[[mia:eyes.closed]]  lids only; the whites and every pupils layer hide, because they all need eyes open
```

Notice that `[[mia:look.left]]` did not have to mention the eyes. The `eyes` group fell back to its default, open, on its own.

Each face carries its own eyes, so each face can have its own default look. If the happy face's pupils are marked `pupils:eyes.open:look.up:default`, then `[[mia:happy]]` looks up and `[[mia]]` looks at the camera. The happy face's eye layers can have exactly the same names as the neutral face's; nothing needs a distinguishing word.

## The naming rules

A layer name is the layer's own name, then optionally a colon and one or more conditions, and optionally `:default` at the end. When the condition alone is enough to describe the layer, the name before the colon can be left out. A tilde is accepted wherever a colon is, `hair-top~hat.off`, and means the same thing; the colon is the one the editor inserts.

| You write | It means |
| --- | --- |
| `body` | Always visible. A plain name. |
| `hat.on` | Show this layer when the `hat` switch is `on`. A layer can be named by its condition alone. |
| `hair-top:hat.off` | Show `hair-top` when `hat` is `off`. A switch is off unless the script turns it on. |
| `face.happy` | Show this layer when `face` is `happy`. `face` is a group; `happy` is one of its options. |
| `face.neutral:default` | The same, and `neutral` is the face that shows when the script chooses none. Only a group with named options needs this. |
| `pupils:eyes.open:look.left` | Show `pupils` when `eyes` is `open` and `look` is `left`. Every condition must hold. |

Rules to keep in mind:

- Use hyphens inside words: `hair-under-brim`, `pupils:look.far-right`. Do not use spaces or other punctuation; drawing programs replace them with hyphens on export, which is fine in a name and wrong in a condition.
- A name cannot contain a dot, because a dot always means "is".
- End every layer that belongs to the resting look with `:default`. Within one folder, the resting layers of a group must all name the same option; if they name two, the editor reports it. If a group of named options has no resting layer in a folder, nothing from it shows there until the script chooses. A switch, a group whose options are `on` and `off`, needs no marker: it is off until turned on.
- A layer may list several options of one group after the dot, `hand-right:arms.down.phone-left`, and then shows for any of them: the right hand is down when the arms are down and also when the phone is in the left hand. Read the extra dots as "or". Conditions from different groups still all have to hold.
- An option with a hyphen in it is a kind of the word before the hyphen: `arms.phone` on a layer also matches `phone-left` and `phone-right`, so one layer can serve every way of holding the phone, while `arms.phone-left` matches only that one.
- A layer inside a conditional layer inherits the condition and does not need to repeat it.
- Layers may share a name. Give every mouth `mouth` and every set of open eyes `whites:eyes.open:default` if that is what they are.
- Layer names are checked when the file is loaded into the project. A name that does not follow the rules is reported in the editor's problems panel with the layer name, so a slip never silently changes what a layer means.

## Group, option, or switch?

Deciding which is which comes down to one test: what question does the layer answer about the character, and how many answers can be true at once?

- If the question has exactly one answer at a time, it is a group, and the answers are its options. "Which face?" is a group called `face`. "What are the arms doing?" is a group called `arms` with options `down`, `phone`, `phone-left`, `script`. "Which outfit?" is `clothes`. Name the group after the part or the slot, and the option after the answer. Choosing one option hides the others, which is exactly what you want for things that cannot both be true.
- If the question is yes or no, it is a switch: a group whose only options are `on` and `off`. "Is the hat on?" is `hat`. Gloves, mask, bandage, helmet are switches too. A switch is right for anything that can be added without changing the rest.
- If one thing can only be true while another is, it is a condition, not a new group. The flattened hair is not a kind of hair, it is hair that shows when the hat is on: `hair-under-brim:hat.on`.

The three punctuation marks follow from that:

- A dot joins a question to its answer: `arms.phone-left`. A second dot lists another answer that also shows the layer: `arms.down.phone-left`.
- A colon means "when": it separates the layer's name from its conditions, and one condition from the next. `hand-right:arms.down:clothes.coat` shows the hand when the arms are down and the outfit is the coat.
- A hyphen joins words inside one name or one option: `hair-under-brim`, `far-right`, `phone-left`. It also makes an option a kind of a broader one, so `phone-left` counts as `phone`.

So for a phone held in the left hand, the question is "what are the arms doing?", and the answer is "holding the phone, in the left hand": `arms.phone-left`. Not `hold-left.phone`, because `hold-left` is not a question about the character, and not `hold.phone-left`, because holding is not a part; the arms are.

## Drawing programs

Every drawing program rewrites layer names on export to make them legal ids: characters it cannot use become hyphens or hex codes, and a duplicate name gets a digit appended. Sparkdown does not read the id. It reads a `data-name` attribute, which holds the name exactly as you typed it, and falls back to the id only when there is no `data-name`.

- Illustrator writes `data-name` itself when the id had to differ from the layer name. Nothing to do.
- Affinity Designer keeps the typed name in a `serif:id` attribute, and Inkscape keeps it in `inkscape:label`. When you add such a file to a project, Sparkdown copies that attribute into `data-name` for you. Inkscape users should save as "Inkscape SVG", not "Plain SVG", because the plain format strips the labels.
- For a program not listed here, whose export gives Sparkdown nothing but a mangled id, wrap each name in underscores in the drawing program, `_face.happy_`: Sparkdown then reads only what is between the first underscore and the next one, so an appended digit or suffix cannot reach the tag.

## Writing it in the script

After the portrait's name, add a colon and an option for each thing you want to set. The conditions are spelled exactly as in the layer names, colon and all:

```
[[mia:happy:hat:look.left]]
```

What you can write after `:`:

| You write | It does |
| --- | --- |
| `:happy` | Chooses `happy` in every group that has it. If only `face` has it, that is the face. If `face` and `eyebrows` both have a `happy`, both are set, which is usually what you want for an expression. |
| `:face.happy` | Chooses `happy` in the `face` group only. |
| `:hat` | Turns the `hat` switch on. |
| `:hat.off` | Turns it off, if a named look had turned it on. |
| `:eyes.closed` | Chooses `closed` in the `eyes` group. |

If a bare word is an option in two groups where choosing both would be a mistake, for example `down` in both `look` and `head`, the editor asks you to write `look.down` or `head.down` instead.

The editor's autocomplete, after you type `:`, lists only the options this portrait actually has. A portrait with no `hat` layer does not offer `hat`.

## Named looks

When you use the same combination often, give it a name:

```
define mia_party as filtered_image with
  image = mia
  attributes = { "happy", "hat", "look.left" }
end
```

The attributes are written in quotes because a bare `look.left` would be read as a table lookup inside a define. Then `[[mia_party]]` is the same as `[[mia:happy:hat:look.left]]`, and you can still adjust it: `[[mia_party:sad]]` keeps the hat and the look and changes the face, because the later choice in the `face` group wins.

## Backgrounds and props

Nothing in this chapter is only about characters. A background whose props change over the story is the same kind of image: a stack of layers, some of which show only in some states. A door that opens, a vase that breaks, a light that goes out, a note that appears on a table, are each a group with its options, drawn once in the same file as the room.

```
door.open
door.closed:default
vase.broken
vase.whole:default
lamp.off
lamp.on:default
room
```

```
[[office]]                          closed door, whole vase, lamp on
[[office:door.open]]                the door opens
[[office:door.open:vase.broken]]    and the vase breaks
[[office:lamp.off]]                 the lamp goes out
```

Write the group with the option here, `door.open`, rather than a bare `open`, because a room often has several things that can be open or broken, and a bare word would set all of them.

The same room as image files follows the rules in the next section: `10_room.webp`, `20_door.closed~default.webp`, `20_door.open.webp`, `30_vase.whole~default.webp`, `30_vase.broken.webp`. A project that already keeps a base image and one file per prop, named on the pattern `bg_office__prop_door_open.webp`, is one rename away from this.

## Portraits made of image files

Everything above works the same when the layers are separate image files (PNG, WebP) instead of layers in one SVG. Put the files in a folder named after the portrait and name each file the way you would name a layer, with two differences:

- Start each file name with a number and an underscore. The number is the stacking order, lowest at the bottom. Files with the same number can be in any order, since only one of them shows at a time.
- Use `~` in place of the colon, because a colon is not allowed in file names. A project that keeps its portraits as image files can use `~` in its script and in any SVG layer names too, so that every surface reads the same.

```
assets/mia/
  10_body.png
  15_hair-back.png
  20_head.png
  30_face.neutral~default.png
  30_face.happy.png
  40_whites~eyes.open~default.png
  41_pupils-camera~eyes.open~look.camera~default.png
  41_pupils-left~eyes.open~look.left.png
  41_lids~eyes.closed.png
  50_hair-top~hat.off.png
  50_hair-under-brim~hat.on.png
  60_hat.on.png
```

A folder has no "layers inside layers", so a file cannot inherit a condition from a parent. If the happy face needs its own eyes, name those files with the face condition as well: `41_happy-pupils-camera~face.happy~eyes.open~look.camera~default.png`. If one set of eyes fits every face, leave the face out and the same eye files serve all of them.

`[[mia:happy:hat:look.left]]` then means exactly what it means for the SVG version.

## When something looks wrong

The editor checks the portrait files and the script and reports these in the problems panel:

- A layer name that does not follow the rules, or a name that mixes `:` and `~`.
- Two different options marked `default` in the same group in the same folder. Only one can be the default.
- An option that appears in only one layer across all of a character's portraits. It is usually a typo of an option or group name used everywhere else, such as `eybrows.angry`.
- A script line that asks for an option the portrait does not have anywhere, such as `[[mia:gloves]]` when no layer in Mia's file mentions `gloves`. The option is ignored and the rest of the line still works.
- A script line that asks for an option one part of the portrait lacks, such as `[[mia:happy:look.left]]` when the happy face has no left-looking pupils. That part shows nothing for the group, the same as if the layer were missing, and the warning names the folder so the artist can add it.
- A bare option that exists in two groups where setting both is unlikely to be intended.

If a portrait shows nothing for a part you expected, the usual reason is a group of named options with no `default` marked in that folder: nothing from that group shows until the script picks one.

## Quick reference

Layer name, read as "show NAME when GROUP is OPTION":

```
name
name:group.option
name:group.option:default
name:group.option:group.option
name:group.option.option
```

Script:

```
[[name]]
[[name:option:option]]
[[name:group.option]]
[[name:switch]]
[[name:switch.off]]
```

File name for an image-file portrait:

```
NN_name~group.option~group.option.png
```


</details>


## Scope of Work
- Asset analysis and workspace initialization.
- Core modeling / development based on specifications.
- Technical validation and quality checks.
- Incorporation of review feedback.
- Clean handover of source files and documentation.

## Required Files & Inputs
1. Complete reference files (drawings, access tokens, test data).
2. Exact dimensional specs or business rules.
3. Schedule/deadline expectations.

## Estimated Price and Timeline
- **Estimated Price:** 800 - 2000 EUR
- **Estimated Timeline:** 3 to 7 business days (to be refined after reviewing the final assets).

## Project Questions
To help me refine this estimate, please clarify:
1. Avez-vous déjà réalisé l'étude de sol géotechnique pour les fondations ?
2. Quelles sont les charges d'exploitation particulières (machines, toiture végétalisée) ?
3. Fournissez-vous les plans d'architecte définitifs au format DWG ?
4. Quels sont les détails d'exécution attendus (nomenclatures d'acier, détails de ferraillage) ?
5. Quel est votre calendrier souhaité pour la validation des plans ?

## Agreement Terms
The final source files will be delivered upon approval of the milestones. Substantial revisions outside the agreed scope will require a change order.
