# Project Proposal — Permit Drawings

## Project Understanding
You need assistance with: 
**P0: Bootstrap autonomous contour into one activation PR**

*Project description:*
Mode: RUNNER_TASK
Parent Program: #2210
Sources: #2242 #2244
Source PRs: #2248 #2247
Priority: P0 / FIRST
Risk: yellow
Privacy Boundary: PUBLIC_SAFE_CODE_AND_SYNTHETIC_TESTS_ONLY
Expected Output: one clean integration draft PR from current main; no merge/deploy/runtime/device side effect in coding task

```task
schema: skeleton.runner_task.v1
repo: alanua/Skeleton
base: main
task_kind: code_generation
payload:
  operation: bootstrap_autonomous_contour_integration
  task: >-
    Build one clean integration slice from current main that combines the reviewed intent of PR #2248 (Scheduler -> Shared Dispatch -> Runner/Loop autonomous continuation, priority pickup, read-only DONE classification) and PR #2247 (internal review gate and exception-only operator escalation) without blindly stacking conflicting patches. Re-read both exact source PR heads before implementation: #2248 head add2eab488653de896a108aaf935d631ff8f9058 and #2247 head 40ed9a17493ece6a56935a7e4b8a35eddce24794. Preserve fail-closed authority boundaries: Scheduler cannot grant capabilities, Loop cannot bypass Runner, merge/deploy/protected runtime remains policy-gated, and only true NEEDS_OPERATOR reaches operator. The resulting integration must remove the bootstrap idle state: after a normal task/result, the system must either autonomously continue through approved review/repair/next-step paths or durably enter NEEDS_OPERATOR with an actionable exact reason; it must never silently stop at a draft PR or DONE issue awaiting chat '+'.
requested_capabilities:
  - repository_read
  - repository_write
  - test_execution
allowed_files:
  - core/shared_dispatch.py
  - core/scheduler_engine.py
  - core/scheduler_store.py
  - core/scheduler_models.py
  - core/loop_controller.py
  - core/loop_runner_adapter.py
  - core/loop_state_store.py
  - core/review_gate.py
  - core/notification_policy.py
  - scripts/scheduler_runtime.py
  - scripts/runner_poll_github_tasks.py
  - scripts/telegram_callback_poller.py
  - schemas/scheduler_dispatch_receipt.schema.json
  - tests/test_shared_dispatch.py
  - tests/test_scheduler_engine.py
  - tests/test_scheduler_store.py
  - tests/test_scheduler_runtime.py
  - tests/test_loop_controller.py
  - tests/test_loop_runner_adapter.py
  - tests/test_review_gate.py
  - tests/test_notification_policy.py
  - tests/test_runner_poll_github_tasks.py
  - tests/test_telegram_callback_poller.py
  - docs/SCHEDULER_AUTONOMOUS_CONTINUATION.md
  - docs/AUTONOMOUS_REVIEW_GATE.md
required_behavior:
  - one shared autonomous continuation/control path only; no second control plane and no per-project handlers;
  - runner ready queue honors queue:RUN_NOW / runner:priority-1 before ordinary ready work, deterministic within class, no preemption of an already-running task, no starvation after priority drains;
  - verified zero-file read-only DONE finalizes as DONE without requiring a PR;
  - file-changing DONE with required publish artifact still fails closed if PR is missing;
  - draft PR/result is immediately routed into internal review gate, not operator relay;
  - safe internal APPROVE continues according to existing merge policy; REQUEST_CHANGES creates/reuses one bounded repair task; DO_NOT_MERGE drives repair/supersede/dependency; only new human authority/protected merge/security-secret/physical-action boundary becomes NEEDS_OPERATOR;
  - if existing merge policy requires exact operator merge approval, create a durable NEEDS_OPERATOR state/receipt containing repository, PR number, exact head SHA, permitted merge method/policy reason and next continuation step; do not perform the merge in this code task;
  - routine DONE/BLOCKED/RETRY/WAITING/REVIEW states produce no Telegram notification; exactly one true NEEDS_OPERATOR notification survives restart/dedupe;
  - Scheduler occurrences are atomically claimed; retry/dependency/restart recovery is idempotent; deterministic workflow next step re-enters gated dispatch;
  - after a protected merge is later authorized and completed, canonical continuation is runtime_sync_main pinned to merged main SHA, then the registered live-safe canary, then resume previously blocked approved work such as Home Edge PR #2241 / fresh reconcile without chat nudges;
  - no automatic canon/governance/security-policy broadening.
required_tests:
  - source intents from #2247 and #2248 both represented without duplicate control paths;
  - normal code task -> draft PR -> internal APPROVE -> policy continuation without chat '+';
  - protected merge-required PR -> exactly one durable NEEDS_OPERATOR with exact PR/head/policy fields, no silent idle and no routine Telegram;
  - REQUEST_CHANGES -> one idempotent repair -> re-review;
  - zero-file read-only DONE -> runner:done with no PR;
  - P0 ready task outranks normal ready task, then normal resumes after P0 drains;
  - synthetic two-step scheduler chain advances automatically and handles one retry + dependency resume;
  - restart does not duplicate dispatch, repair task or NEEDS_OPERATOR notification;
  - full existing pytest remains green;
  - git diff --check passes.
forbidden_actions:
  - no live GitHub merge, deploy, runtime sync, service restart, Home Edge/device/provider action in coding task;
  - no secrets/private values;
  - no direct SSH or alternate executor;
  - no files outside allowlist;
  - do not modify source PR branches #2247/#2248 in place.
validation_commands:
  - [python3, -m, pytest, -q, tests/test_shared_dispatch.py, tests/test_scheduler_engine.py, tests/test_scheduler_store.py, tests/test_scheduler_runtime.py, tests/test_review_gate.py, tests/test_notification_policy.py, tests/test_runner_poll_github_tasks.py]
  - [python3, -m, pytest, -q]
  - [git, diff, --check]
validation_timeout_seconds: 1800
expected_output:
  - one clean draft PR to main
  - exact head SHA and changed-file list
  - focused/full tests PASS
  - synthetic autonomous continuation/review canary PASS
  - explicit next-state classification: CONTINUE_AUTOMATICALLY or NEEDS_OPERATOR with exact protected-action packet
  - no live production/device side effects
privacy_boundary: PUBLIC_SAFE_CODE_AND_SYNTHETIC_TESTS_ONLY
idempotency_key: autonomous-contour-bootstrap-integration-20260808-v1
```

Completion rule
This task exists to eliminate silent idle. A completed coding result must be followed by internal review/repair/continuation or an explicit durable true NEEDS_OPERATOR state. It must never require a bare '+' continuation token.

## Scope of Work
- Asset analysis and workspace initialization.
- Core modeling / development based on specifications.
- Technical validation and quality checks.
- Incorporation of review feedback.
- Clean handover of source files and documentation.

## Required Files & Inputs
1. Complete reference files (drawings, access tokens, test data).
2. Exact dimensional specs or business rules.
3. Schedule/deadline expectations.

## Estimated Price and Timeline
- **Estimated Price:** 300 - 800 USD
- **Estimated Timeline:** 3 to 7 business days (to be refined after reviewing the final assets).

## Project Questions
To help me refine this estimate, please clarify:
1. Could you describe the project scope and deliverables in detail?
2. Do you have any existing drawings or reference documents?
3. What is your target budget range?
4. What is your expected timeline?
5. Which tools/software do you prefer for this project?

## Agreement Terms
The final source files will be delivered upon approval of the milestones. Substantial revisions outside the agreed scope will require a change order.
