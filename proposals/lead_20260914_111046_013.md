# Project Proposal — Unknown

## Project Understanding
You need assistance with: 
**[LIFE-INTAKE] Persist adversarial audit runs as mandatory repair inputs**

*Project description:*
# Zero-authority Life intake

Intake authority: `ZERO`

Runtime control authority: `NONE`

Input class: `VINCE_DIRECTIVE`

Problem ID: `1db9934857f3f8fa6422cf6db40a6293da52fd5f1880fb43079d2c6c10b8c471`

Problem identity inputs:
- problem_class: `VINCE_DIRECTIVE`
- mechanism_id: `verified_defect_repair`
- detector_or_invariant_id: `ADVERSARIAL_AUDIT_FINDINGS_DURABLE_AND_SELECTABLE`

Vince directive, September 14, 2026:

> “Yeah, can you do that in parallel with the other two? Can you make that a durable thing without interfering with their work?”

Conversation context: this follows the adversarial determinism/conformance audit where individual findings were durably routed to owning PR/Issue surfaces but the complete audit matrix remained primarily thread-local. Vince wants the audit result itself to become durable and future workers to consume verified findings without depending on noticing prose comments.

## Canonical relationship

Disposition: `COMBINE`.

Canonical repair/lifecycle owner: Issue #49 — **Automatically convert verified defects into mandatory repair work**.

Related durable-lesson owner: Issue #55 — **Make reusable Life lessons durable and automatically actionable**.

Related consolidation mechanism: Issue #61 — **Prevent duplicate/conflicting Issues and support deterministic combine/dependency classification**.

This intake MUST NOT create a second defect lifecycle, second selector, second Semantic Firewall, or parallel runtime control plane. Audit evidence feeds the existing verified-defect → repair architecture by exact references.

## Required durable audit model

A future versioned implementation must make each adversarial audit run a first-class, machine-readable evidence record bound to exact audited identities rather than leaving the matrix only in chat. At minimum an audit run must bind an exact `audit_run_id`, audited repository/main SHA, audited PR/work-branch identity when applicable, audited head SHA, audit scope/profile version, and the complete required case matrix.

Each matrix row must use one closed audit result state: `REPRODUCED`, `NOT_REPRODUCED`, `NOT_APPLICABLE`, or `NOT_RUN`, with exact evidence references and the exact detector/invariant or control condition tested. A missing row may not be silently treated as pass.

Each reproduced finding must have a stable finding identity, exact audited snapshot, canonical owner/component reference, affected invariant/control class, reproduction evidence, and required falsification/retest reference. The record itself is `EVIDENCE_ONLY` and has zero authorization, routing, priority, completion, release, merge, mutation, or effect-execution authority.

## Promotion into mandatory repair

An auditor/model/review conclusion cannot directly create `VERIFIED_DEFECT`. A reproduced audit row derived only from model review, prose, PR/Issue/comment/review text, or semantic judgment remains `DEFECT_CANDIDATE`.

Promotion to `VERIFIED_DEFECT` must use Issue #49's exact machine-verifiable evidence classes or their later versioned deterministic replacement. Once `VERIFIED_DEFECT` is established, it must resolve to exactly one canonical nonterminal repair identity and use the existing repair/work-selection machinery; duplicate audit findings must converge on the same repair identity rather than spawn competing repairs.

A changed audited head/SHA must not implicitly erase an unresolved finding or imply it is fixed. The finding requires a fresh retest against the new exact snapshot. Terminal repair remains governed by the existing root-cause/verification requirements, including rerunning the original detector/invariant and protected integration/readback when applicable.

For the final from-scratch combined-main adversarial audit after the active fencing and Semantic Firewall mechanisms are integrated, prior findings must be reclassified using the exact states already required by that audit: `VERIFIED_FIXED`, `STILL_FAILING`, `SUPERSEDED`, `NOT_APPLICABLE`, or `NEW_REGRESSION`.

## Worker discovery and selector integration

The permanent mechanism must eliminate dependence on a worker noticing GitHub comments. Bootstrap/dispatch must deterministically discover unresolved audit findings through a versioned machine-readable source. Work selection may consume only findings that have reached the exact verified-defect/repair state required by the existing architecture; audit prose has zero selection authority.

Until the existing problem-intake/repair → selector bridge is versioned, tested, protected-integrated, and read back, this intake remains durable evidence only and must not be represented as selector-active.

## Parallelism and non-interference

This requirement is intentionally designed to proceed independently of Semantic Firewall predicate-level determinism and goal/work fencing where exact resources are disjoint.

At intake creation time, `component:issue_57` is owned by the active Semantic Firewall work item. The goal-lifecycle component is also actively claimed for terminal cleanup after PR #89 integration. This intake does not acquire either claim and does not mutate either work branch.

Future source implementation may proceed in parallel only after deterministic work selection/admission and successful acquisition of its own exact component claim. Any operation whose required resource set intersects an active unexpired claim remains blocked for that operation; non-intersecting operations may proceed under the current coordination protocol.

## Acceptance conditions

1. A versioned audit-run schema/registry or exact later replacement stores the complete audit matrix and exact audited snapshot identities.
2. Validation fails closed when required matrix rows, snapshot identities, evidence refs, finding identities, or lifecycle bindings are missing/invalid.
3. Audit prose/model judgment cannot directly promote a finding to `VERIFIED_DEFECT` or create repair-selection authority.
4. A deterministically verified audit defect creates/updates exactly one canonical repair identity through Issue #49's architecture.
5. An audited SHA change requires explicit retest/reclassification; it cannot silently mark an unresolved finding fixed.
6. Fresh workers can discover unresolved verified audit defects without reading prior chat and without depending on noticing PR/Issue comments.
7. Disjoint repairs can dispatch in parallel; intersecting resource operations fail closed under current claims.
8. Completion requires original detector/invariant rerun, required validation/CI, protected integration, and authoritative readback where applicable.
9. The mechanism is registered with the Semantic Firewall conformance system without creating duplicate canonical authority.
10. Falsification tests prove omitted findings, stale snapshots, duplicate finding identities, model-only promotion, and comment-only handoff cannot create a repair/completion PASS.

## Current execution state

Persistence status: `DURABLE_INTAKE_RECORDED` once GitHub readback verifies this issue.

Source implementation: `NOT_RUN` by this intake operation. Current `governance/work-admissions.json` does not admit Issue #49 or this intake as parallel work, and current source states the problem-intake/selector binding is not active. This issue therefore creates no source-work authorization by itself.

No Supabase mutation. No Vercel mutation. No paid-plan change. No Project Instruction edit. No active implementation-branch mutation.

## Scope of Work
- Asset analysis and workspace initialization.
- Core modeling / development based on specifications.
- Technical validation and quality checks.
- Incorporation of review feedback.
- Clean handover of source files and documentation.

## Required Files & Inputs
1. Complete reference files (drawings, access tokens, test data).
2. Exact dimensional specs or business rules.
3. Schedule/deadline expectations.

## Estimated Price and Timeline
- **Estimated Price:** 300 - 800 USD
- **Estimated Timeline:** 3 to 7 business days (to be refined after reviewing the final assets).

## Project Questions
To help me refine this estimate, please clarify:
1. Could you describe the project scope and deliverables in detail?
2. Do you have any existing drawings or reference documents?
3. What is your target budget range?
4. What is your expected timeline?
5. Which tools/software do you prefer for this project?

## Agreement Terms
The final source files will be delivered upon approval of the milestones. Substantial revisions outside the agreed scope will require a change order.
