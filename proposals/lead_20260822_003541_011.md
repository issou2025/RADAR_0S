# Project Proposal — Flutter App

## Project Understanding
You need assistance with: 
**[Enhancement] The Synchronisatie screen's own chrome and the Home placeholder are still English**

*Project description:*
## Summary

#257 translated the full-panel states, the navigation rail, the sign-in gate and
the last-sync box. The rail destination `Reconcile` became **Synchronisatie**
and the page heading followed it, but the screen's *own* controls under that
heading did not, so the tab now reads Dutch and the buttons on it read English.

The Acties read-only banner already labels the very same action **Synchroniseer**
(`action_tiles.dart`), and since #257 the last-sync box says `synchronisatie` /
`driftcontrole` — so the two buttons are the odd ones out on their own screen.

## What is still English

`account_manager/lib/src/screens/reconcile_screen.dart`:

- the two buttons — `Synchronise` and `Check for drift`;
- the help paragraph under them (`Synchronise pulls WISA and diffs it against
  the previous snapshot; …`);
- the counts section heading `Overview`;
- the smart-diff notice `WISA is unchanged since the previous sync — no account
  changes needed.`;
- the log panel — `Log`, `Copy all`, `Clear`, `No messages yet.` and the
  `Copy line` context-menu label.

`account_manager/lib/src/screens/home_screen.dart` (the Start tab):

- the eyebrow `Arcadia · account synchronisation` and the whole body paragraph
  (`Reconciles user accounts and class groups across WISA, …`). It is a Phase C
  placeholder, but it is the first screen the operator lands on.

## Notes

- No `ValueKey` changes: `reconcile-sync`, `reconcile-drift`,
  `reconcile-log-copy-all`, `reconcile-log-clear`, … are identifiers the tests
  use.
- The developer-facing log *lines* the controller writes are #258, not this.
- Test assertions on these strings live in
  `account_manager/test/reconcile/reconcile_screen_test.dart` and
  `account_manager/integration_test/app_launch_test.dart`; they move with the
  strings rather than being left asserting the English ones.

## Why it was not done in #257

#257 is scoped to the full-panel states, the rail and the gate — the states that
stand in *for* a screen. These strings are the Synchronisatie screen's own
content, and the Home placeholder is a different screen again.

## Affected subsystem

- [x] UI (Flutter / WPF)

## Scope of Work
- Asset analysis and workspace initialization.
- Core modeling / development based on specifications.
- Technical validation and quality checks.
- Incorporation of review feedback.
- Clean handover of source files and documentation.

## Required Files & Inputs
1. Complete reference files (drawings, access tokens, test data).
2. Exact dimensional specs or business rules.
3. Schedule/deadline expectations.

## Estimated Price and Timeline
- **Estimated Price:** 1500 - 4000 USD
- **Estimated Timeline:** 3 to 7 business days (to be refined after reviewing the final assets).

## Project Questions
To help me refine this estimate, please clarify:
1. Do you have UI designs ready (Figma, Adobe XD) or should we design the screens?
2. Which backend service will the app connect to (REST API, Firebase)?
3. Do you require integration with app stores (App Store, Play Store)?
4. What state management solution do you prefer (Bloc, Riverpod, Provider)?
5. What is your target launch date?

## Agreement Terms
The final source files will be delivered upon approval of the milestones. Substantial revisions outside the agreed scope will require a change order.
