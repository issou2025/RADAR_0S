# Project Proposal — Structural Drawings

## Project Understanding
You need assistance with: 
**prove Pantheon professional value on first IFJA architecture vertical**

*Project description:*
## Objective

After the current governance/documentation convergence has stabilized, prove Pantheon on one real architecture-agency task before adding further ontology, Roles, Rites, governed Spaces or broad product abstractions.

The first vertical is deliberately narrow:

```text
CCTP + DPGF/DQE + contractor quote
-> governed ACT / pre-contract review
-> source-linked findings
-> human professional decision
```

The goal is not to demonstrate that an LLM can summarize or compare documents. The goal is to demonstrate that the existing Pantheon architecture produces a review that is materially safer, more traceable and more useful to an architect than an ordinary assistant workflow.

## Why this issue exists

Pantheon already contains substantial doctrine and implementation around:

- Case / Affaire and Situation intake;
- document identity, versions and source authority;
- architecture document review;
- CCTP / quote comparison;
- Evidence candidates and human decision gates;
- Hermes external execution;
- Pantheon governance / policy / bounded persistence;
- Cockpit governed projection;
- document structural-analysis qualification;
- real-environment Project Anatomy qualification.

The current risk is no longer primarily missing conceptual coverage. The risk is that Pantheon architecture and doctrine advance faster than demonstrated professional value.

This issue therefore changes the validation question from:

> Is the architecture internally coherent?

to:

> On a representative architecture task, does Pantheon actually prevent mistakes, preserve the right source/version/authority distinctions, reduce reconstruction effort, and produce a review an architect would choose to use?

## Sequencing

Record this issue now so the product-validation objective is not lost.

Do not use it to interrupt or broaden the current documentation convergence.

Current sequencing posture:

1. #785 is complete.
2. Continue #787 until current-authority documentation ownership is sufficiently converged and stable for implementation work to reuse clear owners rather than encode transient duplication.
3. Revalidate `main`, open PRs/issues, current architecture-domain owners and implementation seams before activation of this issue.
4. Reuse #662 for the representative IFJA document corpus and parser/structural-analysis qualification where applicable; do not create a second document-ingestion benchmark.
5. Reuse #644 / #607 real-environment paths when the vertical reaches environment qualification; do not create a second environment-proof framework.

#662 and #644 are supporting qualifications, not excuses to invent parallel owners or wait for unrelated future work when a bounded synthetic/anonymized vertical can already be exercised safely.

## Governing product hypothesis

```text
model capability is replaceable
professional harness is the durable value
```

For the métier, Pantheon should primarily provide the system around the model:

```text
correct Case / Situation
-> correct source identity
-> correct document versions
-> applicable authority/effect
-> bounded retrieval
-> professional checks
-> explicit missing/contradictory evidence
-> candidate interpretation
-> human decision
-> governed transmission/persistence
```

Pantheon must not become valuable merely because it has more internal concepts than a generic assistant.

## Existing owners to reuse first

Re-read exact current owners at activation time. Expected existing responsibilities include at least:

- architecture domain pack / Case-Affaire model;
- `DOSSIER_SITUATION_INTAKE` or its converged owner;
- `DOCUMENT_REVIEW` or its converged architecture-review owner;
- document/source/version authority owners;
- `INDEX_EFFECT_MATRIX` or its converged responsibility;
- Task Contract / Execution Admission owners;
- Evidence / Registre Probatoire boundaries;
- Hermes integration / runtime-return owners;
- existing document conversion / structured compilation / retrieval seams under `implementation/`;
- Cockpit governed projection.

Before adding anything new, answer:

> Which existing owner cannot represent the demonstrated need, and why?

No new owner is justified merely because this vertical needs orchestration between existing responsibilities.

## First professional vertical

### User-level request

Representative request:

> Analyse le nouveau devis du lot couverture par rapport au DCE applicable et dis-moi ce qui nécessite mon attention.

The user should not need to manually select Pantheon Roles, Rites or internal governance machinery.

The everyday UX should remain métier-first:

```text
Affaire
phase
lot
source documents
review findings
questions / decisions requiring attention
```

Internal governance remains visible when needed for trust, source status, approval or risk, but should not become routine operator burden.

## Representative corpus

Use a small immutable corpus derived from real IFJA failure modes, anonymized or synthetic when required.

Prefer reuse/extensions of the representative corpus already needed by #662.

Include at minimum:

```text
one CCTP version
one DPGF/DQE or equivalent price schedule
one contractor quote
one intentionally superseded or non-applicable version
```

Add bounded supporting material only when it proves a real review need, for example one relevant plan or one amendment/index.

The corpus should deliberately contain known cases such as:

- one required item missing from the quote;
- one explicit exclusion;
- one quantity mismatch;
- one unrequested or ambiguous variant;
- one scope/interface ambiguity;
- one version trap;
- one apparent anomaly that is actually acceptable, to measure false positives;
- one finding whose correct result is `insufficient evidence`, not a fabricated conclusion.

Ground truth for the qualification must be manually reviewable and recorded separately from model output.

## Required professional distinctions

The vertical must preserve at least:

```text
retrieved != truth
source document != extracted derivative
latest file != current authority
highest index != contractual version
transmitted != approved
quote line != accepted scope
comparison candidate != conformity verdict
missing match != proven omission from contractual scope
runtime success != Evidence
runtime success != professional validation
memory != Evidence
projection != persistence
prepared transmission != sent transmission
human review != automatic approval
```

No result may collapse these distinctions merely to make the UX appear decisive.

## Expected finding shape

Reuse existing contracts if they already represent this. Do not create a new schema only to match this example.

Conceptually each material finding needs enough information to answer:

```text
what is observed?
which exact source/version supports it?
which counterpart source/version was compared?
what is missing or contradictory?
what professional consequence may follow?
what remains interpretation rather than fact?
what action/question is recommended?
what status / approval is required?
```

Example classes may include existing architecture-review classes such as:

```text
missing_item
contradiction
ambiguity
variant_detected
exclusion_detected
scope_shift
quantity_mismatch
contractor_question_needed
client_arbitration_needed
```

Do not create synonymous finding taxonomies unless the current owner demonstrably cannot express the case.

## Qualification dimensions

Do not use a single abstract AI score.

Measure at least:

### 1. Coverage

- known material findings detected;
- known findings missed.

### 2. Noise

- false-positive findings;
- duplicate findings;
- findings that are technically true but professionally useless.

### 3. Provenance

- every material claim resolves to the correct source document;
- exact version/index is visible;
- page/clause/structural locator is preserved where available;
- derived extraction never silently replaces the source.

### 4. Version and authority safety

- superseded/non-applicable documents do not silently control the answer;
- the system exposes uncertainty when authority/currentness is unresolved;
- a newer filename/index does not automatically become contractual authority.

### 5. Governance safety

- findings remain candidates until professionally reviewed;
- no automatic compliant/non-compliant verdict from document matching alone;
- no contractor selection/rejection;
- no automatic contract, OS, payment or transmission effect;
- missing evidence produces a bounded refusal/uncertainty state rather than invented certainty.

### 6. Professional usefulness

Human review should answer:

- Would an architect actually use this review?
- Does it reduce time spent reconstructing context?
- Does it expose useful issues that may otherwise be missed?
- Are proposed contractor/client questions directly usable after review?
- Does the system make the important uncertainty easier to see rather than merely producing more text?

### 7. Effort

Record at least qualitatively, and quantitatively where practical:

- manual setup required;
- manual source/version correction required;
- number of review interventions before usable output;
- avoidable repeated clarification;
- rework caused by false positives or bad source selection.

The product test is not merely `workflow completed`.

The stronger question is:

> Would the agency prefer to run this review through Pantheon again on the next comparable case?

## Harness ratchet / failure-to-constraint loop

Every meaningful failure observed during the vertical must be classified before changing prompts or adding architecture.

Use the strongest justified correction layer:

```text
one-off/local context problem
-> task/context correction

recurring judgement/procedure problem
-> existing method/guide owner

objective recurring check
-> deterministic sensor / test / validator / schema where appropriate

action that must be impossible without authority
-> existing policy / permission / contract boundary

source information lost or distorted upstream
-> parser / adapter / normalization owner

already structurally enforced rule
-> remove redundant narrative restatement where safe
```

Compact principle:

```text
one recurring failure -> strongest justified enforceable constraint
```

The ratchet is an engineering/product discipline, not by default a new Role, Rite, governed Space, workflow engine or ontology layer.

## Failure log during qualification

For each significant failure, record at least:

```text
case/ref
observed failure
professional consequence
failure class
current owner responsible
strongest justified fix layer
fix applied or deferred
original failure reproduced before fix
regression check after fix
redundant prose removable after structural enforcement, if any
```

This may live in the issue/PR/test evidence appropriate to the work. Do not create a new current-authority Markdown owner merely to host the log.

## Cockpit acceptance posture

The Cockpit should project professional attention, not internal ontology.

A successful first vertical should be able to present something conceptually like:

```text
Affaire X — lot couverture

3 points require review
- one CCTP item has no matching priced quote item
- one contractor exclusion conflicts with the selected review baseline
- one quantity differs from the DPGF

Baseline used: DCE / CCTP index <X> / DPGF index <Y>
Status: requires human review

Actions:
- inspect sources
- prepare contractor questions
- mark finding reviewed/rejected/accepted according to existing governed statuses
```

The user should not need to invoke `ARGOS`, `THEMIS`, a Rite, PDP/PEP terminology or Task Contract fields manually for ordinary use.

Those mechanisms may explain why a result is gated, but the primary UX vocabulary should remain professional.

## Explicit non-goals

This issue must not create by default:

- a new Pantheon Role / god;
- a new Rite;
- a new governed Space / place;
- a new `Harness` architecture layer;
- a second document parser/ingestion/retrieval pipeline;
- a second Case/Affaire owner;
- a second Evidence or Registre owner;
- a second runtime/orchestrator;
- an ACT engine;
- an automatic conformity engine;
- a generic architecture expert agent;
- a large new finding ontology;
- new current-authority `.md` files merely to describe the experiment;
- automatic external transmission;
- automatic contractor selection, approval, payment, contractual instruction or professional conclusion.

## Follow-up verticals — explicitly out of scope for first closure

If the first vertical proves value, later product-validation issues may reuse the same method for:

```text
incoming document -> impact on Case/Affaire
DET observation/reserve -> evidence-backed closure continuity
```

Do not implement these in this issue simply because they are foreseeable.

## Done criteria

This issue closes only when:

- [ ] current repository owners and seams are revalidated before implementation;
- [ ] one representative immutable architecture corpus is defined without creating a parallel #662 corpus responsibility;
- [ ] known expected findings and expected non-findings are manually recorded;
- [ ] the first CCTP/DPGF/quote vertical runs through existing source/version/retrieval/governance paths;
- [ ] material findings carry resolvable source/version provenance;
- [ ] a deliberate superseded/version trap is handled safely;
- [ ] a deliberate insufficient-evidence case does not produce fabricated certainty;
- [ ] coverage and false positives are reviewed against the known corpus;
- [ ] at least one significant observed failure is converted through the harness ratchet into the strongest justified existing layer and regression-protected;
- [ ] no new god, Rite, governed Space or parallel runtime/document authority is introduced without a separately demonstrated need;
- [ ] Cockpit/user-facing output is métier-first and does not require routine manipulation of Pantheon internal mythology;
- [ ] a human architecture review records whether the output is actually preferable to the ordinary manual/generic-assistant workflow for this case;
- [ ] remaining gaps are classified by existing owner rather than hidden by extra prompting;
- [ ] relevant CI/architecture/governance checks remain green for any repository changes.

## Decision after the vertical

At closure, explicitly classify the result:

```text
professional value demonstrated
professional value partially demonstrated / bounded gaps
professional value not yet demonstrated
```

Then decide which existing responsibility deserves reinforcement.

Do not infer from a successful demo that every architecture workflow should be automated.

## Current context when recorded

Recorded after completion of #785 while #787 documentation-owner convergence is active.

At recording time, `main` is `ce8321ce074f991d81f2463b1370b48218abf6bf` (merge of #823).

This context is provenance only. Revalidate repository state before activating the issue.

## Scope of Work
- Asset analysis and workspace initialization.
- Core modeling / development based on specifications.
- Technical validation and quality checks.
- Incorporation of review feedback.
- Clean handover of source files and documentation.

## Required Files & Inputs
1. Complete reference files (drawings, access tokens, test data).
2. Exact dimensional specs or business rules.
3. Schedule/deadline expectations.

## Estimated Price and Timeline
- **Estimated Price:** 800 - 2000 EUR
- **Estimated Timeline:** 3 to 7 business days (to be refined after reviewing the final assets).

## Project Questions
To help me refine this estimate, please clarify:
1. Avez-vous déjà réalisé l'étude de sol géotechnique pour les fondations ?
2. Quelles sont les charges d'exploitation particulières (machines, toiture végétalisée) ?
3. Fournissez-vous les plans d'architecte définitifs au format DWG ?
4. Quels sont les détails d'exécution attendus (nomenclatures d'acier, détails de ferraillage) ?
5. Quel est votre calendrier souhaité pour la validation des plans ?

## Agreement Terms
The final source files will be delivered upon approval of the milestones. Substantial revisions outside the agreed scope will require a change order.
