# Project Proposal — Structural Drawings

## Project Understanding
You need assistance with: 
**Phase 192: Add bounded explicit-context workflow runner over Phase 190**

*Project description:*
## Goal

Implement the bounded explicit-context workflow runner proven source-change-free by Issue #415 / Phase 191.

Phase 190 now provides one reusable one-step cycle:

```text
prepare_next_step
      ↓
Phase 190
      ↓
prepare_next_step | workflow_complete | persisted_failure
```

Phase 192 must add a public runner that repeats **only public Phase 190** over a finite, explicitly supplied built-in tuple of per-step contexts.

The runner must be bounded by tuple length. It must never manufacture approvals, employees, tools, API keys, transports, or future contexts; never retry; never recurse; and never use an unbounded continuation loop.

Follow `AGENTS.md` and `docs/development/phase-implementation-contract.md`.

## Proven prerequisite

Issue #415 / Phase 191 passed source-change-free on exact main:

```text
9e0b921b40c0f86777101e83aa4371400ef5699f
```

with collect:

```text
12,200
```

The diagnostic proved:

- two explicit contexts execute two remaining steps with Phase190 exactly twice and reach `workflow_complete`;
- one explicit context executes one step and returns the exact resulting `prepare_next_step` by identity when the tuple is exhausted;
- terminal inputs consume zero contexts and call Phase190 zero times;
- first-context runtime failure returns durable `persisted_failure` and later contexts remain unused;
- second-context invalid preparation approval preserves the first context's terminal durable commit;
- second-context invalid execution approval preserves the second step's Phase147 durable `running` commit with no new terminal event;
- wrong context is passed through once and rejected by Phase190/lower public stages without runner repair;
- a finite built-in tuple provides a deterministic continuation bound;
- no runner-level state/event rollback is required;
- no approval/context generation is required;
- no production correction to Phase190 or lower boundaries is required.

Phase 192 must encode only this proven behavior.

## Freshness / preflight guard

Before tracked edits:

1. `git fetch origin`.
2. Confirm this Issue is OPEN and title matches exactly.
3. Confirm Issue #415 is CLOSED / completed and no equivalent/superseding bounded-runner Issue/PR exists.
4. Confirm latest `origin/main`.
5. If an existing worktree is dirty, do not reset/clean/stash/discard it; use a separate clean worktree from exact latest `origin/main`.
6. Confirm branch base / local base / merge-base / `origin/main` all match the implementation-time base.
7. Run `pytest --collect-only -q`; if main is unchanged, expect exactly `12,200`.
8. Inspect the exact current Phase190 implementation and focused suite.
9. Re-run a minimal source-change-free smoke proving two-context success and one-context exhaustion before tracked edits.
10. If Phase190 requires any production correction, an additional production file is needed, or the proven runner semantics no longer hold, STOP rather than expanding scope.

## New production module

Add exactly one new semantic production module:

```text
src/ai_office/engine/bounded_approved_workflow_runner.py
```

### Public immutable context

Add:

```python
@dataclass(frozen=True)
class ApprovedWorkflowContinuationContext:
    preparation_approval: object
    employee: object
    resolved_tools: object
    api_key: object
    execution_approval: object
    transport: object
```

This public model is only a structural holder for the six explicit values required by one Phase190 call.

Requirements:

- exact frozen dataclass;
- no validation that duplicates Phase145/155 ownership;
- no automatic approval generation;
- no employee/tool/key/transport lookup;
- no defaults that manufacture operational context;
- no mutation or future-step storage.

### Public runner

Add:

```python
def route_bounded_approved_workflow_continuation(
    result: object,
    workflow: object,
    state_path: object,
    events_path: object,
    contexts: object,
    *,
    phase190_function=route_approved_workflow_continuation_cycle,
) -> WorkflowProgressionDecision | PersistedExecutionOutcome:
    ...
```

Export the context dataclass, runner function, and Phase192 local public error/detail family from `src/ai_office/engine/__init__.py`.

## Exact input domain

Accepted exact result family:

- exact `WorkflowProgressionDecision(prepare_next_step)`;
- exact `WorkflowProgressionDecision(workflow_complete)`;
- exact `PersistedExecutionOutcome(persisted_failure)`.

Reject other result types/discriminators and subclasses/substitutes where exact domain types are required.

Require exact `WorkflowDefinition`.

### Terminal input short-circuit

For exact initial `workflow_complete` or exact initial `persisted_failure`:

- validate only the authoritative result/workflow linkage needed to establish a valid terminal object;
- return the exact same input object by identity;
- consume zero contexts;
- call Phase190 zero times;
- do not inspect or validate `state_path`, `events_path`, `contexts`, or `phase190_function`;
- do not mutate state/events;
- no retry/readvance.

### Prepare input

For exact `prepare_next_step`:

- require exact concrete `Path` state/event targets;
- targets must be distinct existing regular files;
- require `contexts` to be an exact built-in `tuple`;
- each member must be exact `ApprovedWorkflowContinuationContext`;
- empty tuple is valid and means execute zero steps, returning the exact input `prepare_next_step` by identity;
- require `phase190_function` callable.

Reject list, generator, iterator, subclassed tuple, or other context container forms.

Do not prevalidate the six fields inside each context beyond structural membership. Phase190 and its public lower stages own all step-specific semantics.

## Bounded runner semantics

Use only simple bounded iteration over the exact tuple.

Conceptually:

```text
current = result
for context in contexts:
    if current is terminal:
        return current
    current = Phase190(current, workflow, context..., state_path, events_path)
return current
```

Requirements:

1. One tuple element permits at most one Phase190 call.
2. Preserve tuple order exactly.
3. Never retry a context.
4. Never call Phase190 twice for one context.
5. Never reuse an already-consumed context.
6. Never construct or resolve a new future context.
7. If Phase190 returns `workflow_complete` or `persisted_failure`, return that exact object immediately and consume no later contexts.
8. If contexts exhaust while current remains `prepare_next_step`, return that exact current decision by identity.
9. Do not introduce a new `needs_context` discriminator in Phase192.
10. Do not add a separate max-step/default continuation limit. Tuple length is the exact continuation bound.

## Canonical Phase190 call

For one explicit context, call Phase190 exactly once with ten positional arguments and no kwargs:

```python
phase190_function(
    current,
    workflow,
    context.preparation_approval,
    context.employee,
    state_path,
    events_path,
    context.resolved_tools,
    context.api_key,
    context.execution_approval,
    context.transport,
)
```

Do not pass Phase190's lower dependency injections from the runner.

Phase190 is the **only operational dependency** of Phase192.

Do not directly call Phase145 / 146 / 147 / 155 / 172 or any lower route.

## Phase190 output seam

After each successful Phase190 call, require only a thin exact outer result contract before either returning or continuing:

- exact `WorkflowProgressionDecision(prepare_next_step | workflow_complete)`, or
- exact `PersistedExecutionOutcome(persisted_failure)`.

Validate authoritative workflow/current-step linkage and exact discriminator/string/built-in-int semantics sufficient to prevent an injected malformed Phase190 substitute from escaping or being used for another cycle.

Do not duplicate Phase190's full state/event/history validation.

The runner performs **no state/event snapshot capture, rollback, compensation, or writes** around Phase190. Phase190 owns all durable state/event semantics.

## Error ownership

Add a small Phase192 local public error/detail family only for runner-owned input/seam failures.

Suggested classifications:

- `result_type`;
- `workflow_definition`;
- `state_target`;
- `event_target`;
- `target_conflict`;
- `contexts_type`;
- `context_type`;
- `configuration`;
- `phase190_contract`;
- `dependency_error`.

Messages/details must be safe and must not expose IDs, approvals, credentials, API keys, instructions, model output, provider payloads, transport errors, filesystem contents, or raw exception text.

### Phase190/lower errors

Phase191 proved the runner does not need runner-level rollback or a broad new lower-stage safe-error family.

Requirements:

- default Phase190 and the lower safe errors it deliberately surfaces must preserve exact identity;
- runner must not translate recognized Phase190/lower safe failures into a new semantic error;
- runner performs no rollback when Phase190 raises;
- no retry;
- if an injected substitute Phase190 raises an unexpected non-safe exception, sanitize it as local `dependency_error` without state/event rollback.

To avoid a broad duplicate error tuple, inspect the actual current public Phase190 error surface and use the narrowest mechanism that preserves default Phase190/lower safe identity without copying the complete Phase190 lower-stage policy into Phase192. If this cannot be done cleanly without duplicating a broad private safe family, STOP and report rather than weakening safety.

## Boundedness / no hidden automation

Production must contain:

- no recursion;
- no `while` loop;
- no retry;
- no generator-driven continuation;
- no automatic approval creation;
- no employee/tool/API-key lookup;
- no transport creation;
- no scheduler;
- no finalizer;
- no parallel execution;
- no provider/network/paid API behavior;
- no CLI/GUI behavior.

A simple `for` over the exact built-in tuple is allowed and is the intended explicit deterministic bound.

## Required real-default behavior

Use deterministic synthetic transports only.

At minimum prove:

### A. Two contexts complete two remaining steps

- Phase190 exactly twice;
- context0 only first next step;
- context1 only second next step;
- each transport exactly once;
- first result `prepare_next_step`;
- second result exact `workflow_complete`;
- final succeeded state at last step;
- completed prefix all steps;
- event count increments exactly twice;
- no third call.

### B. One-context exhaustion

- Phase190 exactly once;
- context consumed exactly once;
- exact resulting `prepare_next_step` returned by identity;
- durable state/events remain at first completed continuation;
- no following-step activity.

### C. Empty tuple

For valid `prepare_next_step`:

- Phase190 zero-call;
- exact input object returned by identity;
- state/events unchanged.

### D. Terminal inputs

For `workflow_complete` and `persisted_failure` with deliberately invalid operational runner arguments:

- zero Phase190 calls;
- zero context consumption;
- exact input identity;
- operational arguments ignored.

### E. First-context runtime failure

- Phase190 once;
- durable persisted_failure exact result;
- later context unused;
- no retry.

### F. Second-context preparation approval failure

- first context success durable;
- second Phase190 call raises exact recognized safe error;
- prior first-step terminal state/events unchanged;
- later context unused;
- no runner rollback/retry.

### G. Second-context execution approval failure

- first context success durable;
- second step reaches Phase147 durable running state;
- Phase155 safe failure identity preserved;
- state remains running at second step;
- event log remains at prior terminal count;
- no runner rollback/retry.

### H. Wrong-step context delegation

- runner does not inspect/repair the context fields semantically;
- Phase190 receives it exactly once;
- Phase190/lower stage rejects it;
- later context unused.

### I. Structural context/container rejection

Reject without Phase190 call:

- list instead of tuple;
- tuple subclass;
- generator/iterator;
- tuple member not exact context dataclass;
- context dataclass subclass if possible;
- non-callable dependency.

### J. Malformed injected Phase190 output

- exact-looking context is consumed once;
- malformed result is locally rejected as `phase190_contract`;
- no second context consumed;
- no runner rollback/write.

## Focused tests

Add:

```text
tests/test_bounded_approved_workflow_runner.py
```

Keep exactly **20 collected Phase192 tests**. Inline matrices/subcases where needed so the focused count remains exactly 20.

Expected final collect if base remains unchanged:

```text
12,220
```

## Exact changed files

Exactly five files:

1. `src/ai_office/engine/bounded_approved_workflow_runner.py`
2. `tests/test_bounded_approved_workflow_runner.py`
3. `src/ai_office/engine/__init__.py`
4. `README.md`
5. `docs/architecture.md`

Do not modify Phase190 or any existing production module. If a sixth file or Phase190 correction is required, STOP.

## Validation

Run at minimum:

```bash
pytest -q tests/test_bounded_approved_workflow_runner.py
pytest -q tests/test_approved_workflow_continuation_cycle.py
pytest --collect-only -q
pytest -q
ruff check src tests
ruff check .
python -m compileall -q src tests
git diff --check
ai-office --help
```

Also run directly related runner/Phase190/orchestration/provenance suites as appropriate.

Expected if base remains unchanged:

- Phase192 focused: exactly `20 passed`;
- collect: exactly `12,220`;
- full: exactly `12,220 passed`.

Use synthetic transports only. No real provider/network/paid API/external-tool execution.

## Development authorization

The human requester explicitly authorizes normal development actions for this Issue:

- create/use a fresh clean branch/worktree from current `origin/main`;
- edit the exact five authorized files;
- run tests/validation;
- create normal commits;
- push the Phase192 branch;
- create a **Draft PR** referencing/closing this Issue.

Do not amend, rebase, squash, or force push.

Do not mark Ready, merge, close this Issue manually, delete the branch, or implement Phase193 without ChatGPT review/approval.

## Required completion report

Return a concise `ChatGPTへ貼り付けるメッセージ` with:

- preflight base/merge-base/collect;
- exact public API and context dataclass;
- changed files;
- focused/related/full/collect/ruff/compileall/diff/help results;
- real-default A–J summary;
- Phase190 call/context-consumption/transport counts;
- durable state/event ownership summaries;
- confirmation of no runner rollback/retry/context generation/unbounded loop;
- commit SHA;
- branch;
- Draft PR number/URL;
- latest-head CI run/status;
- Issue state;
- confirmation Ready/merge/close/branch deletion/Phase193 were not performed.

## Scope of Work
- Asset analysis and workspace initialization.
- Core modeling / development based on specifications.
- Technical validation and quality checks.
- Incorporation of review feedback.
- Clean handover of source files and documentation.

## Required Files & Inputs
1. Complete reference files (drawings, access tokens, test data).
2. Exact dimensional specs or business rules.
3. Schedule/deadline expectations.

## Estimated Price and Timeline
- **Estimated Price:** 800 - 2000 EUR
- **Estimated Timeline:** 3 to 7 business days (to be refined after reviewing the final assets).

## Project Questions
To help me refine this estimate, please clarify:
1. Avez-vous déjà réalisé l'étude de sol géotechnique pour les fondations ?
2. Quelles sont les charges d'exploitation particulières (machines, toiture végétalisée) ?
3. Fournissez-vous les plans d'architecte définitifs au format DWG ?
4. Quels sont les détails d'exécution attendus (nomenclatures d'acier, détails de ferraillage) ?
5. Quel est votre calendrier souhaité pour la validation des plans ?

## Agreement Terms
The final source files will be delivered upon approval of the milestones. Substantial revisions outside the agreed scope will require a change order.
