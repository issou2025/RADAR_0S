# Project Proposal — Unknown

## Project Understanding
You need assistance with: 
**Complete generated-advisory identity and review-producer integrity binding**

*Project description:*
Refs #4139

## Context

PR #4174 implemented the main security-pass integrity design, but its final review found residual bypasses in how trusted issue identity and review commit producers are bound. Continue from the closed PR's head and make only the scoped corrections below; do not redesign the completed merge-tree, waiver-key, ownership, advisory-queue, or footer-reconciliation work.

## Blocking defects

- SEVERITY: BLOCKER: in the closed PR (#4174), `.github/workflows/plan.yml:464-482` exports issue author identity through `GITHUB_ENV` but does not publish it as immutable fetch-step outputs. The full-access planner can append replacement identity values before the generated-advisory guard runs.
- SEVERITY: BLOCKER: in the closed PR (#4174), `.github/workflows/implement.yml:133-136` establishes the same mutable identity source. Both the preflight guard and `implement_commit_changes.sh` consume those values after the full-access implementation agent has run.
- SEVERITY: BLOCKER: in the closed PR (#4174), `.github/workflows/review_autofix.yml:1786-1799` refreshes only the metadata collector and validator for stale branches. `review_commit_changes.sh` and `review_rb_judge.sh` can remain branch-first pre-hardening copies, after which the workflow merely hashes and certifies those stale producers.
- SEVERITY: BLOCKER: in the closed PR (#4174), `scripts/review_rb_judge.sh:2048-2057` invokes the scope validator after the full-access writer without rechecking `REVIEW_SCOPE_GUARD_EXPECTED_SHA256`.

## Implementation guidance

- Publish issue author association and login as outputs from the same pre-agent metadata steps that publish generated-advisory status and body/helper digests.
- Pass those outputs directly through the plan guard, implementation preflight, and implementation commit-step `env` blocks. Do not use values recovered from `GITHUB_ENV` as authorization inputs.
- Extend the stale-branch compatibility overwrite so the collector, validator, `review_commit_changes.sh`, `review_conflict_prepare.sh`, `review_conflict_resolve.sh`, and `review_rb_judge.sh` come from one main-primary snapshot before their digests are published.
- In `review_rb_judge.sh`, recompute the validator digest after the writer returns and before invoking it. Missing, malformed, or mismatched expected or actual digests must reject the commit.
- Preserve all existing public identifiers and fail-closed exit-code behavior.

## Acceptance criteria

- A planner or implementer that appends forged `ISSUE_AUTHOR_ASSOCIATION` or `ISSUE_AUTHOR_LOGIN` values to `GITHUB_ENV` cannot make an untrusted generated advisory pass.
- Both implementation scope-guard call sites receive author identity from immutable metadata-step outputs.
- A stale PR branch cannot pair fresh advisory metadata validation with pre-hardening editor or judge commit producers.
- Replacing `files_touched_scope_guard.py` during the review-blocked writer run causes the judge-fix commit to fail closed.
- Add focused regression coverage for forged environment identity, stale producer staging, and post-writer validator mutation.
- Existing generated-advisory, review pipeline, shell syntax, YAML parsing, and direct-runner contract tests pass.

---
**Orchestrator metadata** (do not edit)
- Tracking issue: #4139
- Integration branch: `orchestrator/project-4139`
- Local ID: `security-pass-fix-cycle-1`
- Priority: 1
- Managed by: AI Orchestrator

---
**Review-blocked reissue metadata**
- Replaces: #4173 (PR #4174 closed — approach rework)
- Type: review-blocked-reissue
- prior_pr_baseline_branch: ai/reissue-baseline/pr-4174-bb26b86aba7c-35610007679-1
- files_touched:
  - .github/workflows/plan.yml
  - .github/workflows/implement.yml
  - .github/workflows/review_autofix.yml
  - scripts/review_rb_judge.sh
  - .ai/.workspace_source_manifest.txt
  - README.md
  - agents.md
  - changelog.d/4173-security-pass-integrity-hardening.md
  - scripts/files_touched_scope_guard.py
  - scripts/implement_commit_changes.sh
  - scripts/orchestrate_poll_process.sh
  - scripts/review_collect_pr_metadata.sh
  - scripts/review_commit_changes.sh
  - scripts/review_conflict_prepare.sh
  - scripts/review_conflict_resolve.sh
  - scripts/security_audit.sh
  - scripts/stage_workflow_support.sh
  - tests/test_files_touched_scope_guard.py
  - tests/test_implement_post_codex_recovery.py
  - tests/test_orchestrate_poll_process.py
  - tests/test_plan_template_owned_path_guard.py
  - tests/test_review_autofix_review_pipeline_contract.py
  - tests/test_security_audit_workflow_contract.py

## Scope of Work
- Asset analysis and workspace initialization.
- Core modeling / development based on specifications.
- Technical validation and quality checks.
- Incorporation of review feedback.
- Clean handover of source files and documentation.

## Required Files & Inputs
1. Complete reference files (drawings, access tokens, test data).
2. Exact dimensional specs or business rules.
3. Schedule/deadline expectations.

## Estimated Price and Timeline
- **Estimated Price:** 300 - 800 USD
- **Estimated Timeline:** 3 to 7 business days (to be refined after reviewing the final assets).

## Project Questions
To help me refine this estimate, please clarify:
1. Could you describe the project scope and deliverables in detail?
2. Do you have any existing drawings or reference documents?
3. What is your target budget range?
4. What is your expected timeline?
5. Which tools/software do you prefer for this project?

## Agreement Terms
The final source files will be delivered upon approval of the milestones. Substantial revisions outside the agreed scope will require a change order.
