# Project Proposal — Unknown

## Project Understanding
You need assistance with: 
**Phase 7: add repo_root and generate_resource_id helper scripts**

*Project description:*
Implement Phase 7 (Theme G) from `docs/plans/awesome-claude-code-learnings-plan.md`. Assume the earlier Stable-ID/override phases have landed; this phase adds reusable helpers without changing existing identifier contracts.

Scope:
- Create `scripts/repo_root.py` with `repo_root()` and `repo_root_from(start: Path)` that walk upward looking for a directory containing both `CLAUDE.md` and `.git/`, plus a CLI that prints the resolved root and exits non-zero on failure.
- Create `scripts/generate_resource_id.py` with `generate_id(prefix, salt)` that delegates to `make_record_id(prefix)` when `salt` is absent, and otherwise emits the planned sanitized-prefix + UTC timestamp + first-10-hex-of-sha256(salt) form. Include the simple CLI from the plan.
- Add `tests/test_repo_root_helper.py` and `tests/test_generate_resource_id.py` covering the normal, failure, deterministic-salt, unsalted-variability, and prefix-sanitization cases from the plan.
- Edit `agents.md` and add `## Utility helpers` immediately after the Override conventions section.

Acceptance criteria:
- `repo_root()` / `repo_root_from()` resolve the repo root from nested directories and fail clearly when the markers are absent.
- `generate_id()` preserves the existing format contract and clearly distinguishes salted vs unsalted behavior.
- The new tests cover the contract described in the plan without changing `make_record_id`.

Constraints:
- Stay inside `files_touched`.
- `agents.md` is a shared hot file: insert the new section immediately after Override conventions and keep the rest untouched.
- Do not rename or replace the existing `make_record_id` helper; this phase only wraps it.

Validation:
- Run the two focused helper test modules and confirm the generated IDs still match the Phase-5 regex contract.

---
**Orchestrator metadata** (do not edit)
- Tracking issue: #3601
- Integration branch: orchestrator/project-3601
- Local ID: `phase7_repo_root_id_helpers`
- Priority: 5
- Managed by: AI Orchestrator

## Scope of Work
- Asset analysis and workspace initialization.
- Core modeling / development based on specifications.
- Technical validation and quality checks.
- Incorporation of review feedback.
- Clean handover of source files and documentation.

## Required Files & Inputs
1. Complete reference files (drawings, access tokens, test data).
2. Exact dimensional specs or business rules.
3. Schedule/deadline expectations.

## Estimated Price and Timeline
- **Estimated Price:** 300 - 800 USD
- **Estimated Timeline:** 3 to 7 business days (to be refined after reviewing the final assets).

## Project Questions
To help me refine this estimate, please clarify:
1. Could you describe the project scope and deliverables in detail?
2. Do you have any existing drawings or reference documents?
3. What is your target budget range?
4. What is your expected timeline?
5. Which tools/software do you prefer for this project?

## Agreement Terms
The final source files will be delivered upon approval of the milestones. Substantial revisions outside the agreed scope will require a change order.
